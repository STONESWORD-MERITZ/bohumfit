fastapi==0.138.1
uvicorn==0.48.0
pdfplumber==0.11.9
pandas==2.3.3
# BOHUMFIT-140: 재현성 위해 핵심 전이 의존성도 명시 고정(설치본 기준). pydantic=fastapi, pillow=pdfplumber 경유.
pydantic==2.13.4
pillow==12.2.0
python-multipart==0.0.29
google-genai==2.6.0
python-dotenv==1.2.2
slowapi==0.1.9
sentry-sdk[fastapi]==2.60.0
httpx==0.28.1
# BOHUMFIT-069/070: 구독·사용량 게이트 + 토스페이먼츠 — Supabase 서비스롤 클라이언트.
supabase==2.31.0
# BOHUMFIT-030 리포트 PDF: HTML 템플릿 + 헤드리스 Chromium 렌더.
# 배포(Railway) 빌드 단계는 nixpacks.toml에서 `python -m playwright install --with-deps chromium` 실행.
# 한글 폰트: 시스템 패키지 fonts-noto-cjk 설치(또는 backend/fonts/ 에 Noto CJK 파일 배치).
jinja2==3.1.6
playwright==1.52.0
