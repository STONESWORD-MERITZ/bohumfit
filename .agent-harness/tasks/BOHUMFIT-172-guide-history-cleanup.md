# BOHUMFIT-172 — 필터 가이드에 히스토리 설명 추가 + 히스토리 진입점 중복 정리

Owner flow: Claude Chat -> Claude Cowork -> Codex
Current owner: Cowork

## 배경 (Human 확정 — 실화면 확인 기준)
171b 배포 후 /disclosure 화면에 "분석 히스토리" 진입점이 2개 중복 노출:
① 상단 설계사용/고객용 모드 탭 줄 우측 "분석 히스토리 →" (156b, DisclosureHub)
② 그 아래 줄 "분석 히스토리" 버튼 + "필터 가이드 다시보기" 버튼 (171b Step 4, Disclosure)
또한 필터 가이드(투어)에 히스토리 기능 설명이 없음.

## 요구 2건
1. 필터 가이드에 "분석 히스토리" 사용법 설명 추가
2. 히스토리 진입점을 1개로 통합 — 상단 모드 탭 줄에 정렬

## Step 0 — 진단
- 두 진입점의 렌더 위치·컴포넌트 구조 매핑 (DisclosureHub vs Disclosure)
- 필터 가이드(투어) 구현 방식: 스텝 정의 위치·형식(TourStep 배열·타깃 방식)

## Step 1 — 진입점 통합
- 상단 모드 탭 줄 우측에 [분석 히스토리] [필터 가이드 다시보기] 나란히 (세컨더리/고스트 통일, v1.1 토큰)
- 아래 줄의 중복 버튼 행 제거
- 모바일 폭: 자연 줄바꿈 (겹침/가로 스크롤 금지)
- 결과 화면 쪽 히스토리 링크(171b Step 4)는 화면이 다르므로 유지 — ※ Step 0 확인 결과
  171b의 링크는 결과 화면 전용이 아니라 상단 공통 행이므로 "제거 대상 = 아래 줄 행 전체"로 해석

## Step 2 — 필터 가이드에 히스토리 스텝 추가
- 기존 스텝 형식 그대로 1개 추가, 타깃: 상단 [분석 히스토리] 버튼
- 내용 취지: "분석 결과는 최근 10건이 자동 기록(7일 보관)되고, 남기고 싶은 결과는 저장하면
  90일간 보관됩니다. 분석 히스토리에서 다시 확인할 수 있어요."
- 순서: 분석 흐름 설명 뒤, 마지막 부근

## 수정 금지
- 히스토리 API·저장/승격 로직 / 분석 파이프라인 / 모드 탭 동작
- 가이드 투어의 기존 스텝 내용 변경 금지 (추가만)

## 검증
- 진입점 grep 1개(/disclosure 화면 기준) / 투어 스텝 +1·기존 무변경 / raw gray 0 / 모바일 줄바꿈
- tsc/build/pytest = Codex 권위 (기준선 480/8 — 백엔드 무변경)

## 커밋 (Codex)
fix(BOHUMFIT-172): 히스토리 진입점 통합 + 필터 가이드에 히스토리 안내 추가
