# BOHUMFIT-250 — 엑셀 산출물 시각 디벨롭 (구조=비분양식 · 색=보험핏 브랜드)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: Claude Code (구현·1차 검증)
Risk tier: 중위험 — 풀 하네스. git 쓰기 금지(커밋 Codex). 실 PDF·엑셀 원본 로컬 참조만·stage 금지.
★값·구조 불변 — 스타일 계층만(전/후 재판독 diff 0 증명).
Date: 2026-07-27

## 진행 기록 (Claude Code · 2026-07-27)

### S0 — 3자 실측(원본 양식 · 구 exporter 선례 · 브랜드 토큰)

**① 비분양식 원본 fill 전수(openpyxl 실측)**
- 시트2 값 행 강조(연노랑 FFFFF7CC — 값 셀 포함 행 전체): **일반사망(10)·상해/질병후유장해
  (14·15)·암진단금/유사암진단금(16·17)·뇌혈관질환(24)·심혈관질환(28)·일반종수술 1~5종
  (33~37)·보험료 합계(50)** — "대분류 선두 행 강조" 패턴.
- 시트2 특수 강조(파랑 theme4±0.4): **암 주요치료비(23)·순환계 치료비(32)** — 신담보/특이
  항목 구분.
- 시트2 헤더(3~5행·담보명 열 전부): FFFFF7CC. 데이터 셀: 흰색+전체 그리드 테두리.
- O열 고객정보 패널(인쇄영역 밖): `고등전산`(O2)·`보험나이`(O3)·`1.성명 : `~`8.병원`
  (O6~O14, O8 없음)·`[양식]`(O17·bold)·`손해(건강)`/`20년/20년`/`20년/100세`(O22~24).
- 시트3: 헤더(4행) FFDDEBF7 · 라벨 열 강조가 불규칙 혼재(theme4/5/7/9 파스텔 — 일반사망·
  후유장해·암진단금·뇌/심혈관질환·종수술·수술·입원·운전자특약·실손 등 섹션 선두 계열,
  행35 등 일부 임의 누락) · 보험료 행(45) FFE7E6E6 · [후] 값 K열 빨강(FFC00000).

**② 구 exporter FIT 선례(git show c755a43 — 읽기 전용)**: EMERALD FF084734·EMERALD_SOFT
FFEEF6F1·INK FF0A0A0A·AMBER_TX FFB45309·GRAY_SOFT/TX·화이트 헤더 폰트 — FIT v1.1 상수 재사용.

**③ 브랜드 토큰(src/index.css 실측)**: 에메랄드 파인 #084734(면+흰 글자 대비 10.7:1)·
라임 글로우 #CEF17B(★면 전용)·그린 티 #CDEDB3(★면 전용)·잉크 #0A0A0A.

**치환 규칙(근거)**
| 원본 | FIT 치환 | 근거 |
| --- | --- | --- |
| 연노랑 헤더/강조(FFFFF7CC) | 헤더=에메랄드 면+흰 글자 / 강조 행=그린 티 면 | 브랜드 정합·면 전용 규칙 준수 |
| 파랑 특수 강조(theme4) | 라임 글로우 면 | "특수 강조=라임 면"(패킷 예시) |
| 시트3 파스텔 혼재 | 라벨 열 기본=EMERALD_SOFT, 강조=시트2와 동일 세트(그린 티/라임) | 원본의 불규칙 누락(행35 등)을 섹션 규칙으로 정규화 — 시각 의도(선두 강조) 보존·일관화 |
| [후]·차액 빨강(FFC00000) | 개선(증가/절감)=에메랄드 파인 텍스트·악화=AMBER_TX | FIT 팔레트에 빨강 부재 — 구 exporter의 상태 색 선례(EMERALD/AMBER) 준용. 근거 기록 |

### 구현

- **`excel_style.py` 신설(스타일 단일 소스)**: FIT 팔레트(EMERALD/EMERALD_SOFT/GREENTEA/
  LIME/INK/GRAY/AMBER_TX — index.css·구 exporter 선례) + `FILL_ONLY_COLORS` 계약(라임·그린 티
  면 전용) + 테두리 프리셋(BORDER_GRID/BOX) + `HIGHLIGHT_ITEMS`(원본 강조 행 실측 세트)·
  `SPECIAL_ITEMS`(암 주요치료비·순환계) + `PANEL_LABELS`(O열 고객정보 라벨 원문).
- **A 시트2**: 헤더(담보명·합계·계약 라벨) 에메랄드 면+흰 글자(자동 규칙 — fill=EMERALD면
  폰트 흰색), 강조 담보 행 = 값 셀 포함 행 전체 그린 티(원본 위치 그대로)·특수 2행 라임,
  전체 그리드 테두리 톤 정리, 담보명 가운데 열폭 9→12 + 종수술 행높이 26(긴 라벨 겹침 해소 —
  ★셀 값은 불변, 레이아웃으로만 해결), **우측 고객정보 패널 재현**(원본 O열 라벨 전수 —
  값은 설계사 수기용 공란·PII 미기입·인쇄영역 밖), 부록 스타일 통일.
- **B 시트3**: 라벨 기본=에메랄드 소프트·강조=시트2와 동일 세트(원본의 불규칙 파스텔을 섹션
  규칙으로 정규화 — S0 치환표 근거), [후] 값 = 에메랄드 볼드(원본 빨강 대체 — FIT 팔레트에
  빨강 부재·구 exporter 상태색 선례 준용), 보험료 차액 박스(절감=에메랄드·증가=앰버 +
  BORDER_BOX), 특이사항 헤더 박스.
- **C 표지**: 타이틀 "보험 보장 분석 리포트" 에메랄드 강조.
- **D 인쇄**: `fitToWidth=1`(15계약 가로 1장 폭 — 248 사양 결정 3의 잠정 처리·기록),
  인쇄영역·틀고정(B10) 기존 유지.

## 검증 체크리스트 (1차 — 완료 · 2026-07-27)
- [x] backend pytest — **712 passed, 8 skipped**(710 + 신규 2: 스타일 존재·면 전용 색 계약)
- [x] ★5케이스 값 diff 0 — HEAD(a9dcad9) exporter를 별도 프로세스로 실행한 구 산출물과
      스타일본을 전 시트·전 셀 좌표 대조(구 산출물의 모든 값 셀 = 신 산출물 동일 값.
      신규 추가는 패널 라벨뿐). A~E 전부 diff 0. 임시 실데이터 파일은 검증 후 즉시 삭제.
- [x] 면 전용 색(라임·그린 티) 폰트 사용 0 — 5케이스 전 시트·전 셀 검사 + 합성 회귀 테스트로 고정
- [x] 스타일 존재 5케이스: 헤더 에메랄드+흰 글자·강조 행 그린 티·특수 행 라임·패널 라벨 전부 OK
- [x] tsc·lint·npm test **89** 불변(프런트 무접촉)
- [x] PII 0, diff 범위 = export_excel·excel_style(신설)·테스트 1 + harness만

## Stage 목록 (Codex용)
export_excel·스타일 모듈·테스트, tasks/BOHUMFIT-250-*.md, handoff.md, locks.md

## 커밋 메시지 (Codex용)
feat(BOHUMFIT-250): 엑셀 시각 디벨롭 — 비분양식 구조 강조 재현 + FIT 브랜드 팔레트
