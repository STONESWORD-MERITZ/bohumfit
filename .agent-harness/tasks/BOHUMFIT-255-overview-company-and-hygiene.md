# BOHUMFIT-255 — overview 회사별 복원 + 스모크 세트 상비 + 간편 시트 조사 (3파트)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: Human(P1 판정 결정) / Chat(P3 결정지)
Risk tier: 혼합 — 풀 하네스. git 쓰기 전면 금지(커밋은 Human 복귀 리뷰 후 Codex).
실 PDF·엑셀 원본 로컬 참조만·stage 금지. 파트 독립.
Date: 2026-07-29

## P1 — overview 문서 회사별 열 복원 (★핵심·고위험)

### S1 조사 결과 (실측 · 2026-07-29)

**① 문서 구조**: overview 정본 30페이지 = detail 16 · 미분류 9 · overview 2 · contracts 2 ·
diagnosis 1. 계약 **15개**, matrix 27키(전부 `overview:True`), extra 16키.
- detail 페이지 16개 중 **15개는 계약 귀속 성공**(253 유일성 판정), 1개(p6) 실패.
- 미분류 9페이지는 **담보 매칭 행 0**(금액 행은 있으나 담보 표가 아님) → 담보 손실원 아님.
- ★즉 "overview 문서에도 detail(상품별 가입담보상세)이 있다"는 239 힌트는 **사실**이며,
  detail에서 표준 KB 담보 **69행(16종)** + EXTRA 34행이 계약 귀속과 함께 검출된다.

**② ★대사 판정 — 26종 중 7종만 정확 일치(금액 38%)**
| 구분 | 종수 | 금액 | 비고 |
| --- | --- | --- | --- |
| 대사 일치(귀속 가능) | **7** | 536,400,000 | 질병사망·질병후유장해·허혈성심장질환·교통사고처리지원금·변호사선임비용·자동차사고부상·화재벌금 |
| 불일치(귀속 금지) | 9 | — | 상해사망·상해후유장해·유사암진단금·뇌혈관질환·상해수술·질병수술·질병입원·상해입원·골절진단비 |
| detail 부재 | 10 | — | 암진단금·표적항암치료·벌금·가족일상배상·실손 4종·3대비급여실손·보철치료비 |
- overview 담보 26종 합계 1,400,240,000(전체 enrolled 총액은 1,542,990,000).

**③ 불일치 원인 — 파싱 버그 아님, ★매칭 과포함 + 원문 누락(양방향)**
- **과포함(대부분)**: detail 원문명이 표준 담보로 매핑될 때 **한정 담보를 흡수**한다.
  상해사망 실측: overview 332,100,000 vs detail 392,100,000 — 초과분 **정확히 60,000,000 =
  `운전자용 교통상해사망` 5,000만 + `화재상해사망` 1,000만**. 유사암진단금도 `암진단비
  (유사암제외)`·`26종 항암방사선및약물치료비`·`암 통합치료 생활비` 흡수로 22,000,000 →
  93,200,000. 질병수술은 `124대질병(…)수술비` 등 특정질병수술 흡수(800,000 → 5,900,000).
  질병/상해입원은 `1인실·2-3인실 입원특약`(상급종합·종합) 흡수(20,000 → 460,000).
- **원문 누락(역방향)**: 골절진단비는 overview 1,400,000 > detail 900,000 — detail이 계약의
  전 담보를 담지 못하는 경우가 존재(귀속 페이지 실패 1건 포함).
- **★가설 검증·기각**: "detail의 분류(지급사유) 필드로 매핑하면 정밀해진다"를 3규칙으로 실측
  (R1 이름 / R2 분류 우선 / R3 분류 엄격) — 일치 수 **7 / 7 / 3**으로 개선 없음(R3은 악화).
  `교통상해사망`·`특정상해사망` 같은 분류값도 매칭 계층에서 `상해사망`으로 흡수되기 때문.

### ★판정: **제한적 가능(부분) — 단, 이번 태스크에서는 구현 보류·Human 결정 요청**
- 근거: 정확 귀속 가능 담보가 **26종 중 7종(금액 38%)**뿐이다. 부분 구현 시 엑셀에 15개 회사
  열이 생기지만 **금액의 62%가 "계약 미확인"에 몰린다** — 252가 반려됐던 "회사 열은 있는데
  값이 없어 보이는" 오독 위험과 동종이며, 실무 가치 대비 위험이 크다.
- 근본 해법은 **detail 원문명 → 표준 담보 매칭 정밀화**(한정 담보 분리: 교통/화재 상해사망,
  특정질병수술, 입원일당 계열)인데, 이는 245 패턴·246 분류 체계 변경이라 **표준 문서의
  EXTRA 검출·매트릭스 매칭까지 회귀 위험**이 있다. 이 태스크의 수정 금지("246 대사 변경
  금지"·표준 경로 무영향)와 정면 충돌하므로 **별도 고위험 태스크**로 분리해야 한다.
- 패킷의 "부분이면 구현" 조항을 문자 그대로 따르지 않은 이유: 실측된 '부분'의 실체가
  기대(대부분 복원·일부 미확인)와 **반대**(대부분 미확인)여서, 전제 반전은 보고 후 결정이
  옳다고 판단(251·252 선례와 동일). 코드 변경 0으로 남겼다.

### 안전 구현 설계안 (Human 승인 시 그대로 착수 가능)
1. **귀속 게이트**: 담보별 `detail 계약 합 == overview summary`일 때만 by_company 채움.
   불일치·부재 담보는 전액 `'?'`(계약 미확인 — 253 모델)로 귀속 → **회사합(실계약+'?')
   = 합계**가 전 담보에서 성립(제1원칙 충족·오귀속 0).
2. **렌더 가드 2곳 완화 필요**(현행은 overview면 회사 열 자체를 만들지 않음):
   `export_excel._sheet_compare_form`의 `b_companies = [] if overview else …`와
   `_unknown_bucket_present`의 `row.get("overview")` 제외 규칙.
3. **불변 유지**: overview summary·239 fallback·246 대사·전=후 이월 무변경(by_company 채움만),
   표준 문서는 overview 플래그 가드로 무영향.
4. 선행 권장: 매칭 정밀화 태스크(아래 규모 M~L)를 먼저 하면 귀속률이 크게 오른다.

### 규모 산출(후속 태스크용)
- **255-A 매칭 정밀화**(선행 권장): 한정 담보 분리 규칙 + 표준 5케이스 회귀. **L**(고위험).
- **255-B overview 회사별 렌더**: 위 설계안 1~3. **M**(255-A 이후면 가치 급상승).

## P2 — 스모크 정본 세트 상비 (완료)
- 삭제 사고 재발 방지를 위해 `.agent-harness/verify.md`에 **"스모크 정본 세트"** 섹션 신설:
  2케이스의 성격·경로·**회귀 기준 수치**·재확보 절차·PII 규칙을 문서화(파일 자체는 계속
  `.gitignore` 유지 — 커밋 금지).
- 실측 기준값(2026-07-29 · HEAD `8e63c1e`):
  | 케이스 | 유형 | 계약 | 담보행/enrolled | 총액 | 월납(부값) | overview행 | 경고 |
  | --- | --- | --- | --- | --- | --- | --- | --- |
  | 표준(구 B) | 계약별 매트릭스 | 15 | 46/29 | 604,560,000 | 681,312 (531,312) | 0 | 0 |
  | overview(구 E) | 합계-only(239) | 15 | 54/42 | 1,542,990,000 | 4,675,189 (4,675,189) | 26 | 1 |
- 두 케이스 모두 해지 0에서 **전=후 상이 0** 확인.
- ★인접 정정 1건(사유 기록): `verify.md`·`AGENTS.md`·`CLAUDE.md`의 게이트 기준선이 242 시점
  값(`684 passed, 8 skipped` / `79 passed`)으로 **크게 낡아 있었다**(현행 750/8 · 95). 검증
  상비가 목적인 P2에서 낡은 기준선을 두면 "테스트가 사라졌다"는 오판을 부르므로 3개 문서를
  함께 갱신했다(CLAUDE.md 규칙 "기준선 변경 시 verify.md·AGENTS.md 동시 갱신" 준수 —
  1개만 고치면 오히려 불일치가 커진다).

## P3 — 간편 시트 조사 (완료 · 구현 0)
**① 구조 실측**(목표 양식 `…-OUTPUT.xlsx`의 `간편` 시트, 병합 24+·최대행 49)
- 시트2(비교분석표)와 **동일 스키마**: 3행 `[전]/담보내용/[후]` · 4행 회사명 · 5행 상품명 ·
  6~8행 구분/가입일/납만기 · 9행~ 담보 · 44~47 Y/N · **48행 보험료 합계** · 49행 가설계 ·
  O열 고객정보 패널(고등전산·보험나이·1.성명~8.병원).
- ★차이: **[전]이 비어 있고(회사 열 없음·합계만) [후]에 신계약 3건**(손해(건강)·손해(운전자)·
  생명(건강), 전부 `가입일=신계약`). 즉 **간편심사 상품으로 짠 [후] 신규 설계 제안 시트**.
**② 251(간편심사 고지 문안)과의 관계**: **직접 연계 아님**. 251은 유병자 3-10-5 **고지 문안**
(카카오 텍스트) 경로이고, 간편 시트는 **보장 설계안**이다. 접점은 "고객이 간편심사 대상"이라는
동일 맥락뿐 — 데이터·산출 경로는 분리돼 있다.
**③ 현행 시스템과의 관계·규모**
- 현행 3시트에는 없다. 252/254의 `[후] 신규 설계 반영 대상` 골격 열이 이 영역이며, 가입제안서
  트랙(193 proposal)이 채우면 [후] 회사 열이 생긴다. 간편 시트는 그 [후]를 **별도 시트로 분리**해
  "표준 심사안 vs 간편 심사안" 2안을 병렬 제시하는 형태다.
- 필요 데이터: 기존 proposal 파싱·FORM 40행·Y/N·보험료는 **이미 보유**. 없는 것은 **제안을
  표준안/간편안 2트랙으로 구분하는 메타**(어느 제안이 간편심사 상품인지)와 그 입력 UI.
- 규모: **M** — 백엔드 S(시트2 렌더 함수 재사용 + `[전]` 공란 모드 · 시트 1개 추가) +
  프런트 M(제안 트랙 구분 입력·표시). 제안: Chat이 후속 태스크로 발번 여부 결정.

## 검증 체크리스트 (1차 — 2026-07-29)
- [x] P1: S1 판정 문서화(위 표·원인·가설 기각·설계안·규모). **구현 0**이므로 신규 테스트 없음
- [x] P2: verify.md 스모크 세트 섹션 신설·기준 수치 기록·PII 0(수치만·주민/전화/주소 0)
- [x] P3: 간편 시트 실측·251 관계 분석·규모(M) 산출 · **구현 0**
- [x] backend pytest **750 passed, 8 skipped** · tsc app/node·lint · npm test **95**(무변경 재현)
- [x] PII 0(문서 실명 마스킹 — 파일 경로도 `이*숙`/`라*실`) · **코드 diff 0**
      (pipeline/·coverage/·src/ 전부 무변경) · diff 범위 = 문서 6개

## Stage 목록 (Codex용)
- 커밋1(P1): **없음 — 코드 변경 0**(판정 보고만)
- 커밋2(P2·P3): `.agent-harness/verify.md`, `AGENTS.md`, `CLAUDE.md`(기준선 정정 — 사유 위 기록),
  `tasks/BOHUMFIT-255-*.md`, `handoff.md`, `locks.md`

## 커밋 메시지 (Codex용)
docs(BOHUMFIT-255): overview 회사별 귀속 판정(제한적 가능) + 스모크 정본 세트 + 간편 시트 조사

## Next (handoff 명시)
① Human 복귀 — ★P1 결정: (a) 매칭 정밀화(255-A·L) 선행 후 회사별 렌더(255-B·M),
   (b) 지금 부분 구현(귀속 7종·나머지 계약 미확인), (c) 보류 중 택1
② Chat — P3 간편 시트 후속 태스크 발번 여부 결정(규모 M)
③ Codex — 문서 커밋(코드 변경 0이라 게이트는 현행 재현으로 충분)
