# BOHUMFIT-261 — 엑셀 표지 리디자인 + 20년 총납입 차액 + 리포트 가독성(고령 고객)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: Codex (2차 검증·커밋)
Risk tier: 중위험 — 풀 하네스. git 쓰기 금지(커밋 Codex). 실 PDF·엑셀 로컬 참조만·stage 금지.
★값·집계 불변 — 표시 계층만.
Date: 2026-07-31 · 기준 HEAD `3384e6e`

## 배경 (2026-07-31 Human 실물 검수)
엑셀 회사별 전개·overview까지 완성됨. 남은 개선: 표지가 라벨+공란으로 밋밋, 상담 임팩트용
20년 기준 총납입 차액 부재. 리포트 PDF는 ★50~60대 고객이 직접 보는 자료인데 폰트가 작고
섹션이 페이지 중간에서 끊김.

## S0 실측
- **엑셀 표지**: `B1/B2` 제목 2줄 + 4~7행 설계사 라벨(`● 본인이름`·`● 직급:`·`● E-MAIL :`·
  `● TEL :`)뿐. 브랜드 밴드·작성일·고지 문구 없음. PDF 표지(`_cover_page`)는 이미 브랜드·
  고객명·작성일자·고지 문구를 갖춰 **두 산출물의 격이 어긋나 있었다**.
- **시트3 차액 블록**: `H13` 헤더 + `H14:K15` 병합에 **월납 차액 1개만**. 패킷이 말한
  "기존 총납입(실제 납만기 기준)" 행은 **엑셀이 아니라 PDF ④ 카드**(`총납입보험료
  before→after + 증감`)에 있다 — 엑셀에는 없어 신설이 아니라 **추가**가 맞다.
- **PDF 폰트**: body 10pt · 표 셀 9pt · 카드 라벨 8.5pt · notes 8pt(고령 가독 부적합).
- **PDF 섹션**: `.report-section`에 page-break 없음 → 섹션이 페이지 중간에서 시작.
- **⑤ 회사별 표 폭 실측**: 담보+후보장금액+회사 N열. 15계약이면 **17열 ≈ 356mm 필요** —
  A4 세로 가용 174mm는 물론 **가로 267mm로도 넘친다**. → ★가로 페이지로는 해결 불가,
  **열 분할이 유일한 해법**(회사 5개 = 7열 ≈160mm로 안착, 6개는 179mm로 넘침).

## 구현
### P1 엑셀 표지 리디자인 (`_sheet_cover`)
- 브랜드 밴드(에메랄드 면 + 흰 글자 `BohumFit 보험핏`) · 고객명 대제목 2줄 ·
  작성일(`generated_at` 또는 `report_cover.written_date`) · **담당 설계사 블록**
  (소속(GA)/설계사명/연락처/E-MAIL 라벨 + 기입란) · 하단 고지 문구(PDF 표지와 동일 취지).
- 제공된 값은 채우고 미제공은 **테두리만 있는 빈 기입란**으로 남긴다(설계사 수기 보완).
- A4 세로 1장 정합: `orientation=portrait` · `fitToWidth=1` · `fitToHeight=1` ·
  `print_area=A1:P23` · 눈금선 숨김.
- `build_workbook_bytes(analysis, generated_at=None)` — 작성일 주입(미지정 시 호출 시각).
  ★기존 호출부는 인자 없이 그대로 동작(하위호환).

### P2 20년 총납입 차액 (시트3)
- `MONTHS_20Y = 240` 상수 신설. `H15` 라벨 **"20년 납부 시 총납입 차액"** + `H16` 값
  = 월납 차액 × 240. 기존 `H14` 월납 차액 행은 **유지(병기)** — 성격이 다른 지표라 대체 금지.
- 색: 절감 = 에메랄드 / 증가 = 앰버(250 규칙). 특이사항 블록은 `H17` → `H18`로 이동.
- ※"총납입(실제 납만기 기준)"은 PDF ④ 카드에 이미 존재(라벨 그대로 유지) — 엑셀에는
  월납 차액 + 20년 차액 두 지표를 둔다.

### P3 PDF 고령 가독성
- **폰트 약 1.35배**: body 10→**13.5pt** · 표 셀 9→**12pt**(패딩 4/6→7/8px) ·
  h1 14→20 · h2 11.5→16 · h3 9.5→13 · 카드 라벨 8.5→11.5 / 값 13→17 ·
  notes 8→11 · 배지/칩 8→10.5 · 표지 필드 9/11→12/14pt.
- **섹션 페이지 분리**: `.report-section { page-break-before: always; }` +
  첫 섹션(②)에 `first-section` 예외(표지 다음 브랜드 헤더에 이어지도록 — 빈 페이지 방지).
- **표 헤더 반복·행 보호**: `thead { display: table-header-group; }` ·
  `tr { page-break-inside: avoid; }` · `table { page-break-inside: auto; }`.
- **⑤ 회사 열 분할**: `COMPANY_CHUNK = 5`로 끊어 표를 반복하고 담보·후 보장금액 열을 매 표에
  반복. 2개 이상이면 `회사 1~5 (1/3)` 캡션으로 묶음을 명시. **가로 페이지는 실측상 해결이
  안 돼 미채택**(기록).

## 검증 체크리스트 (1차 — 완료 · 2026-07-31)
- [x] backend pytest **792 passed, 8 skipped**(784 + 신규 8) · tsc app/node · lint ·
      npm test **99 passed**(프런트 무접촉)
- [x] ★**엑셀 값 불변**: HEAD(`3384e6e`)의 coverage 8모듈을 별도 프로세스로 실행해 대조 —
      **시트2 숫자 셀 removed/changed/added 0/0/0**(표준·overview 양쪽),
      시트3도 **차액 블록(14~17행) 외 좌표 diff 0**(그 블록만 계획된 변경)
- [x] 20년 차액 산식: 월납 차액 × **240** — 해지 1건 합성(월 10,000 해지)에서
      `-10,000 → -2,400,000` 일치, 실 문서 2건도 `delta × 240` 일치 확인
- [x] ★**실렌더(헤드리스 Chromium)**: 표준 **14p** · overview **17p**,
      **우측 여백 침범 단어 0**(폰트 확대 후에도 넘침 없음)
- [x] 섹션 페이지 분리 실렌더 확인: `④`·`⑤`가 각각 새 페이지 시작, ⑤는 회사 5개씩
      **3개 표**(1~5 / 6~10 / 11~15)로 나뉘고 각 페이지에 표 헤더 반복
- [x] 표지 회귀: 브랜드 밴드(에메랄드+흰 글자)·고객명·작성일·설계사 4항목(제공값 채움/
      미제공 기입란)·고지 문구·A4 1장 인쇄 설정
- [x] PII 0(합성 픽스처·실데이터 산출물 미보존) · diff 범위 =
      `export_excel.py`·`export_pdf.py` + 테스트 1(신규) + harness.
      **`excel_style.py`·`pipeline/`·집계·parser·`src/` diff 0**

## Stage 목록 (Codex용 · 실측 갱신)
backend/coverage/export_excel.py·export_pdf.py,
backend/tests/test_report_readability_261.py(신규),
tasks/BOHUMFIT-261-*.md, handoff.md, locks.md — 실 PDF·엑셀 제외
※`excel_style.py`는 변경 0(기존 팔레트 재사용) — 패킷 Stage 목록에서 제외

## 커밋 메시지 (Codex용)
feat(BOHUMFIT-261): 엑셀 표지 리디자인 + 20년 총납입 차액 + 리포트 고령 가독성 개선

## Next (handoff 명시)
① Codex — 2차 검증(값 불변·20년 산식·PDF 섹션 분리·폰트·실렌더·배포 스모크) → 커밋·push
② Human — 엑셀 표지·20년 차액·PDF 가독성 실물 검수(특히 ⑤ 회사 5개 분할 표가 상담에서
  읽기 좋은지 — 묶음 수를 조정하려면 `COMPANY_CHUNK` 한 곳만 바꾸면 된다)
③ Chat — 잔여([후] 신규·205 배지·간편 시트·사양 4건)
