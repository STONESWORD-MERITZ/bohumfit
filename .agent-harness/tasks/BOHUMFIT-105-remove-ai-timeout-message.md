# BOHUMFIT-105-remove-ai-timeout-message
## 목표
분석 결과 화면에 표시되는 아래 문구 제거:
"⚠️ AI 보조 판단이 20초 안에 끝나지 않아 결정론 결과를 먼저 표시합니다.
필요 시 진료 원자료로 추가검사·치료종결 여부를 확인해 주세요."
## 작업 범위
- src/pages/Disclosure.tsx (분석 결과 화면 — warnings 렌더)
- 해당 문구 또는 조건부 렌더링 로직 제거
## 수정 지침
- 문구와 해당 조건부 렌더링 블록 전체 제거
- UI 레이아웃에 빈 공간이 생기지 않도록 확인
- 관련 상태변수가 이 문구에만 쓰이면 함께 제거, 다른 로직에도 쓰이면 렌더링만 제거
## 완료 조건
- [ ] 해당 문구 UI에서 제거
- [ ] tsc 통과 / 빌드 통과
## 비고(현황)
- 문구 원천: backend `analyzer.py`(L1055~) `retry_warnings` → 응답 warnings → 프런트가 일괄 렌더.
  같은 'AI 보조 판단' 계열 안내 3종(끝나지 않아/시간 촉박/비활성화) 모두 비-액션 성능 안내.
- 백엔드 무변경(태스크 커밋 범위=Disclosure.tsx) → 프런트에서 'AI 보조 판단' 포함 warning을 렌더에서 필터.
