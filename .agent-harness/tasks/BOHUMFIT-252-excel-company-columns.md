# BOHUMFIT-252 — 엑셀 비교분석표 회사별 열 전개(전/후) + 최종비교분석표 디자인 완성

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: Codex (★1차 게이트부터 전부 실행 — Code 셸 불능 · 재개 절 참조)
Risk tier: 중위험 — 풀 하네스. git 쓰기 금지(커밋 Codex). 실 PDF·엑셀 원본 로컬 참조만·stage 금지.
★값·집계 로직 불변 — 엑셀 렌더 계층만.
Date: 2026-07-27

## 배경 (2026-07-27 Human — 로컬 레이아웃 정본 엑셀 대조)
현행 엑셀 시트2가 [전]/[후]를 합계 1열로만 출력. 목표: 시트2(비교분석표)는 ★회사별 개별
열 + 합계 열(전·후 양쪽), 시트3(최종비교분석표)은 합산 기준 유지 + 디자인 완성.

## 정본 레퍼런스 (읽기 전용 실측)
- 레이아웃 정본: `보장분석\비교분석표\`의 Human 제공 목표 양식(실제 로컬 파일명은 PII라
  하네스에 미기재) — 회사별
  2단 헤더[회사명/상품·구분]·[전] 6열+합계·[후] 3열+합계·담보별 회사 셀·시트3 종합비교/
  차액/특이사항 디자인 전수 실측). ★목표 양식일 뿐 파싱 입력 아님 — 값 검증에 쓰지 말 것.
- 파싱·값 검증 정본: 계약별 매트릭스가 있는 실 PDF(A 등 표준 문서). 레이아웃 정본 대응
  실 PDF가 `보장분석\`에 없으면 A로 by_company 전개 검증(handoff 기록).

## ★불변 계약
- 담보 값·집계(246 by_company·carry·overview 가드)·40행 순서·[후] 이월 = 불변.
  엑셀 렌더가 246 by_company를 열로 펼치는 것뿐. 회사별 열 합 = 기존 합계 열(차이 0).
- 파서·집계·엔드포인트·화면 무접촉. export_excel·excel_style 렌더 계층만.

## S0 — 선행 실측
1. 246 payload by_company 구조: 표준 문서 A에서 담보×회사 셀 채워지는지 /
   overview E는 by_company 비고 summary만인지.
2. [후] by_company: 이월분(유지 계약)은 회사별로 있는지 / 신규 계약분은 트랙 미착수라
   비어있는지 실측.
3. 레이아웃 정본 엑셀 전수 실측(2단 헤더·열 구성·병합·시트3 디자인) → 재현 명세표.
4. 250 export_excel이 합계만 렌더하는 지점 특정.
★S0에서 "표준 문서도 by_company 빔"으로 나오면 = 246 집계가 계약별 셀 미충전 →
 렌더 문제 아닌 집계 확장 필요. 이 경우 구현 중단·판정 보고(별도 파이프라인 태스크 필요).

## 구현 (by_company 존재 전제)
A. 시트2 [전] 회사별 열: by_company 각 계약(회사)을 개별 열 + 합계 열. 2단 헤더
   (회사명 / 상품명·구분) 레이아웃 정본 실측대로. 담보별 회사 셀 금액·빈 셀 공백. 계약 수만큼
   열 확장(누락 0·인쇄영역/fitToWidth 열 수 맞춰 조정).
B. 시트2 [후] 회사별 열: ★두 갈래 —
   · 이월분(유지 계약) = 회사별 열 전개(전과 동일 회사, 해지 제외)
   · 신규 계약분 = 가입제안서 트랙 미착수 → 열 구조(헤더 자리)만·값 공란 + "신규 설계 반영
     대상" 표기(250 신담보 문구 준용). 데이터 생기면 채우도록 렌더 골격만.
   · overview 문서 E = by_company 없음 → 회사 열 미생성·합계 열만 + 특이사항 경고(246).
   문서 유형별(표준/overview) 분기 명시.
C. 시트3 최종비교분석표: 합산 기준 유지(회사별 아님). 레이아웃 정본 시트3 디자인 정합 —
   종합비교(전/비교(→)/후)·보험료 차액 박스·특이사항 박스·좌우 전후 표를 250 FIT 팔레트로
   완성(붉은 강조는 250 규칙 에메랄드/앰버).
D. 250 스타일(에메랄드 헤더·강조 행·그리드·FILL_ONLY 계약) 회사별 열까지 일관 적용.

## 진행 기록 (Claude Code · 2026-07-27)

### S0 실측 결과 — ★전제 일부 반전(집계·렌더는 이미 회사별)
1. **by_company 충전 정상**: A(표준) [전]·[후] 모두 계약 9개·enrolled 40행 전부
   by_company 채움, 회사별 합=summary 대사 상이 **0**. E(overview)는 계약 15개·일부
   행(16/42)만 by_company(세부 매트릭스 있는 행) — 나머지는 summary만(246 설계대로).
   → ★중단 조건("표준 문서도 by_company 빔") 비해당 — 집계 확장 불필요, 렌더 진행.
2. **[후] 이월 by_company 존재**: build_after_analysis(해지 0)에서 [후] 40행 전부 회사별
   유지·대사 0. 클라이언트 경로(buildAfterResult)도 by_company·contract_list 이월 +
   제안 계약을 "신규제안" 회사로 contract_list에 추가함(코드 실측).
3. **현행 렌더도 회사 열 이미 전개**(248 구현·250 유지): A 실물 [전] 9열+합계·[후]
   합계+9열 렌더 확인 — 패킷 전제 "합계 1열만"은 현행 HEAD와 불일치(handoff 기록).
   실제 격차 = ①2단 헤더(상품명 행 부재 — 현행 5행이 회사별 월납 숫자) ②[후] 신규 계약
   골격 열 부재 ③시트3 헤더·종합비교 디자인 미완.
4. **레이아웃 정본 실측표**: 시트2 = 3행 대제목 병합([전 보장]/담보내용/[후 보장]) ·
   4행 회사명 · 5행 상품명 · 6~8행 구분(손해/생명 종목)·가입일·납만기 · 합계 열은 4:5 병합
   "합 계"·메타 "-" · [전] 회사열→합계→담보명→[후] 합계→회사열 순 · [후]에 가입일
   "신계약" 열 존재(제안 계약). 시트3 = B2:L2 제목 · 좌측 표(B=전 값·C:E=담보명·F=후 값,
   헤더 4행 색면) · 우측 종합(전/비교(→)/후) · 보험료 차액 박스(H12~14) · 특이사항 박스
   (H20~37). ※레이아웃 정본 담보 행 구성(9~43행)은 구 개인 양식 — 행 스키마는 246 비분양식
   40행이 정본(불변), 채택 범위는 헤더 구조·디자인만.

### 구현 (렌더 계층만 — 값·집계 로직 무접촉)
- **A 2단 헤더**: 시트2 4행=회사명(기존 240 라벨 규칙 유지), **5행=상품명 신설**
  (`contract_list[].product`, 에메랄드 면+흰 글자·랩·행높이 30). 회사별 월납은 중복 기재를
  제거하고 양식대로 "보험료 합계"(50행) 한 곳만(★값 자체 불변 — 아래 검증 ②).
  ※레이아웃 정본 구분(손해/생명 종목) 행은 파서 원천 데이터 부재로 미기재 — 기존 메타 4행
   (구 분=컨설팅 상태·가입일·납만기·계피관계) 유지. 회사명 열 병합(정본 M4:N4형)은
  계약 정렬(월납 내림차순) 유지 우선으로 미채택 — 열별 독립 라벨(240 규칙).
- **B [후] 신규 골격 열**: after 존재·표준 문서·제안 계약 부재 시 [후] 말미에 1열 —
  4행 "신규 설계 반영 대상"·5행 "(가입제안서 반영 전)"·메타 "-"·담보/보험료 값 전부 공란.
  제안 계약(consulting_status/remark="신규제안")이 페이로드에 있으면 일반 회사 열로
  전개되므로 골격 미생성. overview는 회사 열 자체 미생성(246 유지 — 골격도 없음).
- **C 시트3 디자인**: 헤더(리모델링 전/주요보장/리모델링 후/종합/전/비교/후) = 에메랄드
  면+흰 글자(250 헤더 규칙으로 승격), 종합비교 단계 라벨 = 에메랄드 소프트. 차액 박스·
  특이사항 박스·[후] 강조는 250 기존 유지.
- **D**: 강조 행 fill·그리드·FILL_ONLY 계약은 골격 열까지 동일 적용(값 셀 fill 승계).
- YN 행 회사별 Y 표기(정본 r39~42)는 원천→플래그 매핑이 집계 계층(compute_yn_flags)
  이라 ★렌더만 계약 위반 — 미채택·합계 열만 유지(Chat 후속 결정지).

## 검증 체크리스트 (1차 — 완료 · 2026-07-27)
- [x] backend pytest — **732 passed, 8 skipped**(726 + 신규 6: 2단 헤더·회사셀=payload·
      회사합=합계 대사·신규 골격·제안 존재 시 미생성·overview 미생성·시트3 헤더)
- [x] ★회사별 열 합 = 합계 열: 합성(만원 정수 배수 — 반올림 편차 0 설계) [전]·[후] 대사 0
      + 실 PDF A·B 회사 셀=payload by_company 대조 상이 0
- [x] ★값 불변: HEAD(1e3cf47) exporter를 별도 프로세스로 실행해 실 PDF 3케이스(A/B/E)
      전 시트 숫자 셀 좌표 대조 — removed 0·changed 0·added 0. 유일한 계획 변경 = 시트2
      5행(구 헤더 회사별 월납 → 상품명): 해당 값 전부 50행 보험료 합계에 동일 유지 확인
      (별도 단언 True). 임시 HEAD 모듈·실데이터 산출물 즉시 삭제(파일 저장 0)
- [x] overview E: 회사 열·골격 열 미생성, [전]/[후] 합계·특이사항 경고 유지
      (249 좌표 고정 테스트 + 실 PDF 대조)
- [x] 레이아웃 정본 정합: 2단 헤더(회사명/상품명)·합계 4:5 병합·메타 "-"·[전]→담보명→
      [후] 열 순·신규 열 자리 — 위 실측표 기준 재현(종목 행·회사명 병합 편차는 근거 기록)
- [x] 면 전용 색 폰트 0(250 계약 테스트 통과)·tsc app/node·lint·npm test **95** 불변
- [x] PII 0(합성 픽스처 홍길동·합성손보), diff 범위 = export_excel·테스트 2(신규 252 +
      184 검증 의도 갱신) + harness만. excel_style은 변경 불필요(기존 팔레트 재사용)

## Stage 목록 (Codex용 · 실측 갱신)
backend/coverage/export_excel.py,
backend/tests/test_excel_company_252.py·test_coverage_contract_list_184.py(검증 의도 갱신),
tasks/BOHUMFIT-252-*.md, handoff.md, locks.md — 실 PDF·엑셀 원본 제외
※excel_style.py는 변경 0(기존 팔레트 재사용) — 패킷 Stage 목록에서 제외

---

## 재개 (2026-07-28 · 253 귀속 정합 반영 후 — Claude Code)

### S0 재확인 — 253 반영 상태 (Codex 253 권위 실측 인용 · 사유 아래)
- 반려 원인이던 회사합≠합계는 253(commit `8aa2033`)으로 해소: Codex 권위 검증에서
  **A p17 22,680원→계약9 유일**(항암약물방사선 200만 귀속)·**D p15 167,700원→계약6 유일**
  (재해사망 승격·상해 −1억 = 이중 계상 해소)·표준 회사합 대사 0·**5케이스 '?' 잔존 0** 확정.
- ★이 세션은 셸 실행 환경 불능(모든 명령 exit 2 — 253 회송 때부터 지속·서브에이전트 실증
  포함)으로 자체 재실측 불가 — 위 수치는 Codex 253 검증 기록(handoff·locks) 인용이다.

### '?' 버킷 렌더 정책 (실측 결과에 따른 결정·기록)
- 실 5케이스 '?' 잔존 0 → 평시 미확인 열 **미출력**(현행 유지).
- 단, 253이 동일 보험료 모호 문서에서 오귀속 대신 '?'를 유지하도록 설계됐으므로, '?'가
  실제로 남는 문서에서는 회사 열 합 ≠ 합계 오독이 재발한다 → **잔존 시에만 "계약 미확인"
  열을 조건부 출력**하는 렌더 가드를 추가(오독 방지 — 패킷 3의 두 경우를 모두 충족).
  - `_unknown_bucket_present`: FORM_ITEMS enrolled 행의 by_company에 실계약 외 키가
    있으면 감지([전]·[후] 각각). `_unknown_sum`: 실계약 외 키 값 합(공란=None).
  - 열 위치: [전] 회사 열 뒤·합계 앞 / [후] 회사 열 뒤·신규 골격 앞. 헤더 "계약 미확인"/
    "(원문 계약 특정 불가)", 메타 "-", 보험료 행 공란.
  - 회귀: '?' 존재 합성 → 열 출력+회사열+미확인=합계+[후] 이월 / '?' 부재 → 열 미출력.

### ★검증 실행 불가 — 사유 (AGENTS.md 계약 · 253 회송과 동일 상태 지속)
- 셸 완전 불능 지속(`echo` 포함 전 명령 출력 0·exit 2·서브에이전트 동일) — **pytest
  (신규 2종 포함 전체)·실 PDF 5케이스 재실측·8aa2033 HEAD 대비 값 diff·프런트 게이트
  전부 미실행.**
- 수행한 정적 검증(실행 대체 아님): ① 재개 diff 전량 재독 — b_unk/a_unk가 0이면 열
  배치·좌표가 기존 252 구현과 완전 동일(기존 테스트 6종 좌표 불변) ② '?' 합성 케이스
  좌표 수기 추적(n=1·b_unk=1 → 미확인 3열·합계 4열·[후] 미확인 10열·골격 11열 = 테스트
  단언과 일치) ③ 병합·너비·인쇄영역 루프가 시프트된 col을 자동 포섭함 확인.
- **Codex는 1차 게이트부터 전부 실행**: ① pytest 전체(기준 741/8 + 252 테스트 8종:
  기존 6 + 신규 2) ② 실 PDF 5케이스 회사합=합계([전]·[후])·'?' 잔존 0 → 미확인 열
  미출력 확인 ③ ★값 불변 — HEAD(8aa2033) exporter 대비 재판독 diff(scratchpad
  verify252.py 패턴 — 시트2 5행 상품명 교체 외 diff 0·5행 월납은 50행 유지) ④ overview
  회사 열 미생성 ⑤ 레이아웃 정본 2단 헤더 정합 ⑥ Excel 실렌더 ⑦ 프런트 게이트(무접촉 확인).

## Codex 권위 검증 결과 (2026-07-28 · commit 전)
- PowerShell·루트·remote·리트머스·HEAD `8aa2033` PASS. 표적 **8 passed**, 전체 backend
  **743 passed, 8 skipped**, frontend **95 passed**, tsc app/node·lint 클린. 직전 741에는
  미커밋 252 기존 6종이 포함돼 있었으므로 재개 순증은 2종이며 743이 정확한 실측값.
- 실 PDF A/B/C/D의 FORM 35행 [전]/[후] 전수 대조: payload 회사 셀·회사합=합계 상이 0.
  A 항암약물방사선 계약9 200만, D 재해사망 계약6 1억·상해사망 28,308만원 유지.
  E는 overview 합계-only 분기로 회사 열 미생성·합계+경고 유지.
- detached HEAD exporter 대비 5건 전 시트 숫자 좌표: 계획된 5행 월납 중복 제거(A18/B28/
  C6/D18/E0) 외 removed/changed/added **0/0/0**, 제거값은 같은 열 50행에 전량 동일 유지.
- 실 5건 `?` 잔존 0→미확인 열 0. 합성 `?` 케이스는 조건부 미확인 열·[전]/[후] 대사와
  이월/골격 시프트 통과, `?` 부재 미출력 통과.
- Excel 16.0으로 A·E를 실제 개방해 인쇄영역·landscape·FitToPagesWide=1·페이지 분할 0을
  확인하고, 번들 렌더러로 두 파일 3시트씩 전부 시각 확인해 겹침·잘림 0. 2단 헤더·신규
  골격·시트3 에메랄드/차액/특이사항·면 전용 색 폰트 0 확인.
- PII·실 PDF·레이아웃 정본·실데이터 xlsx·임시 렌더 stage 0. diff는 export_excel·테스트
  2개+harness뿐이고 excel_style·parser/aggregator/pipeline/src/supabase/package diff 0.

## 커밋 메시지 (Codex용)
feat(BOHUMFIT-252): 엑셀 비교분석표 회사별 열 전개(전·후)+2단 헤더+미확인 열 가드+시트3 디자인

## Next (handoff 명시)
① Codex — ★1차 게이트부터 전부 실행(위 목록) → 통과 시 Stage 목록만 stage → 커밋·push
② Human — 표준 문서 A 재다운로드로 [전]·[후] 회사별 열·2단 헤더 확인(★overview E 아님 —
  overview라 회사 열 미생성이 정상) + 셸 불능 지속 시 로컬 환경 점검(세션 재시작)
③ Chat — 잔여: Y/N 회사별 표기(집계 확장)·[후] 신규 채움(가입제안서 트랙)·205 배지
