# BOHUMFIT-296b — 296 반려 보정: Q6 대조표 오류 규명

Owner flow: Claude Chat → Claude Code → Codex → Human | Current owner: **Codex**(Human Q6 재승인 반영·296과 합본 커밋)
Risk tier: 고 — coverage 값 계층 검산. 296 구현 보존, Human 재승인 전 smoke 기준값 갱신·git 쓰기 금지.
Date: 2026-08-18 · 기준 HEAD `05c038f`(298 커밋 완료, 296 미커밋분 위)

## ★상황
296 구현이 워킹트리에 미커밋으로 있고, Codex 독립 대조에서 **Q6 대조표 오류**로 반려됐다.
296 구현을 되돌리지 않고 **대조표를 바로잡고 원인을 규명**한다.

## 반려 사유
기존 대조표는 정본C·D를 **"총액 불변 — 단건"**으로 보고했으나, Codex 독립 대조 실측은 다음과 같다.

- 정본B: **−1,000,000**(승인값 일치)
- 정본C: **−11,700,000**(기존 대조표 누락)
- 정본D: **−4,000,000**(기존 대조표 누락)

## Step 1 — 대조표 재작성
1. Codex 반려 기록의 재현 절차대로 정본 A~D의 배선 전(HEAD)·후 집계를 실행한다.
2. 총액뿐 아니라 행 단위 변화와 대분류 합계 변화를 전수 대조한다.
3. C·D를 불변으로 판정한 원인을 문서/스냅샷/단건 판정 관점에서 특정한다.
4. C −11,700,000·D −4,000,000이 N대수술비 sum→max인지, 이관 값 손실인지, 2열 라벨 제거 영향인지 구분한다.

## Step 2 — 판정
- **(a) 전부 sum→max 정정**: 제품 코드 정상. 대조표만 정정하고 Q6 재승인을 요청한다.
- **(b) 이관 중 값 손실 포함**: 값 보존을 최소 수정하고 대조표를 다시 작성한다.
- **(c) 원인 불명**: 제품 코드를 수정하지 않고 중단·보고한다.

## Step 3 — 검산 강화
- 이관 전후 대분류 합계 검산을 정본 A~D에 고정한다. 이관은 총액을 바꾸지 않고 N대수술비 정정만 총액을 바꿀 수 있어야 한다.
- 각 문서의 N대수술비 복수 건을 명시적으로 열거하고 테스트에 고정한다.
- Q6 대조표 자동 생성 가능성을 검토·기록한다.

## 수정 금지
- 296 구현은 Step 2(b)일 때만 최소 수정한다.
- 295/295b/298·케스케이드·`compute_stage_totals`·`backend/pipeline/`·`filters.py`·종수술비 비고를 수정하지 않는다.
- smoke 기준값을 갱신하지 않는다.
- `vite.config.*`를 수정하지 않는다.

## 검증 체크리스트
- [x] 정본 A~D 행 단위 대조표가 B −1,000,000 / C −11,700,000 / D −4,000,000과 일치
- [x] 각 변화의 행 단위 사유 특정·사유 없는 변화 0
- [x] 기존 대조표 오류 원인 규명
- [x] 이관 값 보존·대분류 합계 검산 4문서 고정
- [x] N대수술비 복수 건 열거·테스트 고정
- [x] backend pytest 1065/8 · frontend 413/42 · tsc app/node · lint
- [x] smoke 예상 FAIL이 정정 대조표와 일치(기준값 미갱신)
- [x] 보호 영역 diff 0

## 수행 결과 — Step 2(a), 제품 코드 추가 수정 0

HEAD(`05c038f`, 백엔드 기준은 296 전)와 296 워킹트리를 같은 정본 A~D·기준일 2026-07-29로 재파싱했다.

| 정본 | 원시 N행(금액, M) | N 전→후 | 총액 전→후 | 월납 | 행 단위 판정 |
|---|---|---|---|---|---|
| A | 1건: `2` | 2M→2M | 614.86M→614.86M | 681,312 불변 | 단건 sum=max |
| B | 8건: `0.3+0.2+0.1×5+1` | 2M→1M | 1,501.69M→1,500.69M | 4,675,189 불변 | N행만 −1M |
| C | 5건: `1+10+0.2+10+0.5` | 21.7M→10M | 410.66M→398.96M | 181,802 불변 | N행만 −11.7M |
| D | 9건: `1×4+0.2×5` | 5M→1M | 480.44M→476.44M | 488,294 불변 | N행만 −4M |

### 대분류 이동 검산

- A: 비고 −2M / 수 술 +2M → 총액 0.
- B: 비고 −21M / 수 술 +1M / 실 비 +9M / 운전자 +10M → 총액 −1M. 3대비급여 9M·6주미만 10M은 그대로 이동했다.
- C: 비고 −21.7M / 수 술 +10M → 총액 −11.7M.
- D: 비고 −5M / 수 술 +1M → 총액 −4M.
- 의미 행 전수 비교에서 N대수술비 외 값 변화 0. 2열 헤더 제거는 payload 변화 0.

### 기존 표 오류 원인

1. C의 `10M`·D의 `1M`은 296 적용 **후 max 값**이다. 기존 기록에는 C·D의 HEAD 전 sum 스냅샷이 없고, 후값을 전값으로도 취급해 불변으로 적었다.
2. parser의 `n_values`는 `if n not in values`로 중복 제거된 **N 종류**다. C `[119]`·D `[2,142]`를 담보 건수로 사용할 수 없는데 기존 표가 이를 단건 판정으로 오해했다. 상세 원시 행을 직접 세면 C 5건·D 9건이다.
3. 재발 방지로 기존 `test_relocation_preserves_value_and_group_total`에 로컬 정본 자동 검산을 연결했다. 파일명/PII를 코드에 넣지 않고 A~D를 구조로 식별하며, 원시 N행 수·sum·max·총액·월납·이관값을 함께 단언한다. 실 PDF가 없는 CI에서는 기존 익명 합성 계약을 계속 실행해 테스트 수 기준선은 유지한다.

### 자동화 판단

Q6 표의 핵심 값은 위 테스트로 자동 고정할 수 있다. 승인용 Markdown 표까지 생성하는 별도 스크립트는 현재 필요하지 않다. 테스트 실패가 전후 계약 중 어느 값이 바뀌었는지 직접 드러내며, 기준값 갱신은 Human 승인 후 Codex 단계로 계속 분리한다.

## Stage(Codex — 재승인 후)
296 + 296b를 합쳐 커밋한다. 실 PDF·PII·수기 엑셀·생성 산출물은 제외한다.

## Human Q6 재승인·Codex 2차 결과

- Human 재승인: A 불변 / B −1,000,000 / C −11,700,000 / D −4,000,000(전부 N대수술비 sum→max).
- Codex 독립 재실행: 원시 N행 수·합계·최대값과 총액 변화가 A~D 전부 일치했고, 이관 값 손실·2열 헤더 제거 값 영향은 0이었다.
- smoke 기준값을 표준 `coverageRows` 59, overview 총액 1,500,690,000으로 갱신한 뒤 PASS.

## 커밋 메시지(재승인 후)
`feat(BOHUMFIT-296): 비고 정규 행 이관·N대수술비 신설·2열 라벨 정리(52행·smoke 기준값 갱신 — Human Q6 재승인)`
