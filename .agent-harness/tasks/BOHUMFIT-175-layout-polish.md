# BOHUMFIT-175 — 레이아웃 폴리시 3건 (헤더 줄바꿈·히어로 공백·사용량 배지 위치)

Owner flow: Claude Chat -> Claude Cowork -> Codex
Current owner: Cowork

## 배경 (Human 실화면 확인 — 27인치·노트북 스크린샷 기준)
163(대시보드 진입점)·174(히어로 svh) 배포 후 발견된 레이아웃 이슈 3건. 기능 정상 — 배치만 수정.

## 문제 1 — 헤더 NAV 줄바꿈
163 "대시보드" 추가로 헤더 폭 증가 → NAV 항목이 27인치·노트북에서 2줄. ★h-14 높이 불변(174 calc 연동).
수정 방향: nowrap + 간격/패딩 축소 + 좁은 구간 이메일 축약(또는 md 이하 숨김) — 항목 제거 금지.

## 문제 2 — 히어로 좌우 공백 과다 (27인치)
콘텐츠 블록 중앙 배치(텍스트 좌정렬 유지). 1440 이하 회귀 금지 — 와이드에서만 개선. 174 svh·세로 중앙 무변경.

## 문제 3 — 무료 분석 배지 위치 (159)
카드 내부 → "알릴의무 필터" 제목 라인 우측(flex justify-between). 모바일 자연 줄바꿈·겹침 금지.
159 노출 규칙·문구 로직 무변경 — 위치만. customer 모드 동일 적용 확인.

## 수정 금지
- h-14 / 174 svh·중앙 / 173 clamp / 159 배지 로직 / NAV 항목 제거·라우트 / backend

## Step 0 — 진단 (실측)
1. NAV+UserArea 소요 폭 vs 1280/1440/2560 가용 폭 → 수정 조합 결정
2. 히어로 컨테이너 max-width·정렬 현황
3. 배지 현재 위치·타이틀 행 구조

## 검증
- 1280·1440·1920·2560 헤더 1줄(코드 계산) / 히어로 1440 이하 회귀 없음 / 배지 이동·겹침 0
- h-14·svh·clamp·배지 로직 무변경 grep / raw gray 0 / tsc·build·pytest = Codex 권위(485/8)

## 커밋 (Codex)
fix(BOHUMFIT-175): 레이아웃 폴리시 (헤더 줄바꿈·히어로 와이드 중앙·사용량 배지 위치)
