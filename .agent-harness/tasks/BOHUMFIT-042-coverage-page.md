# BOHUMFIT-042 /coverage 보장분석 페이지 — 업로드 → 전(前) 비분표

## Owner
Cowork(Claude) — 구현 1차. 검증·커밋·푸시는 Codex(Windows).

## 전제
- BOHUMFIT-041 매핑 엔진 머지 완료(handoff 2026-06-12 Codex 확인). lib 무수정 재사용 — **산식 재구현 금지**.

## 범위 (In)
- 라우트 `/coverage`, 네비 "보장분석". 로그인 보호는 기존 페이지와 동일 패턴(ProtectedRoute).
- `src/pages/CoverageAnalysis.tsx`(신규), `src/lib/coverageParse.ts`(신규, +테스트), `src/App.tsx`·`src/components/Layout.tsx` 소형 편집, `package.json` xlsx 의존성 추가.

### 1단계 업로드
- 원천자료 엑셀(.xlsx) 업로드 → SheetJS 로 **브라우저 내 파싱**.
- 서버 전송 금지 — 고객 계약정보(민감정보)는 화면 내에서만, 비저장 안내 문구 표기.
- 파싱 대상 양식: 헤더 2행(계약정보/담보정보), 열 = 회사명/상품명/구분/보험시기/보험종기/
  납입주기/납입기간/납입보험료/담보명/보장명/상태/가입금액(만원).
- 회사명·상품명은 병합셀 패턴(아래 행 빈 값) — forward-fill 로 계약 그룹핑.
- 041 의 parseSourceRows 시그니처에 연결. 파싱 실패 행은 버리지 말고 경고 목록으로 표시.

### 2단계 매핑 확인
- 보장명→카테고리 자동 매핑 결과 그리드.
- unmapped 항목은 드롭다운으로 36행 카테고리에 수동 배정(또는 '제외' 선택).
- 수동 배정 결과는 이 세션 내에서만 유지(저장 안 함).

### 3단계 전 비분표 렌더
- 표준비분표 레이아웃 그대로: 좌측 계약별 열(회사/상품 헤더, 가입일·납만기) + 합계 열 + 중앙 카테고리 36행.
- 사망분해·종수술 자동셋팅은 041 lib 결과 그대로 표시 — 재구현 금지.
- 종수술 1~4종 제안값(suggested)은 셀 단위 수정 가능(수정 시 합계 재계산도 lib 함수로).
- Y/N형 행은 체크 표시, 보험료 합계 행 포함.
- 계약 7~8개까지 가로 스크롤 대응(기존 결과표 min-w 패턴 참고).
- 하단에 "다음 단계: 해지/유지·신규 제안 설계(준비 중)" 자리만 표시(043 예고, 기능 없음).
- 면책: 분석 참고용·비저장 문구(실손 페이지 톤과 동일).

## 범위 밖 (Out)
- 컨설팅 후 설계 UI(043), 후 비분표 화면.
- 041 lib 수정(coverageMapping.ts 내 parseSourceRows 스텁 포함 — 호환 유지).

## ENV
- 신규 파일 우선, App/Layout/package.json 은 소형 편집. 마운트 git 금지, 검증 /tmp.

## 검증
- /tmp: tsc 가능 범위(파서·테스트·페이지) + 원천자료 양식 구조 기반 파싱 단위테스트.
- 픽스처는 익명 합성 데이터만(실명 등 실데이터 금지).
- Codex(Windows): npm install(xlsx) → tsc(app/node), lint, test, build + /coverage 브라우저 스모크.

## 산출 기록
- handoff 에 파싱 가정·경고 처리 방식 명시. Next: Codex(검증·커밋·푸시).
