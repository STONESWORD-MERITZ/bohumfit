# BOHUMFIT-256~258 — overview 회사별 귀속 100% 완결 (3파트 순차)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: Codex (파트별 순차 검증·3커밋)
Risk tier: 혼합(저→고) — 풀 하네스. git 쓰기 전면 금지. 실 PDF·엑셀 로컬 참조만·stage 금지.
Date: 2026-07-29 · 기준 HEAD `e9116fd`

## 공통 제1원칙
정확도·누락 0·오귀속 0(251/253). 유일 매칭만 귀속, 모호는 `'?'` 유지.
★표준 문서 회귀 0 — 매 파트 게이트에서 값·회사열 diff 0 증명.
overview summary 합계(1,542,990,000)·239 fallback·246 대사·전=후 이월 불변 — by_company 채움만.

## ★S0 실측 — 패킷 전제 1건 정정
- 255 문서의 "현행 귀속 성공 7종(38.3%)"은 **귀속 *가능성*(대사 일치) 판정**이었고, 실제
  `by_company`는 **overview 26종 전부 빈 `{}`**(귀속률 0%)였다(실측).
- 따라서 P1은 "상품명 폴백 추가"만으로는 목표(≈48%)에 도달할 수 없고 **detail→overview
  by_company 채움 경로 자체를 신설**해야 한다. P1에 이 기반 구현을 포함했다(패킷 목표 수치는
  이 기반이 있다는 전제이므로 취지 일치).

## ★설계 결정 — overview 전용 리졸버(표준 회귀 0을 구조적으로 보장)
- 담보 판정·한정담보 배제·미등록 매핑을 **공용 매칭(`constants.py`)이 아니라 overview 귀속
  경로 전용 리졸버**에 둔다. 이유: 공용 별칭/매칭을 고치면 표준 문서의 매트릭스 매칭·EXTRA
  검출까지 파급돼 P2 게이트(표준 diff 0)의 최대 리스크가 된다.
- 결과: `constants.py`·`aggregator.py` **diff 0**(변경 파일은 `parser.py` 단 하나) —
  표준 문서 무영향이 코드 구조상 보장된다. 담보 행 자체는 이미 40행에 존재해 신규 행 추가도
  불필요했다(패킷의 "신 40행 추가" 조항은 비해당 — 원문명→기존 담보 매핑만 필요).
- **귀속 게이트**: 담보별 detail 합이 overview summary와 **정확히 일치할 때만** 채운다.
  불일치·부재는 미충전 유지(오귀속 0). summary·enrolled·overview 플래그는 절대 불변.

## P1 = 256 (채움 경로 + 상품명 폴백) [저위험] — ✅ 완료
- `attribute_overview_by_company()` 신설: detail 페이지 → (계약, 담보) 금액 수집 → 귀속 게이트
  통과분만 `matrix[담보]["by_company"]`에 기입. `parse_document`는 **`matrix_from_overview`일
  때만** 호출(표준 문서 미진입).
- **상품명 폴백**(`_product_index`·`_detail_idx_with_product_fallback`): 보험료 경로(253) 실패 시
  헤더 상품명으로 계약 특정, **유일할 때만** 귀속(복수·0 매칭은 `'?'`). 근거: overview 정본 p6은
  헤더에 월보험료가 아예 없고 해당 계약이 보험료 미제공이라 보험료 경로가 구조적으로 불가.
- **실손 리졸버**: 원문이 `…입원의료비`·`…외래의료비`·`…처방조제료` 표기라 공용 매칭이 못 잡음.
  통원 = 외래+처방조제 합(계약 내부 구성 항목 합 — 실측 25만+5만=30만).
- 계약 내부는 **구성 항목 합**, 계약 간은 `agg`(sum/rep) 적용.
- **결과: 12/26종 · 646,000,000 = 46.1%**(패킷 목표 ≈48% 부합) · 회사합=합계 상이 0 · `'?'` 0.
- 게이트: pytest **759 passed, 8 skipped** / 표준 HEAD 대비 담보·분류·회사합·월납·[후]·경고
  **diff 0** / overview 합계·전체 총액 HEAD 동일 / 상품명 복수 매칭 시 `'?'` 유지 회귀 포함(9종).

## P2 = 257 (한정담보 배제 + 미등록 매핑) [★고위험 — 표준 파급] — ✅ 완료
- **(가) 한정·파생 담보 배제**(`_OVERVIEW_LIMITED_VARIANTS`): 상해사망(교통·화재·특정상해사망) /
  상해후유장해(특정·화재) / 유사암진단금(통합치료생활비·**유사암제외**) / 뇌혈관질환(산정특례) /
  상해수술(특정상해수술·외모·종수술) / 질병수술(특정질병수술·124대·종질병수술) /
  질병·상해입원(1인실·2-3인실).
- **(나) 미등록 담보 직접 판정**(`_overview_direct_target`): 표적항암치료·벌금(대인/대물)·
  가족/일상/자녀배상·보철치료비·골절진단비(진단만 — 수술·부목 제외).
- **실측 기반 정정 2건**: ①`유사암제외`(일반암 문구)가 유사암으로 흡수돼 대사 실패 → 배제어 추가
  ②`입원일당`을 배제어로 쓰면 **기본 입원특약 행까지** 떨궈 귀속 0 → 병실 특약(1인실·2-3인실)만
  배제로 정정.
- **결과: 25/26종 · 1,281,240,000 = 91.5%**(진단 예측치와 일치) · 대사 상이 0 · `'?'` 0.
- 게이트: pytest **765 passed, 8 skipped** / ★표준 diff 0(담보 값·분류·회사합·월납·[후]·경고) /
  ★`constants.py`·`aggregator.py` **diff 0**(표준 분류 무변경을 구조로 증명) /
  overview 합계·총액 HEAD 동일.

## P3 = 258 (암진단금 A안) [고위험] — ✅ 완료
- **S0 분해**: overview 암진단금 119,000,000 = 계약3 `암진단비(유사암제외)` 1,000만 +
  계약6 `암진단(기타피부암,갑상선암및대장점막내암제외)` 5,000만 + 계약8 `…암진단(유병자)` 900만 +
  계약13 `암(유사암제외)진단특약` 5,000만 — **정확히 일치**.
- **A안 구현**: 일반암 → `암진단금`, 유사암(제자리암·상피내암·갑상샘암·소액암) →
  `유사암진단금`(공용 매칭 유지), 재진단암·특정암(경계성종양·기타피부암·대장점막내암)·치료비
  계열 → 상위 담보 흡수 금지(`_CANCER_NON_GENERAL` + 배제 테이블 등록).
- ★핵심 규칙: **`(…제외)` 괄호는 보장 한정 문구이므로 판정 전에 제거**(`_cancer_core`) —
  `암진단(기타피부암,갑상선암및대장점막내암제외)`는 일반암 행이다.
- **실측 기반 정정 1건**: 배제어를 `생활비`로 두면 상품명 `보험료생활비환급특약`까지 걸려
  계약8 900만이 누락됐다 → `통합치료생활비`로 정밀화.
- **결과: 26/26종 · 1,400,240,000 = ★100%** · 대사 상이 0 · `'?'` 0.
- 게이트: pytest **771 passed, 8 skipped** / 표준 diff 0 / overview 합계·총액 HEAD 동일.

### A안 ③("세부는 기타 보존")의 처리 — 미구현·사유 기록
재진단암·특정암 등을 **별도 `기타` 행으로 신설**하면 overview 문서의 전체 enrolled 총액
(1,542,990,000)이 증가해 **총액 불변 계약과 정면 충돌**한다(overview는 담보별 합계가 정본이고
detail은 그 내역이므로 둘을 더하면 이중 계상). 따라서 이번 범위에서는 **"상위 담보에 흡수하지
않는다"는 배제로만 구현**했고, 별도 행 보존은 미채택으로 기록한다(표준 문서에서는 기존
`classify_extra` 경로가 이미 기타 보존을 담당).

## ★스코프 밖(다음 태스크 필요) — Human 검수 전 필독
- 이번 3파트는 **집계/귀속(payload) 완결**이다. **엑셀 시트2의 회사 열은 아직 출력되지 않는다** —
  `export_excel._sheet_compare_form`이 `overview`면 회사 열을 만들지 않도록 가드하고 있다
  (255 문서의 **255-B** 항목·규모 M). 즉 Human이 재다운로드로 확인하려면 255-B가 선행돼야 한다.
- `[후]` 이월은 249 설계대로 overview 행을 **합계 수준만** 이월해 `by_company`가 비어 있다
  (실측: [후] 26종 모두 `{}`, 합계 1,400,240,000 유지). `[후]` 회사별까지 원하면 249
  `carry_coverage_row` 확장이 필요하며 이는 246/249 계약과 맞물린 별도 고위험 작업이다.
- 위 두 건은 이번 패킷의 파트 게이트·최종 검증 항목에 없었고(귀속률·대사 중심), 임의로 렌더를
  켜면 `[전]`만 회사 열이 있는 비대칭 산출물이 되므로 의도적으로 남겼다.

## 최종 검증 (1차 — 2026-07-29)
- [x] backend pytest **771 passed, 8 skipped**(기준 750 + P1 9 + P2 6 + P3 6 = 771 · 기존 무손실)
- [x] tsc app/node · lint · npm test **95 passed**(프런트 무접촉)
- [x] ★귀속률 추이: **0%(실측 현행) → 46.1%(P1) → 91.5%(P2) → 100%(P3)** ·
      최종 회사합=합계 상이 **0** · `'?'` 잔존 **0**
- [x] ★표준 문서 HEAD 대비 diff 0 — 3파트 각각 + 최종(담보 값·group12·agg·enrolled·by_company·
      overview 플래그·월납·[후] 이월·파싱 경고 전수 대조)
- [x] overview summary 합계(1,400,240,000)·전체 enrolled 총액(1,542,990,000)·239 경고 불변
- [x] PII 0(픽스처 익명·문서 실명 마스킹) · diff 범위 = `backend/coverage/parser.py` +
      테스트 3(신규) + harness. **`pipeline/` diff 0 · `constants.py`·`aggregator.py` diff 0**

## Stage 목록 (Codex용 — 파트별 3커밋)
- 커밋1(P1): `backend/coverage/parser.py`(채움 경로·상품명 폴백·실손 리졸버) +
  `backend/tests/test_overview_attribution_256.py`
- 커밋2(P2): `backend/coverage/parser.py`(한정담보 배제·미등록 직접 판정) +
  `backend/tests/test_overview_variants_257.py`
- 커밋3(P3): `backend/coverage/parser.py`(암 A안) +
  `backend/tests/test_overview_cancer_258.py` + `tasks/BOHUMFIT-256-258-*.md`·`handoff.md`·`locks.md`
※ 3파트가 모두 `parser.py` 한 파일에 누적되므로 논리 단위 분리가 어렵다 — 패킷 허용 조항에 따라
  **통합 1커밋도 가능**(그 경우 사유를 커밋 메시지·handoff에 기록).

## 커밋 메시지 (Codex용)
1) feat(BOHUMFIT-256): overview 회사별 상품명 폴백 귀속 (보험료 미제공 계약)
2) feat(BOHUMFIT-257): overview 한정담보 흡수+매핑 13종 등록 (표준 무영향)
3) feat(BOHUMFIT-258): overview 암진단금 A안 매핑 — 라*실 100% 귀속 완결

## Next (handoff 명시)
① Codex — 파트별 순차 검증·3커밋(각 커밋 전 해당 파트 게이트 재현, 특히 P2 표준 diff 0)
② Chat — ★**255-B(엑셀 overview 회사 열 렌더, M)** 발번 — 이게 없으면 Human 눈에 보이지 않음.
   `[후]` 회사별 이월(249 확장)도 함께 결정
③ Human — 255-B 반영 후 overview 문서 재다운로드로 회사별 최종 검수(주력 고객층 커버리지)
