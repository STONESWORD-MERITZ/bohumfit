# BOHUMFIT-300 — 기존 테스트·제품 주석 PII 익명화 (기대값 무변경)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: **Codex**(2차 검증·커밋) · Risk tier: 저(식별자 치환만 · 값 계층 무변경)
Date: 2026-08-20 · 기준 HEAD `f942fa0`(302b) · 야간 무인 · git 쓰기 0 · 실 PDF 복사/stage 0

## ★배경 (295b 잔여)
295b가 신규 4파일의 PII를 익명화하면서, **기존 커밋분 5파일은 범위 밖**이라 별도 태스크로 기록했다.
277이 제품 코드의 PII 경계를 봉인했으니 테스트 자산에도 같은 기준을 적용한다.

## ★원칙 준수
- **값·검증 강도 무변경** — 식별자만 익명 라벨로 치환. 기대값·단언은 **한 글자도 바꾸지 않았다.**
- 익명 라벨은 **기존 체계(정본A~D)**를 따른다. 규칙을 **한 곳**(`backend/tests/real_docs.py`)에 정의했다.

## Step 1 — 전수 스캔 (코드 무변경)
레포 전체(`*.py`·`*.ts`·`*.tsx`·`*.mjs`·`*.js`, node_modules 제외) 실명 스캔 결과 **11파일**.
295b가 지목한 5파일 **외에 제품 코드 5파일 + 스크립트 1파일**이 더 있었다.

| 분류 | 파일 | 성격 | 처분 |
|---|---|---|---|
| 제품 코드 | `constants.py`(6) · `integrated_treatment.py`(1) · `parser.py`(2) · `proposal_parser.py`(2) · `proposal_registry.py`(2) | **전부 주석**(실측 근거 기록) | 치환 — 로직 diff 0 |
| 테스트(주석) | `test_proposal_parsing_accuracy_276b.py`(1) · `test_proposal_tier_surgery_286.py`(2) | 주석·수기 엑셀 파일명 | 치환 |
| 테스트(경로) | `test_export_v2_layout_291.py` · `test_schema_v2_s2_wiring_290.py` · `test_schema_v2_s4_matching_292.py` | **실파일명이 로딩 경로**(값 검증 아님) | 공용 헬퍼로 전환 |
| 스크립트 | `scripts/prototype_286_ohj.py`(5) | 프로토타입 주석 | 치환 |

★**값 검증에 쓰이는 실명 = 0건**(고객명 파싱 테스트 등은 이미 합성 더미 사용) → 손대지 않고 남길 항목 없음.
★주민번호 패턴 **0건**. 연락처 패턴은 `test_password_reset_216.py`의 `010-1234-5678`뿐이며 **명백한 더미**다(Step 4 참조).

## Step 2 — 익명화
치환 체계(한 곳 정의): 정본A · 정본B · 정본C · 정본D · 정본E. 수기 엑셀 실파일명은 `정본C 수기 비교분석표`로.
제품 코드 5파일은 **주석만** 바뀌었다(로직 diff 0 — `git diff`에서 주석 외 변경 없음 확인).

## Step 3 — 실 PDF 경로 의존 전환 (296b 선례 공용화)
신설 `backend/tests/real_docs.py` — 실파일명 대신 **패턴 + 문서 구조 지표**로 정본을 식별한다.
- 정본A = 표준형 `*-INPUT.pdf` · 정본B = overview형(그 외 `*INPUT.pdf`)
- 정본C = 계약 **5건**인 날짜형 · 정본D = 계약 **6건**인 날짜형
- `real_proposals("정본C")` = 해당 정본의 제안서(접두를 실측 문서명에서 유도 — 소스에 실명 미기재)
→ 290·291·292가 `real_doc(라벨)`을 쓰도록 전환. **같은 파일을 같은 순서로 고르므로 기대값 불변.**
★검증: 3파일 **57건 전부 통과**(skip 아님 — 실제 문서를 찾아 실행했다).

## Step 4 — 재발 방지 검사 — ★도입하지 않음 (사유 기록)
주민번호·전화번호 패턴 0건 단언 테스트를 검토했으나 **오탐 위험이 실재**한다:
`test_password_reset_216.py`가 `010-1234-5678`·`01012345678`을 **기능 테스트 더미**로 쓴다(휴대폰 인증 경로).
이 패턴을 금지하면 정당한 테스트가 깨지고, 예외 목록을 두면 검사 자체가 무력해진다.
→ **도입 보류**. 실명은 애초에 패턴화가 어려워 자동 검사 대상이 아니다(295b 판단 승계).
   대안: 커밋 전 `git diff` 실명 스캔(295b가 쓴 방식)을 verify 관례로 유지한다.

## 검증 (2026-08-20 · Windows 로컬)
- [x] ★소스(`*.py`·`*.ts`·`*.tsx`·`*.mjs`) 실명 **0건**(전수 재스캔)
- [x] ★기대값·단언 무변경 — 치환은 주석·파라미터 라벨·로딩 경로뿐
- [x] 익명 라벨이 기존 체계(정본A~D)와 일관 · 규칙 한 곳 정의(`real_docs.py`)
- [x] 290·291·292 실문서 테스트 57건 통과(실제 실행 · skip 아님)
- [x] 제품 코드 로직 diff 0(주석만)
- [x] 제품 5파일의 docstring을 제외한 실행 AST가 HEAD와 완전 동일
- [x] 변경 테스트 5파일의 assert AST 개수·내용 HEAD와 동일(추가/삭제/완화 0)
- [x] backend **1093 passed, 8 skipped** · frontend **413 passed / 42 files** · smoke PASS · tsc·lint

### Codex 2차 검증 보정 (같은 태스크 범위)
익명화된 `scripts/prototype_286_ohj.py`가 실명 접두 glob을 익명 문자열로 단순 치환해 제안서가 **0건**이 되는 회귀를 발견했다.
직접 glob을 제거하고 공용 `real_proposals("정본C")`를 사용하도록 최소 보정해 **3건**을 다시 로드했으며, 파싱 결과도 전부 담보 행을 포함함을 확인했다.

※문서(`.agent-harness/*.md`)의 과거 실명은 **커밋된 역사 기록**이라 이번 범위 밖이다(CLAUDE.md 관례:
  태스크 문서·handoff의 고객명은 허용, 테스트 소스의 PII 0은 별개 계약).

## 커밋 메시지
chore(BOHUMFIT-300): 기존 테스트 픽스처 PII 익명화(기대값 무변경)
