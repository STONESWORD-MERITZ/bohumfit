# BOHUMFIT-291 — S3: 산출물 49행 양식 (엑셀·PDF)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: **Codex**(2차 검증·커밋·★288 문서 편승) / **Human**(육안 확인 1건)
Risk tier: 중~고 — export 2종 전면 교체(값 계층은 동결·모양만). git 쓰기 금지.
Date: 2026-08-17 · 기준 HEAD `9191006`(290 `d58ad95` 반영) · 선행 286-D·289·290

## ★이 태스크의 성격
S2(290)까지 집계는 49행이었으나 export는 **최소 어댑터로 구 40행 양식**을 유지했다. S3에서 엑셀·PDF를
49행 수기표 양식으로 전환하고 어댑터를 제거했다. **값은 S2 그대로 — export는 어떤 값도 재계산·보정하지 않는다**
(§Step 4 값 대조로 증명).

---

## Step 1 — 목표물 실측 (코드 무변경)

### 1-1. 신판 수기표 서식 (openpyxl 덤프)
| 항목 | 실측 |
|---|---|
| 시트 | 표지 / **컨설팅 전** / **컨설팅 후** / 최종 |
| 머리행 | r2 회사명(B2:E6 제목 병합·F2:G3 "회사명 / 납기만기 보험기간") · r3 납기만기 · r4:5 상품명(병합) · r6 월납(`"₩" #,##0"원"`) |
| 담보행 | r7~r52(46행) · B 대분류(구간 병합) · C:E 담보명(병합) · **F:G 합계 2열** · **회사당 2열**(H:I, J:K, …) |
| 2열 병기 | 종수술 5행만 좌우 분할(질병|상해). 그 밖 행은 좌우 병합 1값 |
| 비고 | r53 `비고`(돋움체 24) |
| 서식 | 맑은 고딕 22~33pt bold · 대분류 `FFDBEEF3` · 합계 `FFEBF1DE` · 종수술 값 `FFFFFFCC` · 값 `#,##0_ "만""원"` · 테두리 thick/medium |
| 열 폭 | A 4.8 · B 17.8 · F 20.8 · 그 밖 기본 |

### 1-2. `최종` 시트 구조
r2 기존(F:G)/점검 후(H:I)/기대효과(J:K) 헤더 · r4 월납(`='컨설팅 전'!F6` / `'컨설팅 후'!F6` / 차) · r6 20년 총납입(×12×20) ·
r7~ 담보행(B/C:E + F/H/J) · ★**L 접두**: `L 뇌 졸 중`·`L 뇌 출 혈`·`L 급 성 심 근 경 색`(이 시트만) · 표기 흔들림
(`질 병 / 상 해 입 원`·`뇌 혈 관 수 술 비`·`깁 스 치 료 비`) — 컨설팅 전/후 문자열을 채택(289 확정).

### 1-3. 현행 export 어댑터 지점·FIT 서식
- 어댑터: `aggregator.legacy_form_view`(투영) · `export_excel._coverage_maps`·`_stage_map`·`_yn_map`·`FORM_ITEMS`/`YN_ROWS`/`STAGE_ROWS` ·
  `export_pdf._grp_key`(구 그룹 축)·`_item_key`(NEW_ITEM_ORDER)·stage 7키 튜플·Y/N 블록
- FIT: 에메랄드 `#084734` 헤더+흰 글자 · 그린 티 `#CDEDB3` 대분류 선두 · 라임 `#CEF17B` 특수 · `fitToWidth=1` ·
  PDF 12pt(1.35배)·섹션 페이지 분리·회사 5개씩 분할(`COMPANY_CHUNK`)

### 1-4. ★수기표 서식 vs FIT 브랜드 충돌 — **브랜드 우선, 구조는 수기표**
| 충돌 | 처리 |
|---|---|
| 폰트 맑은 고딕 22~33pt(인쇄 확대용) | FIT 셀 폰트(돋움 10·제목 13) 유지 — 인쇄는 fitToWidth=1 |
| 대분류 `DBEEF3`·합계 `EBF1DE`·종수술 `FFFFCC` | 에메랄드 헤더/에메랄드 소프트 라벨/그린 티 대분류 선두/라임 특수(순환계 치료비) |
| 빨간 글자 합계(`FFFF0000`) | 사용 안 함(FIT 팔레트에 빨강 부재 — 250 근거) |
| thick/medium 테두리 | `BORDER_GRID` 얇은 선(254 구획선 규칙은 구 양식 전용이라 폐기) |
| 우측 고객정보 패널(구 O열·250) | 수기표에 없음 → 폐기 |
| 메타 행(구분/가입일/계피관계·236·254) | 수기표에 없음 → **회사명 셀 메모**로 정보 보존 |
| Y/N 5행 블록 | Q5: **행 안**(라벨 메모 `가입특약 Y/N: Y` · 금액 없는 가입 계약은 셀 "Y") |
| 종합비교 7행(암·초기/중기/말기) | ①: **케스케이드 17행**(compute_stage_totals 1:1) |

---

## Step 2 — 엑셀 (`export_excel.py` 전면 교체 · 표지 유지)
- 시트: 표지(세로) 유지 · **`컨설팅 전`·`컨설팅 후`**(`_sheet_track`) · **`최종`**(`_sheet_final`)
- 컨설팅 전/후: r2~r6 머리행(회사명·납기만기·상품명·월납) · r7~r55 **49행** · B 대분류 병합 · C:E 담보명(수기표 문자열) ·
  F:G 합계 · 회사당 2열 · (계약 미확인 2열·신규 골격 2열은 252 규칙 유지) · 비고 블록 · 하단 단위 안내
- 2열 병기: 종수술 = **질병|상해**, 간병인 = **상해|질병**(Q7). ★종별을 잃은 값(238 표준환산 `unspecified`)은 열로
  못 갈라 **병합 셀에 합계**(추측 금지 · 라벨 메모로 안내)
- Q2: 80% 행 값 표시 + 라벨 메모 `합계 미포함(243) — 값은 표시하되 대분류 합계에 더하지 않음` + 하단 안내 문구
- Q5: yn_source 7행 라벨 메모 `가입특약 Y/N: Y|N`(compute_yn_flags 재파생 — 낡은 payload 무시 254 계약 유지)
- 최종: 기존/점검 후/기대효과(후−전) + r4 월납·r6 20년 총납입(261 P2 지표 유지·메모에 "20년 납부 시 총납입 차액") +
  **L 접두 10행**(뇌졸중·뇌출혈·급성심근경색 + 다빈치·다빈치특정암·표적·면역·세기조절·양성자·중입자 — Human 확정) +
  **종합 판정 블록 17행**(케스케이드 키 순서·전/후/증감) + 비고 블록(전/후 합집합) + 특이사항
- ★값 = S2 그대로(`_man` 반올림 표기만) · fitToWidth=1 · 브랜드 색 유지 · 어댑터 제거
- ★구 페이로드(row_id 없음 — 과거 저장분·구 클라이언트) 호환: `_row_by_id`가 이름을 `resolve()`로 V2 행에 **투영**해
  읽는다(계산 없음·표시 전용). 181 픽스처(레거시 dict)로 검증.

## Step 3 — PDF (`export_pdf.py`)
- 그룹 축 V2 11+비고(구 축 폐기) · 행 순서 = V2 스키마(`ROW_INDEX`) · 비고 헤더 `(49행 밖 담보 — 정보 보존)`
- ⑤ 회사별 보장 세부: 2열 병기는 **한 칸 `질병/상해`**(간병인 `상해/질병` · 종별 미확인은 합계 하나) ·
  Q2/Q5는 이름 뒤 작은 태그 `[합계 미포함]`·`[가입 Y]`(이름 문자열은 건드리지 않음)
- ④ 특약별 비교(= `최종` 대응 섹션): 케스케이드 하위 행 **L 접두** · 종합 판정 블록 **17행**(구 3단·Y/N 블록 삭제)
- 고령 가독성(12pt·섹션 분리·회사 5개씩) **무변경** · 어댑터 제거

## Step 4 — 대조

### ★신판 수기표 vs 새 산출물 — 구조 (값 대조 안 함)
| 항목 | 결과 |
|---|---|
| 시트명 | 표지·**컨설팅 전·컨설팅 후**·최종 — 신판 2·3번 시트명과 일치(테스트 고정) |
| 행명 | 49개 = V2 표시명 문자열 그대로. 신판 46행과 **문자열 일치**(290 추가 3행 제외 — 테스트 고정) |
| 열 구성 | B 대분류·C:E 담보명·F:G 합계·회사당 2열 — 신판과 동일 |
| 종합 블록 | 17행(케스케이드 1:1) — 신판 `최종`에는 종합 블록이 없어 구조 대조 대상 아님(①은 Human 확정) |
| L 접두 | 뇌·심장 3행(신판 실측 그대로) + 암 계열 7행(Human 확정 확장) — `최종`에만 |

### ★S2 집계 vs 새 산출물 — 값 (전 셀)
| 대상 | 셀 수 | 불일치 |
|---|---:|---:|
| 이인숙·라금실·오현지 [전] × 컨설팅 전/후 × (합계 + 회사별 2열) + 최종 종합 17행 | **4,211** | **0** |
| 우상균 6계약 추가 대조(동일 규칙) | **787** | **0** |
| 4문서 확장 합계 | **4,998** | **0** |
| 합성 픽스처(전/후 49행 × 3열) — 테스트 고정 | ≥294 | 0 |
| 월납(6행) | 3문서 × 2시트 | 0 |
→ ★**export가 값을 건드리지 않았다.** 대조 규칙: 시트 셀(만원 반올림) == S2 `summary`/`by_company`/`columns`(2열 병기는 열별).
★Codex 2차 검증에서 로컬 `20260729_우O균님_보장분석.pdf`를 확인해 추가 실행했다. 6계약·월납 488,294원,
S2 스냅샷과 현재 payload의 직렬화 SHA-256이 동일했고 추가 787셀도 불일치 0이었다. 실 PDF·산출물은 stage하지 않았다.

### PDF 실렌더 (4문서)
이인숙·라금실 각 18쪽, 오현지 11쪽, 우상균 15쪽 · **페이지 끝을 넘는 표 행 0** · 회사 5개씩 분할 ·
종합 블록·L 접두 텍스트 확인. 엑셀도 4문서×4시트 전부 렌더해 겹침·잘림 0, 수식 오류 0을 확인했다.

### 286 프로토타입과 나란히
프로토타입(42행·회사 3열·질병|상해 2열)과 같은 열 구조에 **+7행(암 6·심장 1)** — 서식 이탈 없음(같은 브랜드 팔레트).

---

## 검증 (1차 · 2026-08-17 · Windows 로컬)
- [x] ★4문서(정본 2·오현지·우상균) 엑셀·PDF 실렌더 — 49행·시트명·2열 병기·L 접두·종합 17행·비고행 확인
- [x] ★S2 집계 vs 산출물 값 **정본 4,211셀 + 우상균 787셀 = 4,998셀 완전 일치**(export 값 무변경 증명)
- [x] ★신판 수기표 행명 46개 문자열 일치 · 시트명 일치(테스트 고정)
- [x] 80% 행 "합계 미포함" 메모 + 값 표시 · 합계 정합(S2 `group_rollup_v2` 제외 — 표는 수기표대로 세로 합계 행 없음)
- [x] 종합 블록 17행 = 케스케이드 체인 1:1 · 단조성 유지(3문서)
- [x] fitToWidth=1 · 인쇄 1페이지 폭 · PDF 페이지 경계 잘림 0 · 회사 5개씩
- [x] 브랜드 서식 유지(에메랄드·그린 티·라임 3색 · 면 전용 색 폰트 미사용 테스트 통과) — Pretendard는 PDF CSS 무변경
- [x] 어댑터 제거됨 · 제품 코드 참조 0(`legacy_form_view`·`FORM_ITEMS`·`STAGE_ROWS`·`NEW_ITEM_ORDER`·`GROUP13` — 테스트 고정)
- [x] backend `pytest -q` **1003 passed, 8 skipped** — 기준선 994 + 신규 9(291 11 − 폐기 2), 회귀 0
- [x] ★`npm run smoke:coverage` **PASS — 290 기준값 완전 불변**(프로브의 엑셀 좌표만 새 양식으로 갱신 · BASELINE 무변경)
- [x] `npm test` 402/41 · tsc app/node · lint 무경고 · build:verify 예상 FAIL(248)
- [x] `aggregator.py` diff = **`legacy_form_view` 삭제 37줄뿐**(계산 코드 무접촉 — 어댑터 제거) · `v2_mapping`·`constants`·`compare`·`pipeline/`·`filters.py`·`proposal_parser.py`·`parser.py`·`src/`·`vite.config` **diff 0**
- [x] Codex 2차 검토에서 261의 월납/20년 차액 색상 단언이 양식 이식 중 빠진 것을 발견해 같은 export 범위에서 복원했다. 절감은 에메랄드·증가는 앰버이며, 값·산식은 무변경이고 표적 19건 및 전체 backend 기준선을 통과했다.

### 기존 테스트 갱신 — 16파일 (전부 양식 교체의 직접 귀결 · 단언 의미 보존)
| 성격 | 파일 | 내용 |
|---|---|---|
| 좌표 이식(`tests/excel_v2_layout.py` 헬퍼 신설) | 252·254·259·261·272·248·250 | 시트명·행/열 좌표만 새 양식으로. 회사합=합계·값 불변·색·overview 가드·미확인 열·골격 열·20년 지표 단언 유지 |
| 시트명·라벨 문자열 | 181·184·188·191·194·195·236·249 | `비교분석표`→`컨설팅 전`, 4행→2행 회사명 등 |
| 어댑터 이관 | 287·290·v2names | `legacy_form_view`를 aggregator에서 제거 → **테스트 전용**(`tests/v2names.py`)으로 이동 |
| 폐기 | 254 구획선 테스트 1건 · 272 `_stage_map` 폴백 1건 | 구 양식 전용 규칙(사유 docstring) |
★**완화 0** — 값 단언은 전부 유지·강화(값 대조 4,998셀 추가). 261 차액 색상 단언도 Codex가 복원했다.

## ★Human 확인 요청 1건
- **엑셀 육안**: 종수술·간병인 2열 병기 헤더가 없다(수기표도 없음 — 첫 종수술 행 라벨 메모로 안내). 열 헤더(`질병|상해`)를
  6행에 넣을지 결정(넣으면 회사당 2열 머리행이 3단이 된다).

## 확인 불가
- **Pretendard 폰트 실적용** — PDF CSS 무변경으로 계승했으나 렌더 환경 폰트 설치 여부는 확인하지 않음(기존과 동일 조건)

## Stage 목록 (Codex용)
`backend/coverage/export_excel.py`·`export_pdf.py`·`aggregator.py`(어댑터 삭제) · `scripts/smoke-coverage.mjs`(프로브 좌표) ·
테스트 18파일(신설 `test_export_v2_layout_291.py`·`tests/excel_v2_layout.py`) · `tasks/BOHUMFIT-291-schema-v2-s3-export.md` ·
`verify.md`(smoke 상태 갱신) · `handoff.md` · `locks.md` · 기준선 3문서(backend 994→**1003**)
★미추적 `BOHUMFIT-288-before-track-audit.md`는 **별도 커밋**으로 편승(패킷 지시)
※제외: 실 PDF·PII·수기 엑셀·프로토타입·생성 산출물

## 커밋 메시지 (Codex용)
feat(BOHUMFIT-291): 산출물 49행 양식 전환 — 컨설팅 전/후·2열 병기·L 접두·종합 17행(값 무변경)
