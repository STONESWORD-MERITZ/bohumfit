# BOHUMFIT-276b — 가입제안서 파싱 정확도

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: Claude Code (구현·1차 검증)
Risk tier: 중~고위험 — 제안서 파싱 계층. git 쓰기 금지(커밋 Codex).
Date: 2026-08-06 · 기준 HEAD `3201528`(273) · ★**276a 미커밋 워킹트리 위에서 작업**(되돌리지 않음)
선행: BOHUMFIT-274(원인 조사) · BOHUMFIT-276a(폴백 제거)

## 목적
276a가 "없는 값을 지어내는 것"을 멈췄다. 이제 **읽어야 할 값을 정확히 읽는다**.
★설계 원칙 승계: 값이 없으면 빈칸 / **읽은 값을 다른 값으로 바꾸지 않는다** / 확신 없으면 경고.

---

## Step 1 — 문서 구조 실측 (코드 무변경)

### ★★샘플 전제 정정 — 가입제안서는 **1건뿐**이다
패킷은 "오현지 + 우상균 2건"을 전제했으나 실측 결과:
| 파일 | 유형 | 판정 |
|---|---|---|
| `오현지_105,802원_가입제안서_….pdf` (26p) | **가입제안서** | ★유일한 샘플 |
| 우상균 가입제안서 | **레포에 없음** | 274도 "원본 PDF 현재 폴더에 없어 재실행 못 함"으로 동일 기록 |
| `보장분석/황종철 KB보장분석 제안서.pdf` (32p) | ★**가입제안서 아님** — KB 보장분석(기존계약) 문서 | 정본 2건과 같은 유형 |

→ **일반화 판정에 직접 영향**한다(아래 Step 2 설계 근거).

### 오현지 제안서 26페이지 역할 (마커 기반 실측)
| 페이지 | 역할 | 근거 마커 | 금액 표기 |
|---|---|---|---|
| p1 | 표지 | — | 0 |
| p2 | 계약 요약 | `계약사항 : 20년납` | 5 |
| **p3~p5** | ★**실제 가입담보표** | **`가입담보리스트`**(p3·p5) | 5·14·1 |
| **p6** | ★**보험료 비교(예시)** | **`대표계약`**(문서 전체에서 **p6에만**)·`보험료 비교` | 3 |
| p7~p15 | 담보별 **보장내용(약관 설명)** | `보장내용`+`약관` | 0~13 |
| p16 | 예시(해약환급금 등) | `예시` | 33 |
| p17~p26 | 약관·안내·해약환급금예시표 | `약관`·`예시` | 0~1 |

★현재 파서는 `_extract_text`가 **전 페이지를 한 덩어리로 합쳐** 넘기고,
`_extract_rule_entries`가 그 전체 줄을 훑는다 — **페이지·섹션 개념이 아예 없다**.

### T1~T5 원문 위치 대조표
| 표적 | 원문 위치·문자열 | 현재 산출 | 판정 |
|---|---|---:|---|
| **T1** 상해사망 | **p3** `기본계약 1 갱신형 일반상해사망[기본계약] 1백만원 16` = **100만원**<br>**p6** `대표계약 기준 : … 갱신형일반상해사망[기본계약] **5,000만원**, …` | **5,000만원** | ★예시 오채택 |
| **T2** 상해후유장해 | **p3** `사망후유 2 갱신형 일반상해80%이상후유장해 1백만원 5` = **100만원** | 빈칸(276a) | 룰 미인식 |
| **T3** 상품명 | p2~p26 머리글 `(무) 메리츠 The좋은 알파Plus**보장보험2607(2.0)** (해약환급금미지급형)(납입면제 일반형)` | `(무)메리츠 The좋은알파Plus**종합보장보험2604**` | ★**버전 + 상품 종류** 둘 다 다름 |
| **T4** 납입기간 | p2·p3·p6 `계약사항 : **20년납** 20년만기` | 240개월 | ★이 케이스는 **텍스트로 잡힘** — 폴백 미사용 |
| **T5** 월납 | p3 `보장보험료 **105,802** 원` / p3 `1회차보험료(할인후) **105,800** 원` | **105,800** | ★반올림 아님 — **다른 항목을 읽고 있었다** |

★T5 원인 확정: `_PREMIUM_PATTERNS`의 **첫 패턴이 `1회차보험료(할인후)`**라 105,800을 먼저 잡는다.
원문에 두 값이 **모두 실재**하며(할인보험료 0원인데도 2원 차) 파일명·고객 인식 금액은 **105,802**(보장보험료 합계)다.

★깁스치료비: 원문 전체에 `깁스` **0건** 재확인 → 276a가 비운 상태가 정답이며 이번에도 채우지 않는다.

### ★일반화 가능성 판정 (샘플 1건 제약)
| 항목 | 일반화 확신 | 근거 |
|---|---|---|
| **T1 예시 배제** | ★**부분**(보수적 설계로 회피) | `대표계약 기준 :`은 p6 **한 줄**에만 있고 문구가 매우 명시적이다. 다만 **다른 보험사 양식에서도 같은 문구를 쓰는지는 확인 불가** → **페이지 배제가 아니라 줄(window) 배제**로 좁혀 부작용을 최소화한다 |
| **T2 명칭 추가** | **높음** | `80%이상후유장해`는 메리츠 표준 담보명이고 기존 룰(`상해후유장해`)과 **문자열이 겹치지 않는다** |
| **T3 상품명** | **높음** | 머리글이 **p2~p26 전 페이지에 반복**돼 추출 근거가 강하다 |
| **T4 납입기간** | **높음**(이 케이스 영향 0) | 텍스트로 잡히므로 폴백 제거해도 값 불변 |
| **T5 월납** | ★**Human 확인 권장** | 어느 항목이 "월납보험료"인지는 **도메인 판단**이다. 파일명·합계 기준으로 `보장보험료 합계`를 채택했다 |

---

## Step 2~5 — 구현

변경 파일은 **`backend/coverage/proposal_parser.py` 하나**다.

### Step 2 (T1) — 예시 줄 배제
`EXAMPLE_SECTION_MARKERS`(`대표계약 기준`·`보험료 비교(예시)`) + `_is_example_window()`로
해당 **window를 아예 읽지 않는다**. ★값을 낮게 채택하는 우회는 쓰지 않았다.
★**페이지가 아니라 줄 단위**로 배제한 이유: 샘플이 1건뿐이라 페이지 역할 규칙을 일반화할 근거가 없다.
반면 이 문구는 **그 줄 스스로 "예시"라고 밝히므로** 다른 양식에서도 오탐 위험이 낮고 부작용이 그 줄로 한정된다.

### Step 3 (T2) — ★★구현하지 않았다 · 중단·기록
**BOHUMFIT-243 Human 결정(2026-07-24)과 정면 충돌**한다.
- `constants.py:250` `(re.compile(r"80[%％]이상\S*후유장해"), "80%이상 후유장해", AGG_SUM, False)`
- `constants.py:291` 주석: **"243 결정 유지: '80%이상 후유장해'는 미등록(기타) — 후유장해 집계는 3-100%만."**
- 243 태스크 S1: *"결정: **'80%이상' 후유장해를 후유장해 대분류 집계에서 제외(미포섭)**. 상해/질병 후유장해
  집계는 **3-100% 담보만**. … 담보 자체는 기타에 남아 정보 손실 0"*

즉 **[전] 열은 `80%이상`을 후유장해 집계에서 빼고 있다.** 패킷 체크리스트대로 제안서의
`일반상해80%이상후유장해 100만원`을 `상해후유장해` 행에 넣으면 **[후]만 다른 기준으로 집계**돼,
전/후 비교가 서로 다른 잣대가 된다 — 이 시리즈가 계속 잡아온 종류의 결함을 새로 만드는 셈이다.
★그래서 구현하지 않고 **현행(미확인 경고) 유지**했다.

**권고안(Human 결정 필요)**: 이 담보를 `상해후유장해`가 아니라 **`80%이상 후유장해`(기타)** 로 인식하면
243을 지키면서 정보 손실도 0이 된다. 다만 `coverage_meta("80%이상 후유장해")`가 `None`이라
**40행 양식에는 표시되지 않는다**([전]과 동일한 취급). 표시까지 원하면 **스키마 결정**이 선행돼야 한다.

### Step 4 (T3) — 상품명 원문 보존
`_extract_product()` 신설 — `(무)`/`(유)`로 시작하고 `보험`/`공제`를 포함한 줄을 **그대로** 쓴다.
프로필 키워드로 한 번 더 확인해 오탐을 막고, 못 읽으면 대표 상품명으로 폴백하되 **버전 확인 경고**를 남긴다.
★272b 선례(과잉 절삭 금지) — 괄호 수식어를 깎지 않는다.

### Step 5 (T4·T5)
- `_extract_pay_months`: `DEFAULT_PAY_MONTHS`(240) 폴백 제거 → 못 읽으면 `None` + 경고.
- `_PREMIUM_PATTERNS`: `보장보험료 합계` 패턴을 **맨 앞에 추가**. ★기존 패턴은 **지우지 않고 순서만** 바꿔
  합계 표기가 없는 양식의 폴백으로 남겼다.

---

## 검증 결과 (1차 · 2026-08-06 · Windows 로컬)

### ★엔드투엔드 엑셀 실렌더 (오현지 · `비교분석표` 신규제안 열 = Q)
| 표적 | 276a 이후 | **276b 이후** | 판정 |
|---|---|---|---|
| **T1** 상해사망 | **5,000만원** | ★**100만원**(Q=100) | ★해소 — [후] 합계도 5,200→**300**만원 |
| **T2** 상해후유장해 | 빈칸 | 빈칸(경고 유지) | ★**미구현 · 243 충돌로 중단** |
| **T3** 상품명 | `…종합보장보험2604` | ★`(무) 메리츠 The좋은 알파Plus보장보험2607(2.0) (해약환급금미지급형)(납입면제 일반형)` | ★해소 |
| **T4** 납입기간 | 240(폴백 있음) | 240(**원문 `20년납`에서 추출** — 폴백 제거해도 값 불변) | ★해소 |
| **T5** 월납 | 105,800 | ★**105,802**(원문 일치) | ★해소 |
| 깁스치료비 | 빈칸 | **빈칸 유지** | ★276a 계약 유지 |
| 뇌혈관/심혈관수술 | 빈칸 | **빈칸 유지** | ★276a 계약 유지 |

### 게이트
- [x] backend `pytest -q` **861 passed, 8 skipped** — 기준선 848 + **신규 13**, **기존 회귀 0**
- [x] ★★`npm run smoke:coverage` **PASS** — 정본 2건 기준값 **완전 불변**
      (604,560,000·681,312 / 1,542,990,000·4,675,189) · 정본 상품명 파싱 경로 **무접촉**
- [x] tsc app/node · lint · `npm test` **342/35**(★`src/` diff **0**)
- [x] `npm run build:verify` **예상 FAIL**(248 껍데기)
- [x] ★보호 영역 diff 0: `backend/pipeline/`·`filters.py`·`aggregator.py`·`parser.py`·`constants.py`·
      `export_excel.py`·`export_pdf.py`·`src/`·`vite.config.ts` · 40행 스키마·회사 열 무변경
- [x] ★276a 폴백 제거·미확인 상수 **되돌리지 않음**(테스트로 고정)
- [x] ★`registry_hints`가 담보 값으로 새지 않음(테스트로 고정)
- [x] 실 PDF·PII·산출물 **stage 0**

### 우상균 건(6계약) — ★확인 불가
가입제안서 원본 PDF가 **레포에 없다**(274도 동일 기록). T1~T5가 그 건에서 어떻게 바뀌는지는
**재실행 대조가 불가능**하다. 다만 변경은 전부 제안서 파싱 경로 한정이고 기존 계약(KB) 경로는
무접촉이므로, 그 건의 **기존 계약 열은 영향 0**이다. 제안 열 재확인은 원본 확보 후에만 가능하다.

### 신규 테스트 13건
T1 3(예시 줄 판정·예시 금액 미채택·줄 단위 배제라 다른 담보 정상) / T3 3(원문 보존·폴백 경고·오탐 방지) /
T4 3(None 반환·정상 추출·경고) / T5 2(합계 우선·기존 패턴 폴백 유지) / 276a 계약 2.

## 남은 것 / Human 결정
1. ★**T2 — 243 충돌**: `80%이상후유장해`를 어떻게 다룰지(권고: `80%이상 후유장해` 기타 인식).
   40행 표시까지 원하면 **스키마 결정** 선행.
2. ★**T5 도메인 확인**: 월납보험료로 `보장보험료 합계(105,802)`가 맞는지, `1회차보험료(할인후)(105,800)`가
   맞는지는 업무 판단이다. 파일명·합계 기준으로 전자를 채택했다.
3. ★**샘플 1건 한계**: 가입제안서가 오현지 1건뿐이다. `대표계약 기준` 배제 규칙이 **다른 보험사 양식**에도
   유효한지 확인 불가 — 메리츠 외 제안서 표본이 필요하다.
4. 우상균 제안서 원본 확보 시 T1~T5 재확인.

## Stage 목록 (Codex용)
`backend/coverage/` 변경분 + 276b 테스트,
`.agent-harness/tasks/BOHUMFIT-276b-proposal-parsing-accuracy.md`, `handoff.md`, `locks.md`
★기준선 변동 시 `verify.md`·`CLAUDE.md`·**`AGENTS.md` 3문서 모두**
※제외: 실 PDF·PII·엑셀 원본·생성 산출물

## 커밋 메시지 (Codex용)
fix(BOHUMFIT-276b): 가입제안서 섹션 경계 파싱·담보 명칭 인식·상품명 버전 교정
