# BOHUMFIT-244 — 비분양식 기준 담보 체계 전면 재정의 (매핑 설계·명세)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: Chat/Human (매핑표 승인 대기) · Codex(문서 커밋)
Risk tier: 조사·설계 전용 — **코드 변경 0**. git 쓰기 금지(커밋 Codex). 실 PDF 로컬 참조만·stage 금지.
Date: 2026-07-24

※ PII: 실 PDF는 로컬 전용, 케이스는 **A~E**로만 표기(실명 0). 원문 인용은 담보명·금액에 한정.

## S3 차단 해소 이력 (2026-07-25 갱신)

- 2026-07-24 최초 수행 시 `비분양식.xlsx` 부재로 S3 차단(보장분석·레포·Desktop·Downloads·Documents·OneDrive 전수 탐색 0건).
- **2026-07-25 Human 배치 확인**: `보장분석/비교분석표/비분양식.xlsx`(843,518 B · 2026-07-24 22:51) — S3 실측 수행 완료(아래 섹션). S1·S2·S4는 최초 수행분 유지.

## S1 — 매핑표 (신 항목 전 행 · 현행 → 신 체계)

커버 판정: **✓커버**(현행 항목 그대로/명칭만 변경) · **△부분**(현행이 일부만 포섭) · **✗미커버**(신규 구현 필요)

### [계약 메타]
| 신 항목 | 현행 대응 | 원문/산출 근거 | 판정 |
| --- | --- | --- | --- |
| 구분(유지/해지) | `consulting_plan.existing[].disposition` + `consulting_status` | 236/240 구현분 | ✓커버 |
| 가입일 | `contracts[].contract_date` | 계약리스트 파싱(179) | ✓커버 |
| 납만기 | `pay_years`+`maturity` | 236 D 표시 | ✓커버 |
| 계피관계 | `remark`(계피동일/상이) | 179b `notes` | ✓커버 |

### [사망]
| 신 항목 | 현행 대응 | 실 PDF 원문 예 | 판정 |
| --- | --- | --- | --- |
| 일반사망 | **없음** | A detail `일반사망`(cls 질병사망 6,000만 / cls 상해사망 6,000만) | ✗미커버 → S2-① |
| 재해사망 | **없음** | D detail `재해사망특약`(cls 상해사망 1억) | ✗미커버 → S2-② |
| 질병사망 | KB 표준 `질병사망` | 매트릭스 행 | ✓커버 |
| 상해사망 | KB 표준 `상해사망` | 매트릭스 행 | ✓커버 |

★주의: `일반사망`·`재해사망`은 원문에서 **KB 분류 컬럼이 상해사망/질병사망**으로 잡힌다. 신 체계에서
별도 행으로 뽑으면 KB 표준 상해/질병사망과 **이중 계상 위험** → 집계 규칙 결정 필요(Human).

### [후유장해] — 243 결정 반영(80%이상 제외)
| 신 항목 | 현행 대응 | 근거 | 판정 |
| --- | --- | --- | --- |
| 상해후유장해(3-100%) | KB 표준 `상해80%미만후유장해` | 명칭만 상이(동일 개념). E 검산: 5,100만 = 3-100% 2건 | ✓커버(명칭 매핑) |
| 질병후유장해(3-100%) | KB 표준 `질병80%미만후유장해` | 〃 | ✓커버(명칭 매핑) |
| ~~80%이상~~ | EXTRA `80%이상 후유장해` | **243에서 후유장해 집계 제외·기타로 이동** | 제외(정보는 기타 유지) |

### [암]
| 신 항목 | 현행 대응 | 실 PDF 원문 예 | 판정 |
| --- | --- | --- | --- |
| 암진단금 | KB `일반암` | 매트릭스 | ✓커버 |
| 유사암진단금 | KB `유사암` | 매트릭스 | ✓커버 |
| 암수술 | KB `암수술비` | 매트릭스 | ✓커버 |
| 항암약물방사선 | **없음** | A `항암치료특약`(cls 항암방사선약물치료비 100만) / B `항암방사선약물치료비` 500만 | ✗미커버 → S2-③ |
| 표적항암치료 | KB `고액(표적)항암치료비` △ | B `표적항암약물치료비`(고액항암치료비 7,000만) / D `표적항암약물허가치료특약` 6,000만 | △부분 → S2-④ |
| 면역항암치료 | 없음 | **5케이스 원문 0건** | ✗미커버 · **데이터 없음** |
| 중입자방사선 | **없음** | D `항암 중입자방사선치료특약` 5,000만 / E `갱신형 항암중입자방사선치료비` 5,000만 | ✗미커버 → S2-⑤ |
| 암 주요치료비 | 없음 | **5케이스 원문 0건** | ✗미커버 · **데이터 없음** |

### [뇌]
| 신 항목 | 현행 대응 | 판정 |
| --- | --- | --- |
| 뇌혈관질환 | KB `뇌혈관질환` | ✓커버 |
| 뇌졸중 | KB `뇌졸중` | ✓커버 |
| 뇌출혈 | KB `뇌출혈` | ✓커버 |
| 뇌혈관수술 | KB `뇌혈관질환수술비` (+EXTRA `심·뇌혈관수술비` 접점) | ✓커버(명칭 매핑) |

### [심장]
| 신 항목 | 현행 대응 | 실 PDF | 판정 |
| --- | --- | --- | --- |
| 심혈관질환 | 대응 항목 없음(KB는 허혈성심장질환) | 원문 `심혈관질환` **0건** | ✗미커버 · **데이터 없음**(명칭 정의 Human 확인 필요) |
| 허혈성심장질환 | KB `허혈성심장질환` | 매트릭스 | ✓커버 |
| 급성심근경색 | KB `급성심근경색증` | 매트릭스 | ✓커버(명칭 매핑) |
| 심혈관수술 | KB `허혈성심장질환수술비` (+EXTRA `심·뇌혈관수술비`) | | ✓커버(명칭 매핑) |
| 순환계 치료비 | **없음** | A `갱신형 특정순환계질환 통합치료비` 5,000만 / E `특정순환계질환 통합치료 생활비` 100만·50만 | ✗미커버 → S2-⑥ |

### [종수술] — 238 환산 로직 접점
| 신 항목 | 현행 대응 | 판정 |
| --- | --- | --- |
| 일반종수술 1종~5종 | EXTRA `종수술비(k종·표준환산)`(238 룩업 환산) + 원문 종별 분리형 그대로 | ✓커버(단, **환산분과 원문분 구분 표기 유지 필수** — 238 "표준 환산 기준" 문구) |

### [수술]·[의료이용]·[골절]
| 신 항목 | 현행 대응 | 실 PDF | 판정 |
| --- | --- | --- | --- |
| 상해수술 | KB `상해수술비` | | ✓커버 |
| 질병수술 | KB `질병수술비` | | ✓커버 |
| 응급실 | **없음** | B `갱신형 응급실내원비(응급/비응급)` 각 2만 / E `응급실내원치료비(응급)` 5만 (cls `응급치료`) | ✗미커버 → S2-⑦ |
| 질병입원 | KB `질병입원일당` | | ✓커버 |
| 상해입원 | KB `상해입원일당` | | ✓커버 |
| 골절진단비 | KB `골절진단비` | | ✓커버 |
| 깁스치료비 | **없음** | B `갱신형 깁스치료비`·`[갱신형]깁스치료비(부목치료 제외)` 30만·50만 / C `깁스치료비` 10만 (cls `깁스치료`) | ✗미커버 → S2-⑧ |

### [Y·N 표기 항목] — 금액이 아닌 가입 여부
| 신 항목 | 현행 대응(Y 판정 근거) | 판정 |
| --- | --- | --- |
| 운전자특약 | KB `벌금(대인/스쿨존/대물)`·`교통사고처리지원금`·`변호사선임비용` 중 1건 이상 enrolled | ✓커버(파생 규칙 신설) |
| 자동차부상치료비 | KB `자동차사고부상` enrolled | ✓커버(파생) |
| 가족일상배상책임 | KB `가족/일상/자녀배상` enrolled | ✓커버(파생) |
| 상해실손의료비 | KB `상해입원의료비`·`상해통원의료비` 중 1건 이상 | ✓커버(파생) |
| 질병실손의료비 | KB `질병입원의료비`·`질병통원의료비` 중 1건 이상 | ✓커버(파생) |

### [합계]·[시트3]
| 신 항목 | 현행 대응 | 판정 |
| --- | --- | --- |
| 보험료 합계 | `premium.monthly_total` (+236 A `monthly_total_active` 병기) | ✓커버 |
| 보험료 차액 | `comparison.premium.delta_monthly` | ✓커버 |
| 특이사항 | `remark`/`notes`/`warnings` | △부분(자유서술 규칙 필요) |
| 종합비교 — 암 | 암 그룹 합계 파생 | △부분(집계 규칙 정의 필요) |
| 종합비교 — 뇌 초기/중기/말기 | 파생 합산(원본 수식 실측 — S3 시트3 표 참조) | ✓커버 가능(파생 규칙이 양식 수식으로 확정) |
| 종합비교 — 심장 초기/**중기**/말기 | 파생 합산(★시트3 H10 "심장중기" 오타 — 수식 대역으로 실측 확정·S3) | ✓커버 가능(동일) |

★~~뇌·심장 초기/중기/말기 규칙은 Human 결정 항목~~ → **S3 실측으로 해소(2026-07-25)**: 원본 수식이
포섭 구조(뇌: 초기 질환+졸중+출혈 ⊃ 중기 졸중+출혈 ⊃ 말기 출혈 / 심장: 초기 질환+허혈성+급성 ⊃
중기 허혈성+급성 ⊃ 말기 급성, 공통 가산 = 해당 부위 수술 + 종수술5종 + 질병수술)로 규칙을 정의한다.

### ★전면 대체 시 "자리 없는" 현행 항목 (처리 결정 필요 — Human)
신 체계에 대응 행이 없는 현행 항목들. 폐기 / 기타 유지 중 택일 필요:
`고액암` · `3대비급여실손` · `간병인/간호간병 상해·질병일당` · `보철치료비` · `화재벌금` ·
EXTRA `상급/종합병원 일당`·`상급/종합병원 수술비`·`장기이식수술비`·`양성종양·폴립`·`2대주요치료비(뇌·심)`·
`중환자실 입원일당`·`N대수술비`·`골절수술비`·`화상/화상진단비/화상수술비`·`통원일당`·`교통사고처리지원금(6주미만)`·
`80%이상 후유장해`(243에서 기타로 이동), 그리고 이미 제외 중인 `장기요양간병비`·`경증치매진단`.
→ **권장: 일괄 폐기 대신 "기타" 그룹 유지**(정보 손실 0, 신 양식 본표에는 미표기).

## S2 — 신규 필요 항목 명세 (원문 채집 + 패턴 초안)

원문이 확인된 항목만 패턴 초안을 제시한다. **데이터 없음 항목은 패턴을 만들지 않는다(추측 금지).**
모든 패턴은 현행 `classify_extra` 규약(담보명 기준 판정 · 234 ⑨ 분류-전용 매칭 금지)을 따른다.

| # | 신 항목 | 원문 표기(마스킹) | 패턴 초안 | 비고 |
| --- | --- | --- | --- | --- |
| ① | 일반사망 | `일반사망`(cls 질병사망/상해사망) | `^일반사망` | ★KB 상해/질병사망과 이중 계상 주의 |
| ② | 재해사망 | `재해사망특약`(cls 상해사망) | `재해사망` | 〃 |
| ③ | 항암약물방사선 | `항암방사선약물치료비`, `항암치료특약` | `항암\S*(?:방사선|약물)\S*치료` | 표적/중입자와 우선순위 분리 필요 |
| ④ | 표적항암치료 | `표적항암약물치료비`, `표적항암약물허가치료특약` | `표적항암` | 현행 KB `고액(표적)항암치료비`와 **중복 매핑 검토** |
| ⑤ | 중입자방사선 | `항암 중입자방사선치료특약`, `갱신형 항암중입자방사선치료비` | `중입자` | ③보다 **선순위**로 배치(③ 패턴에 먼저 걸림 방지) |
| ⑥ | 순환계 치료비 | `특정순환계질환 통합치료비`, `특정순환계질환 통합치료 생활비` | `순환계\S*(?:통합)?치료` | 생활비형(연간 N회 조건)은 별도 표기 검토 |
| ⑦ | 응급실 | `응급실내원비(응급/비응급)`, `응급실내원치료비(응급)` | `응급실\S*(?:내원|치료)` | 응급/비응급 구분 표기 여부 Human 결정 |
| ⑧ | 깁스치료비 | `깁스치료비`, `[갱신형]깁스치료비(부목치료 제외)` | `깁스` | 골절 그룹 귀속 |
| ⑨ | 면역항암치료 | — | **없음** | **데이터 없음** — 실 문서 확보 후 착수 |
| ⑩ | 암 주요치료비 | — | **없음** | **데이터 없음** — 정의(어떤 담보 묶음인지) Human 확인 |
| ⑪ | 심혈관질환 | — | **없음** | **데이터 없음** — `허혈성심장질환`과의 관계 Human 확인 |

## S3 — 엑셀 양식 재현 명세 (2026-07-25 실측 · openpyxl로 원본 판독)

실측 대상: `보장분석/비교분석표/비분양식.xlsx` — 시트 3개 `표지(세로)` · `비교분석표` · `최종비교분석표`.
아래 병합·문구·수식은 전부 원본에서 그대로 추출한 값이다(추측 0).

### 시트1 `표지(세로)` — A1:P22 · 세로(portrait)

- 병합 9개: `A1:A22`(좌측 여백 밴드) / `B1:P1` / `B2:P2` / `B4:P4` / `B5:P5` / `B6:P6` / `B7:P7` / `B8:P9` / `B10:P22`(하단 대형 공백 블록).
- 문구(원문 그대로): B1 `OOO님을 위한`(바탕 28) · B2 `보험 보장 분석 리포트`(바탕 28 **B**) ·
  B4 `● 본인이름`(나눔고딕 20B) · B5 `● 직급:` · B6 `● E-MAIL : ` · B7 `● TEL : `(B5~B7 Cambria 20B).
- 열너비 A=1.5 / B=10.8 / C=9.0(나머지 기본). 행높이 1행 75 · 2행 42 · 4~7행 28.9 · 이후 17.6대.
- 재현 요건: 설계사 정보(이름·직급·메일·전화)는 profiles에서 주입, `OOO` 자리에 고객명(마스킹 규칙은 기존 리포트 정책 준용).

### 시트2 `비교분석표` — 인쇄영역 A1:N50 · **가로(landscape)** · 틀고정 B19

**열 구조(패킷 추정 "B~D" 실측 확정)**:

| 열 | 역할 | 너비 |
| --- | --- | --- |
| A | 여백(1.6) · `A2:A49` 세로 병합 | 1.6 |
| **B~D** | **[전] 개별계약 3열** (4~5행 헤더 문구 없음 — 계약명 기입란) | B=14.4, C·D 기본 |
| **E** | [전] 합계 — `E4:E5 "합 계"` | 14.0 |
| **F:H** | 담보명(각 행 F:H 병합, `F3:H5 "담보내용"`) — 종수술 33~37행만 F/H 분리 + `G33:G37` 세로 `일반종수술` | 5.5/기본/5.5 |
| **I** | [후] 합계 — `I4:I5 "합 계"` | 14.0 |
| **J~M** | **[후] 개별계약 4열** (헤더 문구 없음) | K=14.0, J·L·M 기본 |
| N | 여백(1.6) · `N1:N49` 병합 | 1.6 |
| O~Q | 인쇄영역 밖 사이드 메모: O2 `고등전산` · O3 `보험나이` · O6~O14 `1.성명 : `~`8.병원` · O17 `[양식]` · O22~24 `손해(건강)`/`20년/20년`/`20년/100세`(예시 메모) | 9.9/22.6/9.9 |

**행 구조**: B2(`B2:M2`) 타이틀 `" ()" 님을 위한 비교분석표`(돋움 13B) → 6~9행 계약 메타
(`구 분`·`가입일`·`납만기`·`계피관계`, E·I열은 리터럴 `-`) → **10~44행 금액**(신 담보 순서 그대로:
일반사망~깁스치료비, 종수술=33~37행) → **45~49행 Y/N 5항목** → **50행 보험료 합계** → 51행
`B51:M51 "가설계"`(빨강·우측정렬 — ★서식이 `mm-dd-yy`인 텍스트 셀, 원본 잔재로 보임·기록만).

**수식(원본 그대로)**:
- 금액 합계(10~44행): `E10 =SUM(B10:D10)` / `I10 =SUM(J10:M10)` … 행별 반복.
- **Y/N(45~49행)**: `E45 =IF(COUNTA(B45:D45)=0,"N", IF(COUNTA(B45:D45),"Y"))` — **개별계약 칸에 값 존재 여부**로 Y/N 자동 판정(I열 동형). → 244 S1의 "파생 규칙" 방향과 일치.
- 보험료 합계(50행): `E50 =SUM(B50:D50)` / `I50 =SUM(J50:M50)`.

**스타일**: 돋움 11(타이틀 13B) · 헤더/담보명 채움 `FFFFF7CC`(연노랑) · 데이터 셀 흰색(theme0) ·
외곽 medium·헤더 하단 double · 금액 서식 `#,##0_ ` · 메타행 `@`(텍스트).

**★확장 규칙 실측 판정**: [전] 3열·[후] 4열 **고정**이며, T~AME 열 값 0건(빈 서식 잔재만) —
**양식에 계약 수 초과 시 확장 규칙이 정의돼 있지 않다**. 실측 케이스는 계약 9~15건(A=9·B=15·E=15)으로
3열을 항상 초과 → **Human 결정 필요**(선택지: ⓐ열 삽입 확장(병합·수식 범위 자동 보정 필요)
ⓑ상위 N건만 개별 표기+나머지 "기타 합산" 1열 ⓒ계약 그룹핑(보험사별 합산)). 권장: ⓑ(양식 폭 유지·인쇄 1장 보존).

**★단위 추정(양식만으로 불명)**: 시트3 보장금액 서식이 `#,##0"만원"`, 보험료가 `#,##0"원"` —
보장금액은 **만원 단위**, 보험료는 **원 단위** 기입 의도로 추정(서식 접미 근거). 확정은 Human.

### 시트3 `최종비교분석표` — 인쇄영역 A1:L46 · 세로(portrait) · 눈금선 숨김

- `B2:L2` 타이틀 `주요보장분석(이름)`(맑은고딕 48B).
- **좌측 블록**: 4행 헤더 `리모델링 전`(B4·40B)/`주요보장`(C4:E4·32B, 채움 `FFDDEBF7`)/`리모델링 후`(F4·40B).
  5~44행 = 시트2 담보와 1:1(전 행이 `C{n}:E{n}` 병합 담보명 + `B{n} =비교분석표!E{10+n-5}` + `F{n} =비교분석표!I{...}` 참조 수식).
  종수술은 C28/E28=`1`~C32/E32=`5` + `D28:D32` 세로 `일반종수술`. 45행 = 보험료 합계(`B45 =비교분석표!E50` ·
  `F45 =비교분석표!I50` · **`C45 =B45-F45` 차액**, 빨강 `#,##0"원"`, 채움 `FFE7E6E6`).
- **우측 종합비교 블록**(H4:K11): 헤더 `종합`/`전`/`비교`/`후`, J열 = `→`, I열 검정·K열 빨강(`FFC00000`), 서식 `#,##0"만원"`.

| 행 | 항목(원문) | [전] 수식(원본) | 구성 해석 |
| --- | --- | --- | --- |
| H5 | 암 | `=SUM($B$11:$B$17,$B$32,$B$34)` | 암진단~중입자(B11~17) + 종수술5종(B32) + 질병수술(B34) — ★`암 주요치료비`(B18) **미포함** |
| H6 | 뇌초기 | `=SUM(B19:$B$21,$B$22,$B$32,$B$34)` | 뇌혈관질환+뇌졸중+뇌출혈 + 뇌혈관수술 + 5종 + 질병수술 |
| H7 | 뇌중기 | `=SUM(B20:$B$21,…)` | 뇌졸중+뇌출혈 + 수술류 (초기⊃중기⊃말기 포섭 구조) |
| H8 | 뇌말기 | `=SUM(B21:$B$21,…)` | 뇌출혈 + 수술류 |
| H9 | 심장초기 | `=SUM(B$23:$B25,$B$26,…)` | 심혈관질환+허혈성+급성심근경색 + 심혈관수술 + 5종 + 질병수술 |
| **H10** | **`심장초기`(원문)** | `=SUM(B$24:$B25,…)` | ★**H10=심장중기 오타 확정** — 문구는 `심장초기`로 중복 표기돼 있으나 수식 대역(B24:B25=허혈성+급성심근경색)이 초기(B23:B25)와 말기(B25) 사이의 **중기 대역**임을 실측으로 확인. 재현 시 `심장중기`로 정정 기입 |
| H11 | 심장말기 | `=SUM(B$25:$B25,…)` | 급성심근경색 + 수술류 |

- **★원본 수식 이상 1건(기록)**: [후] `K7 =SUM(F20:F22,$F$22,$F$32,$F$34)` — `F22`(뇌혈관수술)가 범위와
  절대참조로 **이중 합산**된다(좌측 `I7 =SUM(B20:$B$21,$B$22,…)`은 정상). K8은 정상. → 재작성 시 **I열 규칙으로 통일** 권장(Human 확인).
- 보험료 차액: `H13:K13 "보험료 차액"` + `H14:K15 =F45-B45`(빨강 `#,##0"원"`). ※시트3 좌측 C45(=B45-F45)와 **부호가 반대** — C45는 절감액(+), H14는 증가액(+) 관점. 재현 시 어느 쪽 관점으로 통일할지 Human 확인.
- 특이사항: `H17:K18 "특이사항 "` 헤더 + `H19:K45` 본문(빈칸 — 자유 서술란).
- 인쇄영역 밖 잔재: `W24 =+J5` · `M18 "; "` — 재현 불필요(기록만).

### `[후]` 열 공란/placeholder 정책 (가입제안서 트랙 전 단계)

- 원본 수식 구조상 [후] J~M 공란이면 **I열 합계=0, Y/N행은 COUNTA로 자동 `N`** — 미작성 상태가
  "보장 0원·미가입(N)"으로 **오독될 위험**이 실물 수식으로 확인됐다.
- 제안: ⓐ 기본은 J열 1열에 `신규 제안 미입력` placeholder 텍스트(COUNTA에 안 잡히게 **I·Y/N 수식은 [후] 미입력 시 `-` 표시로 변형**) ⓑ 236 E 수동 담보 입력분이 있으면 그 값으로 J열 기입 + `수동 입력` 구분 표기(238 "표준 환산"과 동일한 정직 표기 원칙) ⓒ 가입제안서 파싱 트랙 완성 후 원본 수식 그대로 복원. **원본 수식 무변형 유지가 우선이면 ⓐ 대신 "0/N 표시 감수 + 특이사항란에 '리모델링 후 미입력' 자동 기재"** — 택일은 Human.

### export_excel 재작성 요건 (S4 ④단계 입력)

1. 시트 3개 동일명·동일 순서 생성. 인쇄영역(`A1:N50`/`A1:L46`)·용지방향(가로/세로)·눈금선 숨김·틀고정(B19) 재현.
2. 시트2: 담보 41행(10~50) **순서 고정**(S1 매핑표의 신 항목 순서와 동일함을 실측 확인). 병합 51개·열너비·`FFFFF7CC` 채움·medium/double 테두리 재현. E·I열은 **수식 재현**(값 박기 금지 — 양식 정합·사후 수기 수정 허용). Y/N도 COUNTA 수식 재현(단 [후] 정책 ⓐ 채택 시 변형).
3. 시트2 [전] 계약 열: 확장 규칙 Human 결정(ⓐ/ⓑ/ⓒ) 반영 전까지 **구현 착수 불가**(실측 케이스 전부 3열 초과).
4. 시트3: `=비교분석표!E10` 참조 수식 재현. **H10 `심장중기` 정정** · K7 이중합산 정정(I열 규칙) · 암 합계의 B18(암 주요치료비) 포함 여부 Human 확인 반영.
5. 단위: 보장금액 **만원**·보험료 **원**(추정 — Human 확정 후 백엔드 값(원 단위)을 만원 환산해 기입).
6. 스타일 토큰: 돋움 11(시트2)·맑은고딕 26~48B(시트3)·연노랑 `FFFFF7CC`·파랑 `FFDDEBF7`·회색 `FFE7E6E6`·빨강 강조 `FFC00000`/`FFFF0000`.
7. 구현 스택: openpyxl(현행 xlsx 스택)로 전부 재현 가능. ※237의 "금액 문자열화" 결정은 **이 양식에는 부적용** — 원본이 숫자+`#,##0` 서식이므로 숫자 유지(만원 환산 후 정수).

## S4 — 구현 분할안 (단계별 태스크 제안)

전면 대체는 **한 태스크로 하지 않는다**. 각 단계는 독립 검증 가능하고 회귀를 국소화한다.

| 단계 | 제안 태스크 | 범위 | 회귀 위험 | 검증 기준 |
| --- | --- | --- | --- | --- |
| 1 | **신 항목 사전·패턴 추가**(additive) | `constants.py` — S2 ①~⑧ 패턴을 EXTRA로 **추가만**(기존 매핑 무변경) | 낮음(추가 전용). 단 ③⑤ 우선순위·④ 중복 매핑 주의 | pytest 기준선 + 실 PDF 5건에서 신 항목 검출 건수·금액 기록, 기존 수치 전부 불변 |
| 2 | **집계·스키마 전환** | `constants.GROUP*`·`aggregator` — 신 대분류 체계로 교체, "자리 없는" 항목은 기타로 | **높음** — 대분류가 바뀌면 화면·엑셀·PDF·compare 전부 영향 | 실 PDF 5건 그룹별 합계 전/후 비교표 필수. `compare`/`consulting` parity(211) 동시 갱신 |
| 3 | **화면(CoverageRemodel)** | 신 대분류 섹션·Y/N 열·계약 메타 | 중간 | npm test 79 + 라우트 스모크, 240 P1~P3 표시 규칙 유지 |
| 4 | **엑셀 재작성** | `export_excel` — 시트1 표지/시트2/시트3 | 중간(★S3 명세 선행 필수) | 원본 양식과 병합·문구 대조(파일 확보 후) |
| 5 | **PDF 재작성** | `export_pdf` | 중간 | 실렌더 스모크, 238 환산 문구·243 후유장해 규칙 유지 |

**단계별 공통 가드**: 각 단계 후 실 PDF 5건 재실행 + 기준선(pytest 688/8·npm 79·청크 343 kB대) 유지.
**종수술 접점**: 238 룩업 환산은 신 체계의 `일반종수술 1~5종`에 그대로 매핑되나, **환산 산출분에는
"표준 환산 기준(상품별 상이 가능)" 문구가 반드시 따라붙어야 한다**(238 결정). 단계 2에서 이 구분 필드
(`estimated`)가 유실되지 않도록 스키마에 보존할 것.

## Human 결정 대기 항목 (구현 착수 전 필요)

1. ~~엑셀 양식 파일 제공~~ — **해소(2026-07-25 실측 완료)**.
2. `일반사망`·`재해사망`을 별도 행으로 뽑을 때 **KB 상해/질병사망과의 이중 계상 처리** 규칙.
3. `표적항암치료` 신 항목과 현행 `고액(표적)항암치료비`의 **중복 매핑** 정리.
4. `면역항암치료`·`암 주요치료비`·`심혈관질환` — 원문 데이터 없음. 정의·해당 담보 예시 제공 필요.
5. ~~뇌·심장 초기/중기/말기 단계 판정 규칙~~ — **해소(S3 실측)**: 원본 수식이 포섭 구조로 정의
   (뇌 초기=질환+졸중+출혈 / 중기=졸중+출혈 / 말기=출혈, 심장 초기=질환+허혈성+급성심근경색 /
   중기=허혈성+급성 / 말기=급성, 공통 가산=해당 부위 수술+종수술5종+질병수술).
6. "자리 없는" 현행 항목 처리(권장: 기타 유지).
7. 응급실 **응급/비응급 구분** 표기 여부.
8. **(S3 신규)** 시트2 [전] 계약 열 3열 고정 vs 실계약 9~15건 — 확장 규칙 택일(ⓐ열 삽입 ⓑ상위 N+기타 합산 ⓒ보험사별 그룹핑 · 권장 ⓑ).
9. **(S3 신규)** 종합비교 `암` 수식의 `암 주요치료비`(B18) 미포함 — 원본 의도 vs 오류.
10. **(S3 신규)** K7 이중합산 수식 정정 승인(I열 규칙 통일) · 차액 부호 관점(C45=전−후 vs H14=후−전) 통일.
11. **(S3 신규)** 기입 단위 확정(보장금액 만원·보험료 원 — 서식 접미 근거 추정).
12. **(S3 신규)** `[후]` 미입력 표시 정책 택일(ⓐ placeholder+수식 변형 / 원본 수식 무변형+특이사항 자동 기재).

## 검증 체크리스트 (2026-07-24)

- [x] 매핑표에 신 항목 전 행 등장 — 미커버 항목도 "✗미커버 / 데이터 없음"으로 명시
- [x] 실 PDF 5건 원문 근거 인용(케이스는 A~E 마스킹, 실명 0)
- [x] **엑셀 양식 실측 — 완료(2026-07-25)**: 원본 openpyxl 판독으로 병합(9/51/44개)·문구·수식·서식 그대로 수록. 담보 41행 순서가 S1 신 항목 순서와 1:1 일치 확인. 원본 파일 stage 0.
- [x] git diff = `tasks/BOHUMFIT-244-*.md` + handoff·locks만, **코드 diff 0**

## Stage 목록 (Codex용)

tasks/BOHUMFIT-244-*.md, handoff.md, locks.md

## 커밋 메시지 (Codex용)

docs(BOHUMFIT-244): 비분양식 기준 담보 체계 재정의 매핑·구현 분할 명세
