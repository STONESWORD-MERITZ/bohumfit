# BOHUMFIT-253 — by_company 귀속 복원 (합계 있고 회사별 0인 담보 해소)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: Claude Code (진단·복원·1차 검증)
Risk tier: ★고위험(집계 정확성) — 풀 하네스. git 쓰기 금지(커밋 Codex). 실 PDF 로컬 참조만·stage 금지.
Date: 2026-07-27

## 배경 (2026-07-27 Codex 252 반려)
표준 문서 A에서 항암약물방사선 등이 합계 200만인데 by_company 회사합 0 — 담보는 검출·
합계 반영되나 계약(회사) 귀속이 비어 회사별 전개 시 "어느 회사에도 없음"으로 오독.
HEAD 기존 결함(합계 열만 볼 땐 비가시). 유력 원인: 245 EXTRA 신규 패턴(항암약물방사선·
중입자·순환계·응급실·깁스·일반/재해사망 등)이 검출·합계는 하되 by_company 셀 미충전.

## 제1원칙
★정확도·누락 0 + 귀속 정합: 담보별 회사합 = 합계 (전 담보·전 케이스 차이 0).
귀속 불가한 담보(원문에 계약 근거 없음)는 임의 배치 금지 → "계약 미확인" 명시 처리
(251 미특정 블록 원칙과 동일 — 오귀속보다 미배치+명시).

## S0 — 진단
1. 표준 5케이스에서 by_company 회사합 ≠ 합계인 담보 전수 목록화(담보·케이스·합계·회사합·차액).
2. 각 담보의 검출 경로 추적: 245 EXTRA 패턴 검출 시 원문 라인에 계약(회사) 귀속 정보가
   있는지 실측 — 있으면 파서가 버리는 것(복원 가능), 없으면 원천 부재(계약 미확인 처리).
3. 매트릭스 담보(by_company 정상)와 EXTRA 담보(귀속 누락)의 처리 경로 차이 특정 —
   EXTRA가 매트릭스 셀 귀속 로직을 안 타는 지점.

## S1 — 복원
A. 원문에 계약 귀속 근거가 있는 담보: 파서가 계약(회사) 키를 by_company에 채우도록 복원.
   매트릭스 담보와 동일 귀속 경로 사용(단일 소스 지향).
B. 귀속 근거가 원문에 없는 담보: by_company에 "계약 미확인" 의사 열/키로 명시 귀속
   (합계에는 이미 포함 — 회사합=합계 성립하되 "미확인" 버킷이 차액을 흡수).
   ★251 원칙: 오귀속 금지, 미확인 명시.
C. ★불변: 담보 합계·40행·[후] 이월·총액 대사(246) 무변경 — by_company 내부 채움만.
   246 총액 대사 5건 차이 0 유지.

## S2 — 검증
- 표준 5케이스 담보별 회사합=합계 차이 0(미확인 버킷 포함).
- 246 총액 대사 5건 0·전=후 동일성·월납/부값 불변.
- 미확인 버킷 발생 담보·금액을 태스크 문서에 기록(어느 담보가 왜 미확인인지 — Human 검토용).

## 수정 범위
backend/coverage/(parser·aggregator by_company 귀속), 테스트. export_excel 무접촉(253 후 252 재개).

## 수정 금지
- backend/pipeline/ 무접촉·담보 합계값 변경 금지(귀속만)·246 대사 원칙 변경 금지
- 실 PDF·PII stage 금지(픽스처 익명 합성)

## 진행 기록 (Claude Code · 2026-07-28)

### S0 진단 결과
**1. 불일치 전수(실계약 키 기준 회사합 vs 합계 — 5케이스)**
| 케이스 | 담보 | 합계 | 실계약 회사합 | 차액 | by_company 실제 키 |
| --- | --- | --- | --- | --- | --- |
| A | 항암약물방사선 | 2,000,000 | 0 | 2,000,000 | {'?': 200만} |
| A | 양성종양·폴립 | 3,650,000 | 2,650,000 | 1,000,000 | {'1': 265만, '?': 100만} |
| A | 화상 | 1,100,000 | 0 | 1,100,000 | {'?': 110만} |
| D | 재해사망(계약 미확인) | 100,000,000 | 0 | 100,000,000 | {'?': 1억} |
| B·C·E | (없음 — 전 담보 일치) | | | | |
- ★구조 확정: 금액은 by_company 안에 전부 있고 **의사 키 `'?'`(계약 미상 — 246 기존
  설계)에 귀속**돼 있었다. 파이프라인의 "계약 미확인" 데이터 모델은 이미 존재 —
  실계약 키가 아니라서 엑셀 회사 열 합산에서 빠졌던 것.
**2. '?' 발생 원인(원문 실측 — 페이지 헤더 구조 신호만 조사·원문 미출력)**
- `_detail_contract_idx`가 **기간 라인(YYYY-MM-DD ~ YYYY-MM-DD) 위의 보험료만** 매칭 —
  layout=True 추출이 표 행을 줄바꿈해 **월보험료가 기간 라인 2줄 아래**에 놓인 페이지
  (A p10·p17, D p15)에서 None → '?'. ★해당 페이지 모두 16줄 헤더 안에 계약 월보험료가
  존재하고 premium 맵과 유일 매칭 — **원문에 근거 있음·파서가 버림(복원 가능)**.
- D p20(재해사망 중복 행)은 기간·보험료 모두 없음 + 상품명 2건 등장(모호) — 원천 모호.
**3. 경로 차이**: 매트릭스 담보는 열 헤더로 계약 귀속(정상), EXTRA(245)는 detail 페이지
  단위 `_detail_contract_idx` 귀속 — 이 함수의 기간 라인 제약이 유일한 공백 지점.

### S1 복원 (parser.py — `_detail_contract_idx` 폴백 1개)
- 기간 라인 매칭 실패 시 **헤더 16줄 전체에서 계약 월보험료 일치 값을 수집, ★유일한
  계약으로 결정될 때만 귀속**(복수/0 매칭 = 모호 → None → '?' 유지 — 오귀속 금지·251 원칙).
- B(계약 미확인 버킷): 신규 구현 불필요 — 기존 '?' 키가 그 자체(합계에 이미 포함·246
  이월 규칙도 '?' 보존). 대사 정의를 "회사합 = 실계약 키 + '?' 버킷"으로 명문화(테스트 고정).
- 집계(aggregator)·합계 산식·246 대사 원칙 무접촉.

### S2 결과 — ★복원 후 5케이스 전수
- A·B·C·E: **summary 변화 0** · '?' 잔존 0행 · 실계약 키만으로도 회사합=합계 상이 0 ·
  월납 불변 · 전=후 유지. (A 항암약물방사선 200만→계약9, 화상 110만→계약2,
  양성종양·폴립 100만→계약1 — 전부 실계약 귀속 복원.)
- **D ★파생 변화(보고 — Human·Codex 검토 필수)**: p15 재해사망 1억이 계약6으로 귀속되며
  246 사망 배타 설계의 **본래 경로(근거 있으면 차감+승격)가 정상 발동** —
  `재해사망(계약 미확인)[기타] 1억` 소멸 → 정식 `재해사망[사망] 1억` 승격 +
  `상해사망 383,080,000 → 283,080,000`(지급사유 셀 1억 차감). 즉 기존 산출은 같은 1억이
  상해사망 셀과 기타 버킷에 **이중 계상**돼 있었고, 귀속 복원이 이를 해소한 것.
  ※패킷 불변 계약 C("담보 합계값 변경 금지")와 표면상 충돌하나, 이는 귀속 복원의 직접
  파생(246 설계 경로 발동)이며 제1원칙(정확도)·246 대사 원칙(이중 계상 0)에 부합 —
  임의 값 변경이 아님. 억제하려면 이중 계상을 의도적으로 유지해야 해 미채택. Human 결정
  필요 시 D 케이스 재검토 대상으로 명시.
- 미확인 버킷 잔존: **실 5케이스 0건**(전부 복원 또는 dedup 흡수). '?' 유지 가드는 합성
  회귀로 고정(모호·원천 부재 2케이스).

## 검증 체크리스트 (1차 — 완료 · 2026-07-28)
- [x] backend pytest — **736 passed, 8 skipped**(732 + 신규 4: 폴백 귀속·모호 '?' 유지·
      원천 부재 '?' 유지·'?' 포함 대사+[후] 이월 보존)
- [x] ★표준 5케이스 담보별 회사합=합계 차이 0 — 실계약 키만으로 전 케이스 성립('?' 잔존 0)
- [x] 246 대사·전=후·월납/부값: A·B·C·E summary 변화 0·월납 5케이스 불변·전=후 유지.
      D는 위 ★파생 변화(이중 계상 해소 — 항목·금액 명시 보고)
- [x] overview E 무영향 — summary 변화 0·대사 성립
- [x] PII 0(합성 픽스처·원문 라인 미출력), diff 범위 = parser.py + 테스트 1 + harness.
      aggregator 변경 0(불필요 판정)·pipeline/ diff 0·export_excel 무접촉(252 워킹트리
      변경분은 별개 태스크 — 253 diff 아님)

## Stage 목록 (Codex용 · 실측 갱신)
backend/coverage/parser.py, backend/tests/test_coverage_bycompany_253.py,
tasks/BOHUMFIT-253-*.md, handoff.md, locks.md
※aggregator.py 변경 0(기존 '?' 모델 재사용) — 패킷 범위에서 축소.
※워킹트리의 export_excel.py·test_excel_company_252.py·test_coverage_contract_list_184.py·
  tasks/BOHUMFIT-252-*.md는 **252 태스크分**(Codex BLOCKED 상태 유지) — 253 stage에 포함 금지.

---

## 회송 보정 (2026-07-28 · Codex 반려 — 동일 보험료 계약 축약 오귀속 → Claude Code)

### 반려 원인
`premium_to_idx = {monthly_premium: idx}` dict의 last-wins — 동일 월보험료 계약 2건 이상이면
앞 계약이 덮여, 실제로는 모호한 매칭을 "유일"로 오인(Codex 실증: 50,000원 계약 2건에서
200만원이 '?'가 아닌 계약 2로 ★오귀속). 기간 라인 경로·헤더 폴백 양쪽 동일 위험.

### 보정 (parser.py — Codex 제시 방향 그대로)
1. **`_premium_index`**: 월보험료 → 계약 idx **★집합(frozenset) 역인덱스** — 중복 보존,
   last-wins dict 제거.
2. **`_resolve_unique_idx`**: 후보 보험료들의 계약 후보 **합집합이 정확히 1개일 때만** 귀속
   — 기간 라인 경로·헤더 16줄 폴백이 이 **단일 헬퍼를 공유**(경로별 복제 금지 — 251 골든
   픽스처 교훈). 동일 보험료 복수 계약·상이 보험료 복수 매칭·매칭 0 = 전부 모호 → '?'.
3. 기간 라인 경로의 구 reversed last-match 순회도 제거 — 모호하면 폴백으로 진행 후
   그래도 모호하면 '?'(오귀속 0이 제1원칙 — 251 미배치+명시).

### 필수 테스트 추가 (test_coverage_bycompany_253.py — 기존 4 + 신규 5 = 9)
- ★반려 재현: 동일 보험료 2계약 + 담보 1건 → '?' 유지(계약2 오귀속 0) — **헤더 폴백·기간
  라인 양 경로 각각** 고정.
- 동일 보험료 3계약 → 양 경로 '?' 유지.
- 상이 보험료 유일 매칭 → 양 경로 귀속(기존 통과 유지).
- 기간 라인에 두 계약의 상이 보험료 동시 등장(구 last-match 승자 지점) → '?'.

### ★검증 실행 불가 — 사유 명시 (AGENTS.md 계약)
- **이 세션의 셸 실행 환경이 태스크 도중 완전 불능**: 모든 Bash 명령(`echo`·`true`·
  `python --version` 포함)이 출력 0·exit 2로 즉시 실패. 서브에이전트(별도 셸)로도 동일 —
  세션/호스트 수준 셸 초기화 실패로 판정(수 회 재시도·수십 분 간격 포함).
- 따라서 **pytest(신규 9종·전체 736/8)·실 PDF 5케이스 재확인(A 유일 특정 여부·D p15
  재해사망 유일 매칭 여부)·tsc/lint/npm — 전부 미실행**. 1차 검증 없음이 사실이다.
- 수행한 정적 검증(실행 대체 아님 — 한계 명시):
  ① 보정 diff 전량 재독(역인덱스 frozenset·단일 헬퍼 공유·잔여 premium_to_idx 참조 0 확인)
  ② 신규 테스트 픽스처의 경로 유효성 육안 추적(기간 정규식·DETAIL_PREMIUM_RE 매칭·
     _page 헤더 16줄 범위)
  ③ 기존 detail 테스트 픽스처(179b·234·237·238) 보험료 중복 없음 확인 — 동일 보험료
     회귀로 인한 기존 테스트 파괴 가능성 낮음(단, 실행 증명 아님)
- **Codex는 2차가 아니라 1차 게이트부터 전부 실행해야 한다**: ①신규 9종 ②전체 pytest
  ③실 PDF 5케이스 회사합=합계·summary 변화 재계측(특히 A 항암약물방사선 계약 9 유일
  특정 여부 — 동일 보험료 계약이면 '?' 잔존이 정답, D p15 재해사망 계약 6 유일 매칭 여부 —
  유일 아니면 D 파생(승격·상해 −1억)이 소멸하고 '?' 복귀·handoff 기록 필요) ④프런트 게이트.

## Codex 권위 검증 결과 (2026-07-28 · commit 전)
- 셸 정상·루트 게이트 PASS. 표적 신규 파일 **9 passed**, 전체 backend **741 passed,
  8 skipped**. 이전 예상 745는 기존 4종을 신규 파일 합계 9에 다시 더한 중복이며,
  정확한 기준은 `732 + 9 = 741`. tsc app/node·lint·frontend **95 passed**.
- 정적: `_premium_index`=`frozenset` 후보 보존, `_resolve_unique_idx` 양 경로 공유,
  `premium_to_idx` last-wins 잔재 0. 동일보험료 2계약 반려 재현은 헤더/기간 양 경로 모두
  `?`로 고정.
- 실 PDF A: p17 월보험료 22,680원의 후보는 계약 9 하나. 항암약물방사선 200만은 계약 9로
  귀속되며 회사합=합계 차이 0, `?` 잔존 0.
- 실 PDF D: p15 월보험료 167,700원의 후보는 계약 6 하나. 재해사망 1억은 정식 사망으로
  승격되고 상해사망은 383,080,000→283,080,000. D 총액은
  1,733,820,000→**1,633,820,000**으로 1억 감소하며 회사합=신총합 차이 0 — 동일 1억
  이중 계상 해소의 직접 파생으로 확정.
- 총액 A 1,373,510,000 / B 604,560,000 / C 1,670,740,000 / E 1,542,990,000은 HEAD와
  동일. 표준 A~D 회사합 대사 0, overview E는 합계-only 원천이므로 HEAD 전체 동일성으로
  무영향 확인. 전 5건 전=후·월납 부값(A 2,282,564/B 681,312/C 183,621/D 495,389/
  E 4,675,189) 불변, estimated 생존.
- PII·실 PDF·임시 산출물 stage 0. 253 diff는 parser+신규 테스트+harness뿐이며
  aggregator·pipeline/ 0. 워킹트리 252 `export_excel`·관련 테스트/task는 별도 보존·stage 제외.

## 커밋 메시지 (Codex용)
fix(BOHUMFIT-253): by_company 귀속 복원 — 동일보험료 모호 시 '?' 유지(오귀속 방지)

## Next (handoff 명시)
① Chat — BOHUMFIT-252 재개(귀속 정합 위에서 회사별 렌더, '?' 잔존 시 미확인 열 정책)
② Human — D 최종 상태 확인(재해사망 별도 표시·상해사망 1억 차감은 유일 귀속 재계측 결과)
