# BOHUMFIT-285 — 수술 판정 정확도 조사 (★조사 · 코드 0)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: **Human**(개선안 채택 결정) · Risk tier: 조사(코드 변경 0 · 문서만)
Date: 2026-08-22 · 기준 HEAD `8ecb38e`(300) · 야간 세션 · git 쓰기 0 · 실 PDF 읽기 전용
★실 고지 PDF는 Human이 세션에 입력한 비밀번호로 열람했다 — **비밀번호는 이 문서·handoff·레포 어디에도 기록하지 않았고**,
비밀번호가 담겼던 임시 스크립트는 실행 후 덮어쓰기+삭제했다. 환자명·병원 실명 미기재(상병코드·시술명·건수만 — 277 기조).

---

## ★핵심 판정 요약
1. **오분류 기전 특정**: `인두이물제거술(단순[편도상와])`은 `keywords.json > surg_keywords`의 광범위 키워드
   **`제거`** 에 걸리고, negative·비수술 행위 목록 어디에도 안 걸려 **수술 확정**으로 간다.
2. ★**EDI 수가코드는 이 심평원 양식의 파싱 데이터에 존재하지 않는다**(실측) — **A안(코드 사전)의 전제 반증**.
3. 이 실 고지 문서의 수술 확정은 **1건뿐이고 그 1건이 강등 후보(오분류율 100%)**. ★294 B 유형은 **이 표본에서 0건**.
4. ★반대 방향(누락) 실측 수확: **`누점폐쇄술`이 수술 미판정**(잠재 누락 후보 — "폐쇄술"이 키워드에 없음).
5. ★카운트 영향 실증: 이 건의 그룹(T17)은 통원 1회·투약 1일뿐이라 **수술 확정이 유일한 Q3 사유** —
   강등을 카운트에서 빼면 그룹 전체가 Q3에서 사라진다. → **권고: 표시만 2단, 카운트는 유지**(아래 4장).

---

## 1. 데이터 실측 (실 고지 PDF 3종 · 파싱오류 0)

### 1-1. 파싱 결과
| 파일 | 레코드 | ftype |
|---|---|---|
| 병력(진료정보) | 124 | basic |
| 세부진료내역 | 550 | detail |
| 투약이력 | 440 | pharma |

### 1-2. ★분기점 — 원문 필드 전수 (detail 헤더)
`순번 · 진료시작일 · 병·의원&약국 · 진료내역 · 코드명 · 1회투약량 · 1일투여횟수 · 총투약일수`
- ★**수가코드(EDI)·분류번호 컬럼이 없다.** `코드명`은 숫자 코드가 아니라 **행위 명칭 텍스트**다
  (인두이물 행 실측: 진료내역=`처치및수술(양방)` · 코드명=`인두이물제거술(단순[편도상와])`).
- basic 헤더도 `주상병코드`(KCD)뿐 — 행위 코드는 없다.
- 파서(`pdf_parser`)는 테이블 **전 컬럼을 rec에 보존**하므로, 코드 컬럼이 있는 다른 발급 양식이 오면
  자동으로 담긴다. 그러나 **이 표준 양식에서는 명칭 기반 판정이 유일한 선택지**다. → A안 전제 무너짐.

### 1-3. 인두이물 건 판정 트레이스 (함수 단위 실측)
| 단계 | 결과 |
|---|---|
| `is_non_surgery_excluded`(062 전역 제외) | False — `NON_SURGERY_NAMES`에 없음 |
| `is_non_surgery_action`(059 행위 제외) | False — 목록에 `이물제거` 계열 없음 |
| positive(`surg_keywords`) | **`제거` 매칭** ← 원인 |
| negative(`surg_negative_keywords`) | 0건 — `(단순…`은 `단순처치`와 미일치 |
| `_is_surgery_match` / `_is_detail_surgery_match` | **True / True → 수술 확정** |
진료기간=최초진단=같은 날(2026-04-11 당일 외래)·의원급·`(단순)` 한정자 — 어느 신호도 판정에 반영되지 않는다.

### 1-4. 판정 흐름 (확정 1개 필드가 4경로를 전부 물린다)
`disease_aggregator`: detail 행 → `s["surgeries"]`+`surgery_dates`(확정) / nhis 행 → `surgery_suspected_*`(의심·강/약).
- **Q1~Q5**(filters): Q1=3개월(진단·수술·투약), Q3=5년(입원 OR **수술** OR 통원7 OR 투약30), Q4=5~10년(입원·수술 + 공단 의심 포함)
- **카카오**(main): `surgery_count>0`이면 건별 전개(`surgery_records`) 또는 이름 나열 · 의심은 `수술 의심: …(강/약)` 별도 줄
- **리포트 PDF**(report_pdf)·**프런트**(Disclosure.tsx): 같은 item 필드 소비 — 030~032 "배지=헤더 판정 일치" 원칙대로
  **surgeries/surgery_dates 하나가 4경로를 전부 결정**한다. 판정만 고치면 4경로가 함께 고쳐진다(251 4경로 정합 구조 그대로).

## 2. 기존 자산 실측
- **보장분석 "수술 등급 DB"의 실체 = `jong_surgery_conversion`(238·`DEFAULT_JONG_TABLE`)**: 키가 **5종 기준 금액**인
  금액 환산표다. 수술명→등급 매핑이 아니므로 **고지 확정 티어 근거로 재사용 불가**(B안 기각 근거).
  레포·Supabase(manual SQL 전수 확인)에 수술명 기반 등급 DB는 존재하지 않는다.
- **`medical_judgment`(ai_judgment)**: 수술 판정을 하지 않는다. `cross_surgery_hints`를 프롬프트 컨텍스트로
  전달만 한다(판정 주체는 결정론 코드 — 169 이후 유지).
- **291·301 종수술 tier 체계와의 관계**: 무관. 그쪽은 보장분석 담보명(`1-5종수술비(N종)`)의 **가입금액 라우팅**이고
  여기는 진료내역 행위명의 **수술 여부 판정**이다. 공유 자산 없음.
- 이미 존재하는 방어 3겹(빼기 방향): 062 `NON_SURGERY_NAMES`(4건 exact) · 059/104/106/130
  `_NON_SURGERY_ACTION_KEYWORDS`(강수술 신호 통과) · `_is_detail_support_only`(SNARE·카테터 등 보조재).
  → **이번 건은 3겹 어디에도 안 걸리는 "명칭은 수술스럽지만 약관상 수술성이 불확실한" 새 부류**다.

## 3. 오분류 표본 (실 고지 전수)
- 수술 확정 전수: **T17 그룹 1건 — `인두이물제거술(단순[편도상와])`** (2026-04-11 · 의원급). 그 외 확정 0.
- 강등 후보 패턴(`이물제거술(단순`·`발사`·`드레싱`·`세척`·`흡인`·`천자`·`도뇨`·B 유형 4종) 적용:
  **1/1건(100%)** 이 강등 후보. ★**B 유형(치료재료비용·수술팩·IVPCA·절삭기류)은 이 문서에 0건** —
  294가 캡처한 B 유형 문서는 이 고객 건이 아니다. **B 유형 정량은 해당 원문 확보 시 재측정**(확인 불가 사유: 표본 불일치).
- 공단(nhis) 파일 없음 → 수술 의심 0건(이 표본에서 의심 경로 미발동).
- ★**누락 방향(더 중요)**: 수술 미판정 detail 행 중 수술성 어휘 보유 전수 —
  `누점폐쇄술`(★잠재 누락 — 눈물점 폐쇄는 시술·수술성 판단 여지, `폐쇄술` 키워드 부재) ·
  침술 계열 4건(구술·복강내 침술·투자법 침술·침전기자극술 — 한방 자극요법, 미판정이 **정당**).
  → 강등 설계와 별개로 `폐쇄술` positive 추가 검토 대상(누락 0 원칙).

## 4. 개선안 (★구현 금지 · Human 결정용)

### A안 — EDI 수가코드 사전
**전제 반증됨**(1-2): 이 양식엔 코드가 안 들어온다. 코드 컬럼이 실재하는 발급 양식이 확인되기 전까지 **보류**.
(파서는 전 컬럼 보존이라 코드가 오는 양식이 나타나면 그때 무변경으로 데이터가 쌓인다.)

### B안 — 보장분석 등급 DB 재사용
**기각**(2장): 실체가 금액 환산표라 수술명 판별 불가. 신규 수술명 DB 구축은 별도 대형 과제.

### C안 — 처치 패턴 **강등**(제외 아님) ★권고
- 확정 조건은 그대로 두고, 확정된 이름이 **강등 패턴**(`이물제거술(단순`·`발사`·`세척`·`흡인`·`천자`·`도뇨`* ·
  `치료재료비용`·`수술팩`·`정맥내주입(IVPCA)`·`절삭기류`)에 걸리면 `surgeries` → 신설 `surgery_review`
  ("수술 여부 확인")로 옮긴다. (*`도뇨`·`천자`는 이미 059 목록에 있어 detail 경로에선 대부분 걸러짐 —
  강등 대상은 주로 컬럼 경로·basic 경로 통과분.)
- **기존 `surgery_suspected`와의 관계 = 별개 축 유지, 3단 정리**:
  | 티어 | 원천 | 의미 |
  |---|---|---|
  | 수술 확정 | 심평원 detail/basic 키워드·컬럼 | 명칭·컬럼이 수술을 명시 |
  | **수술 여부 확인(신설)** | 심평원 — 확정 조건 충족 + 강등 패턴 | **정보는 있으나 약관상 수술성 불확실** |
  | 수술 의심(기존 강/약) | 공단 — 진료비 기반 추정 | 정보가 없어 추정 |
- ★**카운트 처리(핵심 트레이드오프)** — 2안:
  - **C-1(권고): 표시만 2단, Q1~Q5 카운트 유지.** 강등 건도 `surgery_dates`에 남긴다 → 헤더 판정·배지 불변
    (030~032 일치 원칙 자동 준수) · smoke·골든 픽스처 무변동 · **누락 위험 0**. 화면·카카오·PDF에서
    `수술 확정` 대신 `수술 여부 확인` 라벨만 바뀐다. 047 면책 문구와도 정합(제품이 단정하지 않음).
  - C-2: 카운트에서도 제외. ★실측 실증(T17: 수술이 유일한 Q3 사유)처럼 **그룹이 헤더에서 통째로 사라질 수 있다**
    — 고지 누락 방향 위험. 위험 비대칭 원칙상 **비권고**. 채택하려면 Q6급 대조표+Human 승인 필수.
- 접촉 파일(구현 시): `surgery_exclusions.py`(강등 패턴 사전 — 한 곳) · `disease_aggregator.py`(확정→review 분류) ·
  `result_builder.py`/`main.py`(카카오 라벨) · `report_pdf.py` · `Disclosure.tsx`(배지 문구) · keywords.json 무변경.
- 테스트 전략: 실 PDF 회귀(이 문서 1건 = 확정→확인 이동) · 합성 픽스처(강등 있음/없음/강수술 공존) ·
  251 4경로 동등성(서버 카카오=프런트 memo=PDF=화면) · C-1이면 filters 골든 **무변동 단언**이 그대로 회귀 방패.

### 조합안
**C-1 즉시 + `폐쇄술` positive 추가(누락 보강) + A안은 코드 실재 양식 확보 시 후속.** ← 권고.
트레이드오프: C-1은 과잉 표시를 완전히 없애지 못한다(카운트에 남음) — 그러나 위험 비대칭(누락≫과잉)과
047 면책 정합, 표시 개선의 즉효를 모두 만족하는 유일한 조합이다.

## 5. AI 판정 확장 검토 (비추천 전제)
`medical_judgment`는 현재 수술 판정 주체가 아니다(2장). 프롬프트 확장으로 푸는 안은 **비추천 유지**:
백로그의 프롬프트 축소 방향과 역행 · 약관 해석 판정의 근거 추적 불가 · 사전 기반이 감사 가능.
필요 지점 없음 — 강등 패턴은 결정론 사전으로 충분하다(이번 실측에서 반례 0).

## ★Human 결정 필요
1. **C-1(표시 2단·카운트 유지) 채택 여부** — 채택 시 구현 태스크 발행(값 계층 무변동·표시 계층만).
2. **`폐쇄술` positive 키워드 추가**(누락 보강 — `누점폐쇄술` 실측 근거). 카운트가 **늘어나는** 방향이라 Q6 대조 필요.
3. 294 B 유형 원문(다른 고객 캡처 문서) 확보 여부 — 확보 시 B 유형 정량 재측정.

## 확인 불가 (사유 명시)
- **294 B 유형 정량(건수·줄 수 비중)**: 이 실 고지 표본에 B 유형 0건 — 294 캡처의 원본 문서가 레포·세션에 없다.
- 코드 컬럼이 실재하는 타 발급 양식의 존재 여부: 표본 1세트(3파일)로는 판단 불가.

## 검증
- [x] `git status` 제품 코드 diff 0 (문서만)
- [x] ★비밀번호·실명·연락처·주민번호·주소 기록 0건 · 비밀번호 담긴 임시 스크립트 덮어쓰기+삭제
- [x] 실 PDF 미복사·미stage (`보장분석/` gitignore 그대로)
- [x] 확인 불가 2건 사유 명시

## 커밋 메시지 (Codex용 · 문서만)
docs(BOHUMFIT-285): 수술 판정 정확도 조사 — EDI 수가코드 부재 실측·C-1 표시 강등 권고·폐쇄술 누락 발견
