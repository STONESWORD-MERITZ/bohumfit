# BOHUMFIT-245 — 신 체계 1단계: 신규 담보 패턴 additive 추가 (244 S2 기반)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: Codex (2차 검증·커밋)
Risk tier: 중위험 — 풀 하네스. git 쓰기 금지(커밋 Codex). 실 PDF 로컬 참조만·stage 금지.
Date: 2026-07-25

## 배경 (2026-07-24 Human 확정)
244 매핑표 승인. 1단계 = 현행 체계를 유지한 채 미커버 7종 + 표적 통합만 additive 반영.
대분류 교체(2단계)는 별도 — 이번엔 검출·표시(기타 또는 기존 그룹)까지만.

## 구현 항목 (244 S2 원문 근거 기반 — 추측 금지, S2 채집 표기 사용)
1. 일반사망 / 재해사망: 원문에 독립 담보로 명시된 경우만 검출(A `일반사망`, D `재해사망특약`).
   ★상호배타 가드 필수: 동일 담보가 상해/질병사망과 이중 계상되지 않도록 —
   일반/재해사망으로 잡힌 라인은 상해/질병사망 집계에서 제외(또는 그 역), 총합 보존 검증.
2. 항암약물방사선: `항암방사선약물치료비` 계열.
3. 중입자방사선: `항암중입자방사선치료비` — ★패턴 우선순위 중입자 > 항암약물방사선
   (포괄 패턴 선점 방지, 244 명시).
4. 순환계 치료비: `특정순환계질환 통합치료비` 계열.
5. 응급실: `응급실내원비`(cls 응급치료).
6. 깁스치료비: `깁스치료비`.
7. 표적항암 통합: `고액(표적)항암치료비` + `표적항암약물치료비` → 단일 라벨
   "표적항암치료" (기존 고액(표적) 값 이관 — 값 유실 0, 이중 계상 0).
※ 면역항암·암 주요치료비·심혈관질환은 원문 데이터 없음 — 패턴 미작성(2단계에서 행만 존치).

## 수정 범위
backend/coverage/constants.py·parser.py(필요 최소), 관련 테스트

## 수정 금지
- ★대분류 체계·GROUP_ORDER 변경 금지(2단계 소관) — 신규 항목은 기존 인접 그룹 또는 기타로
  귀속하되 라벨은 신 체계 명칭 사용(2단계 이관 대비)
- backend/pipeline/ 무접촉, 234~243 보정 로직 재변경 금지
- 실 PDF·PII stage 금지(픽스처 익명 합성)

## 단계별 지시
1. S0: 244 S2의 원문 표기를 실 PDF 5건에서 재확인(라인 채집), 각 패턴이 기존 37표준·EXTRA와
   충돌하지 않는지 매칭 시뮬레이션(특히 중입자·항암약물 선점, 표적 통합 시 기존 테스트 파급).
2. 패턴 추가 + 상호배타 가드(사망) + 표적 통합. 각각 익명 픽스처 테스트.
3. 실 PDF 5건 재실행 — 신규 검출 목록·값 기록, ★기존 수치 파급 검증:
   표적 통합·사망 배타로 값이 이동하는 항목은 [전/후 비교표]로 기록(총합 보존 확인),
   그 외 전 항목 불변.

## 검증 체크리스트 (1차)
- [ ] backend pytest — 688/8 + 신규분(기존 무손실 — 표적 통합 파급 갱신은 근거 주석)
- [ ] tsc·lint·npm test 79·build 통과(청크 343 kB대)
- [ ] 실 PDF 5건: 신규 7종 검출 기록(A 일반사망 6,000만·D 재해사망 1억·D·E 중입자 5,000만 등
      244 S2 값과 대조), 사망 이중계상 0(총합 보존), 월납·부값 전부 불변
- [ ] PII grep 0, diff 범위 = constants·parser·테스트 + harness만

## Stage 목록 (Codex용)
수정 파일, 테스트, tasks/BOHUMFIT-245-*.md, handoff.md, locks.md — 실 PDF 제외

## 커밋 메시지 (Codex용)
feat(BOHUMFIT-245): 신 체계 1단계 — 신규 담보 패턴 추가·표적항암 개명 통합·사망 dedup

## Next (handoff 명시)
① Chat — BOHUMFIT-246(집계·스키마 전환, 고위험) 투입 — 프롬프트 기수령 상태
② Human — 재업로드로 기타 그룹 신규 6종 표시 확인(선택 — 246 후 통합 검수 가능)
③ 246 결정 이관 — A 항암약물방사선 100만×2 dedup 여부(detail 원문 대조 후 판정)

---

## 진행 기록 (Claude Code · 2026-07-25)

### S0 — 실측·매칭 시뮬레이션 (5케이스 전 상세 라인)
- **기존 EXTRA와 교집합 0**: 신규 패턴에 걸린 라인 중 기존 라벨 보유 라인 0건 → 기존 패턴 뒤 추가로 우선순위 불변.
- **중입자>항암약물 선순위 실증**: D·E의 `항암중입자방사선치료…`가 ③ 포괄 패턴에도 걸림(복수히트) → ⑤를 앞에 배치.
- **표적 라인은 매트릭스에 이미 포함(수치 검증)**: B detail 표적 7,000만 = matrix 고액(표적) idx8 7,000만 / D 표적 6,000만+중입자 5,000만 = matrix idx8 1.1억, 표적 1,000만 = idx10. → **표적은 EXTRA 패턴 미작성, 표준행 개명만**(이중 계상 0·값 이관).
- **A 일반사망 중복 표기 실증**: 동일 담보 6,000만이 질병사망·상해사망 지급사유 2행 — 매트릭스에도 양쪽 6,000만씩 반영(KB 자체 표기 방식). → dedup 가드 필요 근거.
- **C `상해 통합치료비 (실속형) 깁스치료`**: 분류 전용 행이 광의 패턴 `깁스`에 걸림(234 ⑨ 위반형) → 패턴을 `깁스치료(?:비|특약)` 종결형으로 정밀화해 배제(화상 복합특약 처리와 동일 원칙).

### 구현
- `constants.py`: EXTRA_PATTERNS 말미에 7종 추가(중입자 → `(?<!표적)`항암약물방사선 → 일반사망(`^`접두) → 재해사망 → 순환계 → 응급실 → 깁스). EXTRA_LABEL_GROUP에 깁스→골절만 등록(나머지 6종 의도적 기타 — 주석 명기). KB_COVERAGES `고액(표적)항암치료비`→`표적항암치료` 개명 + `KB_NAME_ALIASES`로 원문 표기 계속 매칭(매트릭스·진단·239 overview 공통).
- `parser.py`: 사망 2종 dedup 가드 — 동일 (계약, 담보명, 금액) 재등장 1회만 계상. **상호배타 가드 방식 = "그 역"**: 매트릭스 상해/질병사망 집계는 무변경(234~243 무접촉), 일반/재해사망은 그룹 미등록(기타)로 사망 대분류에 미가산 → 이중 계상 구조적으로 0.
- `proposal_registry.py` 1건(★스코프 인접 확장·사유): compare가 kb_name 문자열로 before·제안 행을 결합(`compare.known_coverages`)하므로 레지스트리 `고액(표적)항암치료비`를 미개명 시 미매핑 강등(값 유실) — "값 유실 0" 요건상 필수 최소 수정. test_193 기대값 갱신(근거 주석).

### 실 PDF 5건 — 신규 검출 (244 S2 값과 대조 일치)
| 케이스 | 신규 검출 (라벨: 값·그룹) |
| --- | --- |
| A | 일반사망 6,000만(기타·dedup 2행→1회) · 순환계 5,000만(기타) · 항암약물방사선 200만(기타) |
| B | 응급실 4만(기타) · 깁스 80만(골절) · 항암약물방사선 500만(기타) · **표적항암치료 7,000만(암·개명 행)** |
| C | 깁스 10만(골절 — 통합치료비 분류 행 배제 확인) · 항암약물방사선 100만(기타) |
| D | **재해사망 1억(기타)** · 깁스 50만(골절) · 항암약물방사선 2,000만(기타) · **중입자 5,000만(기타)** · 표적항암치료 1.2억(암) |
| E | 항암약물방사선 2,410만(기타) · 순환계 150만(기타) · **중입자 5,000만(기타)** · 응급실 5만(기타) · 깁스 100만(골절) · 표적항암치료 5,000만(암 — overview 별칭 경로 검증) |

### 전/후 비교표 (HEAD 코드 vs 신 코드 — 대분류 합계, 5케이스)
- **사망·암 그룹: 5케이스 완전 불변**(배타 가드·표적 값 이관 검증 — D 암 2.23억 그대로, 라벨만 개명).
- **골절**: 깁스 가산분만 — B 3,250만→3,330만(+80만) · C 110만→120만(+10만) · D 110만→160만(+50만) · E 370만→470만(+100만) · A 불변(깁스 없음).
- **기타**: 신규 6종 가산분만 — A 5,805만→1억7,005만(+1.12억) · B +504만 · C +100만 · D 5,660만→2억2,660만(+1.7억) · E 5,530만→1억3,095만(+7,565만). 산식 대조 전 건 일치.
- 그 외 대분류(후유장해·뇌·심장·수술·입원·운전자·실손·화재·배상책임) **전부 불변**. 월납·부값 5건 불변(A 2,835,744/2,282,564 · B 681,312 · C 183,621 · D 763,089/495,389 · E 4,675,189).

### 관찰 (스펙 밖 — 기록만, Human 판단)
- A `항암치료특약 항암방사선약물치료비` 100만이 동일 표기 2행 → 합산 200만. 실제 별개 담보(보장구간 분리 등)인지 중복 표기인지 원문만으로 판별 불가. 사망 외 dedup은 패킷 범위 밖이라 미적용 — 2단계에서 규칙 결정 권장.
- 레지스트리 `항암방사선약물치료`(193 bundle)와 신규 EXTRA `항암약물방사선` 라벨이 유사하나 상이 — 결합 없음(기존 동작 그대로). 명칭 통일은 2단계 소관.
- A·D의 일반/재해사망 by_company 키 '?'(계약 idx 미해석 페이지) — 기존 extras와 동일한 기존 동작(무변경).

## 검증 체크리스트 (1차 — Code, 2026-07-25)
- [x] backend pytest — **694 passed, 8 skipped**(기준선 688 + 신규 6). 기존 파급은 test_193 2건 갱신(근거 주석)뿐.
- [x] tsc app/node · lint · npm test **79** · build **343.22 kB**(불변)
- [x] 실 PDF 5건: 신규 7종 검출 기록(위 표 — A 일반사망 6,000만·D 재해사망 1억·D·E 중입자 5,000만 등 244 S2 일치), 사망 이중계상 0(사망 그룹 5케이스 불변·HEAD 대조), 월납·부값 전부 불변
- [x] PII grep 0. diff 범위 = constants·parser·proposal_registry(인접 1건·사유 기록)·테스트 2건 + harness만. pipeline/·supabase/ diff 0.
