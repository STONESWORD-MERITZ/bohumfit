# BOHUMFIT-286 — 오현지 3제안서 트랙: 대조 분석 · 파싱 QA·수정 · 목표 디자인 프로토타입

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: **Codex**(2차 검증·커밋) / **Human**(해석 차이 결정)
Date: 2026-08-11 · 기준 HEAD `329a630` · ★야간 무인 세션 · git 쓰기 0

## ★패킷 전제와 실제가 다른 점 (먼저 기록)
| 패킷 | 실제 | 영향 |
|---|---|---|
| "handoff 최신 항목(284 **e644261**)" | ★**284는 미커밋**. HEAD는 `329a630`이고 284 변경분이 워킹트리에 그대로 있다 | Phase B 수정이 284와 **같은 워킹트리**에 쌓인다 → 아래 "Codex 분리 안내" 참조 |
| Phase B 게이트 "backend **916/8**" | 실제 기준선 **932/8**(284 신규 30건 포함) | 게이트는 실제 기준선으로 판정했다 |
| Phase B 게이트 "frontend **393/40**" | 실제 기준선 **402/41** | 동일 |

### ★Codex 분리 안내 (파일 겹침 있음)
`backend/coverage/proposal_parser.py`를 **284와 286이 모두** 건드렸다. hunk는 완전히 분리돼 있다.
- **284 hunk**: `RESPONSE_STRIPPED_METADATA_KEYS` 상수 + `_finalize_proposals` 앞부분 strip 루프
- **286 hunk**: `_TIER_RANGE_RE` 상수 + `_extract_tier_surgery_entries` 본문
겹치는 줄은 없으므로 `git add -p`로 분리 가능하다.

---

# Phase A — 실측·대조 (Phase A는 코드 무변경 상태에서 수행)

## A-1. 원자료 파싱 실측

### 제안서 3건 — ★전부 메리츠화재·동일 양식 (D-7 분기점)
| 파일 금액 | 상품 | 쪽수 | 가계약번호 | 파서 월납 | 원문 `보장보험료 합계` | 수기표 |
|---|---|---:|---|---:|---:|---:|
| 22,157원 | (무) 메리츠 운전자 상해 종합보험2607(2.0) | 15 | 726046107202608111 | **22,150** | 22,157 | **22,150** ✔ |
| 45,765원 | (무) 메리츠 The건강한 5.10.5 보장보험2607(2.0) | 11 | 726046107202608076 | **45,760** | 45,765 | **45,760** ✔ |
| 77,476원 | (무) 메리츠 The좋은 알파Plus보장보험2607(2.0) | 27 | 726046107202608053 | **77,470** | 77,476 | **77,470** ✔ |

★**3건 모두 같은 보험사·같은 양식**이다(가계약번호 앞 9자리 `726046107` 공통, 동일 컨설턴트,
페이지 구조 동일: p1 표지 → p2 계약사항/스크립트 → p3~ 가입담보리스트 → 말미 해약환급금예시표).
→ **D-7 일반화 가능성 높음**(메리츠 양식 한 종으로 3상품을 모두 처리했다).

★★**월납 3건이 수기표와 정확히 일치한다.** 276c의 "제안서만 원 단위 버림" 결정이 설계사 실무와
같다는 강력한 증거다 — 원문 자체가 `보장보험료 합계 22,157원` 옆에
`1회차보험료(할인후) 22,150원`·`2회차이후보험료 22,150원`을 싣는다(메리츠가 실제로 청구하는 값).
수기표 합계 F6 = 185,068 = 0 + 39,688 + 45,760 + 77,470 + 22,150 ✔

### 담보 추출 (수정 전 현행 파서)
| 제안서 | 추출 담보 | 비고 |
|---|---:|---|
| 22,157원(운전자) | 4건 | 원문 가입담보리스트는 35행 이상 |
| 45,765원(건강 5.10.5) | 6건 | 원문 9행 |
| 77,476원(알파Plus) | 6건 | 원문 45행 이상 |

### KB 분석서(`20260805_오현지님_보장분석.pdf`)
★**확인 불가** — 이번 세션에서 [전] 트랙 전수 추출까지는 하지 못했다. 사유: Phase A-3의
3층 대조를 제안서 트랙에 집중했고, [전] 트랙은 수기표 '기존' 시트(55열)와의 대조가
별도 분량이라 한 세션에 담기지 않았다. **다음 세션의 첫 항목으로 남긴다.**

## A-2. 수기표 구조 (디자인 목표물)

### ★두 수기표는 **같은 구조**다 — 확정 목표 스키마
| 항목 | 오현지 260810 | 우상균 -찐 | 판정 |
|---|---|---|---|
| 시트 | 표지·기존·리모델링·최종 | 동일 | ✔ 동일 |
| 담보 행 | **7~48행 = 42행** + 49행 비고 | 동일 | ✔ 동일 |
| 대분류 11개 | 실비·수술·암·뇌·심장·입원·사망·후유장해·골절·배상책임·운전자 | 동일 | ✔ 동일 |
| 유일한 차이 | 리모델링 r24 `중입자 / 정위 방사선` | `중 입 자 치료` | ★표기 흔들림(기존 시트는 양쪽 다 `중 입 자 치료`) — **같은 항목** |
→ ★**고객별 가변이 아니다.** 42행 체계를 목표 스키마로 확정할 수 있다.

### 열 구성 (리모델링 시트)
- B/C = 대분류/담보명, **F = 합계**(`=SUM(H:AE)`), H~ = 회사별 열(2열씩 병합)
- r2 회사명, r3 납기만기/보험기간, r4 상품명, r6 월납(`₩ #,##0"원"`)
- ★**종수술 5행만 2열 병기**(질병 | 상해) — r11~15의 N/O열. 다른 행은 단일 열
- r49 비고: 자유 텍스트(예: `대장용종 100만원`, `10대 골절 진단비/수술비 100만원`)

### 서식
글꼴 맑은 고딕(비고행만 돋움체) · 대분류 22~24pt bold · 값 22pt bold ·
채움 대분류 `FFDBEEF3`, 합계열 `FFEBF1DE`, 종수술 값 `FFFFFFCC` ·
숫자형식 값 `#,##0_ "만""원"`, 월납 `"₩"\ #,##0"원"` · 행 높이 40.15(데이터), 열 너비 B 17.8/C 17.2/F 20.8

## A-3. 3층 대조 (기준 = 수기표 정답 / 피검체 = 현행 코드 신규 산출물)

★대조 기준물은 `BohumFit_보장분석_오현지_20260806.xlsx`가 **아니라** 현행 코드로 새로 만든
프로토타입(Phase C)이다. 패킷 지시대로 276a/b/c 수정 전 산출물은 쓰지 않았다.

### 셀 단위 전수 — 값이 있는 25행 중 **13 일치 / 12 차이**
| 행 | 담보 | 수기표(메리츠 3열) | 현행 | 판정 | 원문 근거 |
|---:|---|---|---|---|---|
| 9 | 상해수술비 | 100만 | 100만 | ✔ | p3 `102 상해수술비 1백만원` |
| **10** | 질병수술비 | **40만** | 20만 | ★불일치 | p3 `261 질병수술비 20만원` + `262 질병수술비(특정5대질병 제외) 20만원` |
| 11~15 | 1~5종 수술비 | 질병 20/30/50/100/400 · 상해 20/30/100/500/1000 | **동일** | ✔✔ | p10~11 — ★**Phase B-1 수정으로 일치**(수정 전 전부 5종·1,000만) |
| **16** | 뇌혈관 수술비 | **1,000만** | 300만 | ★불일치 | 파서=p3 `271 131대질병수술비(뇌혈관질환) 3백만원`(원문 정확). 수기표 1,000만은 p14 `176 혈전용해치료비Ⅱ(뇌졸중) 1천만원` 계열로 **추정** |
| **17** | 심장질환 수술비 | **1,000만** | 300만 | ★불일치 | 위와 동형(p3 `270 …(심장질환) 3백만원` / p14 `177 혈전용해치료비Ⅱ(특정심장질환) 1천만원`) |
| 18 | 암진단비(일반암) | 2,000만 | 2,000만 | ✔ | p3 `129 암진단및치료비Ⅱ[암진단비(유사암제외)] 2천만원` |
| 19 | 유사암진단비 | 400만 | 400만 | ✔ | p3 `139 유사암진단비 4백만원` |
| **20** | 암수술/로봇암수술 | 2,500만 | — | ★누락 | p12 `117 암 통합치료비(실속형) 3천만원` 등 — **금액이 안 맞아 단순 매칭으로 해결 안 됨** |
| **21** | 항암방사선약물치료 | 1,000만 | — | ★누락 | p13 `126 항암방사선약물치료비 10만원` — **수기표와 금액 100배 차** |
| **22** | 고액항암치료(표적,면역) | 9,500만 | — | ★누락 | 45,765건 p3 `131`(담보명이 줄바꿈으로 분리) `1천만원` + 알파Plus `119 1억원` |
| **23** | 세기조절/양성자 방사선 | 5,000만 | — | ★누락 | 대응 담보 특정 못 함 |
| **24** | 중입자/정위 방사선 | 5,000만 | — | ★누락 | p13 `124 항암중입자방사선치료비 5천만원` — ★금액 일치, 매칭 규칙만 없음 |
| 25 | 뇌혈관질환 | 1,000만 | 1,000만 | ✔ | p3 `187 뇌혈관질환진단비Ⅱ 1천만원` |
| 29 | 허혈성심장질환 | 1,000만 | 1,000만 | ✔ | p3 `190 허혈성심장질환진단비Ⅱ 1천만원` |
| **31** | 상해입원 | 2만 | — | ★누락 | p3 `93 일반상해입원일당(1일이상) 2만원` — ★금액 일치, 매칭 규칙만 없음 |
| **36** | 상해사망 | (빈칸) | 200만 | ★초과 | ★**유령 아님** — p3 `1 일반상해사망(건강가입)[기본계약] 1백만원`·`1 갱신형 일반상해사망[기본계약] 1백만원` 실재. 수기표가 **기본계약 사망을 뺀 것** |
| **41** | 골절진단비 | **50만** | 100만 | ★불일치 | 파서=`167 10대골절진단비 1백만원` 채택. 수기표 50만 = `158 골절진단비Ⅱ 30만` + `159 골절(치아파절제외)진단비Ⅱ 20만` |
| **42** | 골절수술비 | 10만 | — | ★누락 | p3 `160 골절수술비Ⅱ 20만원`(★수기표 10만과도 불일치) |
| 43 | 깁스치료비 | 50만 | 50만 | ✔ | p3 `175 깁스치료비 50만원` |
| 44 | 일상생활배상책임 | 1억 | 1억 | ✔ | p3 `520 갱신형 가족일상생활중배상책임(Ⅳ) 1억원` |
| 48 | 자부상 | 30만 | 30만 | ✔ | p3 `34~48 운전자용 자동차사고부상치료비(1-14급)` — 14급 기준 |

### 층위별 분류
- **1층(버그)**: r11~15 tier 오검출 → ★**Phase B-1에서 수정 완료**.
  r10·r41은 **합산 정책(REP/SUM)** 문제라 Phase B 금지 영역 — 아래 참조.
- **2층(스키마 갭)**: ★r11~15 **1~5종 수술비 5행이 현행 40행에 자리가 없다**.
  `GROUP12`에 `종수술` 그룹은 있으나 `KB_COVERAGES`에 **해당 행이 0개**다.
  즉 값을 정확히 읽어도 표에 실을 자리가 없다. 그 밖에 r7·r8(실비 2행)·r26·r27·r33·r34 등도
  현행 40행과 이름·분해 단위가 다르다 → Phase D 매핑표 참조.
- **3층(파싱 커버리지)**: 누락 7건(r20~24, r31, r42). 이 중 **r24(5,000만)·r31(2만)은
  원문 금액이 수기표와 정확히 일치**해 매칭 규칙만 추가하면 되는 후보다.
  r20~23은 원문 담보와 수기표 금액이 맞지 않아 **설계사 해석이 개입**돼 있다.

### ★F-5 재현 — **성공** (283이 "확인 불가"로 남긴 항목)
283 F-5는 "`_merge_entries` 큰 값 채택이 실측값끼리도 발동하는가"였다. **발동한다.**
알파Plus 제안서에서 `N종수술비(질병 5종)` 한 이름에 10개 라인의 금액
(20·30·100·500·1,000·20·30·50·100·400만)이 모여 **최댓값 1,000만원**이 채택됐다.
★기전은 `_entry(...)`의 `merge_rule` 기본값이 **`AGG_REP`**여서다 — 4번째 인자로 `AGG_SUM`을
넘겨도 그것은 `agg` 필드이고, `_merge_entries`가 보는 `merge_rule`은 별개 인자다.
★★이 REP 기본값은 **의도된 방어**이기도 하다. 같은 담보가 가입담보리스트와 상세설명 페이지에
중복 등장하므로, SUM이면 이중 계상된다. 따라서 **정책 변경 없이는 r10·r41을 고칠 수 없다.**

---

# Phase B — 명백한 결함 수정

## ★B-1 (실시) — 1~5종 수술비 tier 오검출
- **파일**: `backend/coverage/proposal_parser.py` (`_TIER_RANGE_RE` 신설 + `_extract_tier_surgery_entries`)
- **원문 근거**: 알파Plus p10~11. 상해 20/30/100/500/1,000만 · 질병 20/30/50/100/400만
- **수정 전 재현**: 10개 라인이 **전부 `5종`으로 붕괴**했다. 상세면 서술이
  `1-5종 수술분류표Ⅱ`로 시작해 `([1-5])\s*종\s*수술`가 **범위 표기의 5**를 먼저 잡았기 때문이다.
  게다가 종별을 못 가리면 상해·질병 **양쪽에 같은 값**을 넣었고, REP(큰 값)라 두 행 모두 1,000만원이 됐다.
  → ★설계사가 고객에게 안내하는 표에 **질병 5종이 400만원 대신 1,000만원**으로 나갔다.
- **수정 내용**: ①범위 표기(`1-5종`·`1~5종`)를 tier 탐색에서 제거 ②종별이 한쪽만 적힌 줄은
  그쪽으로만 기입 ③가입담보리스트 대괄호 표기(`[상해3종]`)도 tier로 인정
- **수정 후**: 10종별이 각자 금액을 지킨다 — ★**수기표 r11~15와 완전 일치**
- **테스트**: `backend/tests/test_proposal_tier_surgery_286.py` 5건(원문 문구 그대로)

## B-2~B-4 (미실시) — 사유
| 후보 | 왜 안 했나 |
|---|---|
| r10 질병수술비 20만→40만 | `261`+`262` 합산이 필요한데, 두 행이 **같은 이름·같은 금액**이라 REP에 뭉개진다. 고치려면 `merge_rule` 정책을 SUM으로 바꿔야 하고 그러면 **중복 페이지가 이중 계상**된다. ★패킷이 `_merge_entries` 동작 변경을 금지했고, 실제로도 섹션 구간 구분(276b `_is_example_window` 방식)이 선행돼야 한다 |
| r41 골절진단비 100만→50만 | 동형. `158`+`159` 합산 대 `167` 채택의 **정책 결정**이다 |
| r20~r23 암 계열 누락 | 원문 담보 금액과 수기표 금액이 **맞지 않는다**(예: 원문 항암방사선약물 10만 vs 수기표 1,000만). 패킷의 허용 조건 "담보명 매칭 추가로 해결되는 누락"에 **해당하지 않는다** — 설계사 해석이 개입돼 있어 Human 결정이 먼저다 |
| r24 중입자 5,000만 · r31 상해입원 2만 | ★**금액이 정확히 일치**해 매칭 추가만으로 해결 가능한 유이한 후보다. 다만 두 담보 모두 **현행 40행에 대응 행이 없어**(2층) 지금 매칭을 추가하면 산출물에 실리지 않는다. 층위2 본구현과 함께 넣는 것이 맞다 |
| r16·r17 뇌혈관·심장 수술비 | 파서가 **원문을 정확히 읽었다**(3백만원). 수기표 1,000만은 다른 담보를 쓴 것으로 보이며 이는 **해석 차이**다 → Human |
| r36 상해사망 | 파서가 **원문을 정확히 읽었다**. 수기표가 기본계약 사망을 뺀 것 → Human |

## 게이트 (1차 · 2026-08-11 · Windows 로컬)
- [x] backend `pytest -q` **937 passed, 8 skipped** — 기준선 932 + **신규 5**, 회귀 0
- [x] ★`npm run smoke:coverage` **정본 2건 기준값 완전 일치**(월납 681,312 · 4,675,189 불변)
- [x] `npx tsc -p tsconfig.app.json --noEmit` — PASS · `npm run lint` 무경고
- [x] `npm test` **402 passed / 41 files** — 회귀 0(프런트 변경 없음)
- [x] `npm run build:verify` — 343,702 B **예상 FAIL**(248 Application Control)
- [x] ★보호 영역 **diff 0**: `backend/pipeline/` · `backend/filters.py` · `vite.config.ts`
- [x] ★불변 결정 무충돌: 상해사망 합산값 유지 · 243(80%이상) 집계 제외 유지 ·
      월납 3건 수기표 일치 · 276a 폴백 0 · `registry_hints` 응답 노출 0

---

# Phase C — 목표 디자인 프로토타입

- **생성물**: `보장분석\비교분석표\BOHUMFIT_프로토타입_오현지_260811.xlsx` (★gitignore 폴더)
- **생성기**: `scripts/prototype_286_ohj.py` (★제품 export 경로 **무접촉** — 독립 스크립트)
- **내용**: 수기표 42행 체계·2열 병기·서식(색/숫자형식/테두리)을 재현. 회사 열 3개(메리츠 3건).
- ★**빈칸은 빈칸으로 뒀다** — 수기표 값을 베껴 채우지 않았다. **18/42행만 값이 있고**
  나머지는 비어 있는 것이 현재 파싱 실력의 정직한 모습이다.
- 색 규약: 연분홍 담보명 = 현행 40행에 자리가 없는 행(2층 갭) / 연노랑 값 = 파서가 실제로 읽은 값
- `차이 사유` 시트에 불변 결정과 빈칸 원칙을 적었다.

---

# Phase D
별도 산출: `.agent-harness/tasks/BOHUMFIT-286-schema-design.md`

---

# ★확인 불가
1. **[전] 트랙 전수 대조** — `20260805_오현지님_보장분석.pdf` → 수기표 '기존' 시트(55열) 대조를
   못 했다. 제안서 트랙 3층 대조에 세션을 썼다. **다음 세션 최우선.**
2. **r16·r17·r20~r23의 수기표 근거 담보 특정** — 원문에 후보는 있으나 어느 것을 그 행에 넣었는지는
   설계사만 안다. 추측하지 않고 Human 질문으로 남겼다.
3. **비밀번호 걸린 PDF** — 해당 없음(3건 모두 열렸다).
4. **우상균 6계약 결과 불변 확인** — 우상균은 KB 보장분석서 경로라 `proposal_parser` 변경의
   영향을 받지 않는다(구조적 판정). 실제 6계약 산출물 재생성 대조는 하지 않았다.

# Stage 목록 (Codex용)
★**284와 분리 필요**(위 "Codex 분리 안내"). 286 몫:
`backend/coverage/proposal_parser.py`의 286 hunk · `backend/tests/test_proposal_tier_surgery_286.py` ·
`scripts/prototype_286_ohj.py` · `tasks/BOHUMFIT-286-ohj-proposal-track.md` ·
`tasks/BOHUMFIT-286-schema-design.md` · `handoff.md` · `locks.md`
※제외: `보장분석/` 하위 전부(실 PDF·수기 엑셀·프로토타입 산출물)

# 커밋 메시지 (Codex용)
fix(BOHUMFIT-286): 가입제안서 1~5종 수술비 tier 오검출 수정 + 오현지 3제안서 대조 기록
