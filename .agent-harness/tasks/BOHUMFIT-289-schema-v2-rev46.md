# BOHUMFIT-289 — S1b: V2 스키마 46행 개정 + 지급 케스케이드·분배 상수

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: **Codex**(2차 검증·커밋) / **Human**(패킷 대비 미확인 2건)
Risk tier: 중 — 상수 개정만. ★제품 동작 무변경이 성립 조건. git 쓰기 금지(커밋 Codex).
Date: 2026-08-13 · 기준 HEAD `559e972`(287 `13e9b0a` 반영) · 선행 BOHUMFIT-287

## S1b의 성립 조건 (287 승계)
"46행이 됐다"가 아니라 ★**"제품 동작이 하나도 안 바뀌었다"**가 합격선이다.
케스케이드·분배 상수도 **정의만** 하고 어떤 계산 경로에도 들어가지 않는다.

---

## Step 1 — 신판 실측

### 1-1. 시트 구성 — ★시트명이 바뀌었다
| 구판(찐·260810) | **신판(민규판)** |
|---|---|
| 표지 / **기존** / **리모델링** / 최종 | 표지 / **컨설팅 전** / **컨설팅 후** / 최종 |
담보 행 = **r7~r52 = 46행**(구판 r7~r48 = 42행), 비고 r53. `컨설팅 전`·`컨설팅 후` **완전 일치**.
→ `SHEET_NAME_BEFORE_V2`·`SHEET_NAME_AFTER_V2`·`SHEET_NAME_FINAL_V2`로 기록(S3가 쓴다).

### 1-2. 42 → 46: 암 대분류만 7행 → **11행** (다른 대분류는 무변동)
| 구 42행 | 신 46행 |
|---|---|
| 암 수 술 / 로 봇 암 수 술 | **암 수 술 (레보아이 포함)** + **다빈치 로봇 수술** |
| 항 암 방 사 선 약 물 치 료 + 고액항암치료(표적,면역) | **항암 약물 치료** + **표적 약물 치료** + **면역 약물 치료** + **방사선 치료** |
| 세기조절 / 양성자 방사선 + 중입자 / 정위 방사선 | **세기조절 방사선 치료** + **양성자 방사선 치료** + **중 입 자 치료** |
★**Q3 폐기 확인**: 신판은 구형 표기 **`중 입 자 치료`** 를 쓴다. 287이 채택했던
`중입자 / 정위 방사선`은 이제 **별칭으로 내려간다**(표기 결정이 뒤집혔다).
★대분류는 **11개 그대로**다 — 늘어난 4행이 전부 `암` 안에서 생겼다.

### 1-3. ★"L 접두 표기" — `최종` 시트에서 발견
`컨설팅 전`·`컨설팅 후`에는 없고 **`최종` 시트**에만 있다:
`L 뇌 졸 중`(r30) · `L 뇌 출 혈`(r31) · `L 급 성 심 근 경 색`(r34).
→ ★**케스케이드 하위 행 표시**다. 붙지 않는 행이 정확히 체인 정의와 맞물린다:
| 행 | L 접두 | 케스케이드상 위치 |
|---|---|---|
| 뇌 혈 관 질 환 | 없음 | **루트** |
| 뇌 졸 중 · 뇌 출 혈 | **있음** | 하위 |
| 심 장 질 환 | 없음 | ★**독립**(체인 밖 — Human 확정과 일치) |
| 허혈성 심장질환 | 없음 | **루트** |
| 급 성 심 근 경 색 | **있음** | 하위 |
| 암 계열 11행 | 없음 | 체인은 있으나 **표기는 아직 미적용** |
→ `CASCADE_CHILD_PREFIX_V2 = "L "` 로 기록. ★암 계열에 L을 붙일지는 S3 표시 결정.

### 1-4. `최종` 시트의 표기 흔들림 (별칭 후보 — S3/S4 입력물)
| 최종 시트 | 컨설팅 전/후(**채택**) |
|---|---|
| 질 병 / 상 해 입 원 · 질 병 / 상 해 통 원 약 제 | 상 해/질 병 입 원 · 상 해/질 병 통 원 약 제 |
| 뇌 혈 관 수 술 비 · 심 혈 관 수 술 비 | 뇌혈관 수술비 · 심장질환 수술비 |
| 상해**+**질병 후 유 장 해 80% | 상해 질병 후 유 장 해 80% |
| 깁 스 치 료 비 | 깁스치료비 |
★목표물은 `컨설팅 전/후` 시트이므로 그쪽을 채택했다. 위 표기는 S4 별칭 후보로만 남긴다.

---

## Step 2~4 — 상수 개정·신설 (`constants.py` V2 블록 안에서만)

| 이름 | 변화 |
|---|---|
| `KB_COVERAGES_V2` | 42 → **46행**(암 11행 재설계) |
| `GROUP12_V2` | **무변동**(11) |
| `APPENDIX_ITEMS_V2` | 4 → **6항목**(`장기요양간병비`·`경증치매진단` 확정 편입) |
| `DISTRIBUTED_ITEMS_V2` | ★신설 — `암 주요치료비`(부록 아님, 분배로 해소) |
| `PENDING_DISPOSITION_V2` | ★**빈 튜플**(287의 보류 3건 전부 해소 — 미결 0) |
| `LEGACY_DISTRIBUTED_V2` | 신설 처분 구분 |
| `NEW_ROWS_V2` | 13 → **16행**(암 분리분 반영) |
| `SHEET_NAME_*_V2` | 신설 — `컨설팅 전`/`컨설팅 후`/`최종` |
| `CASCADE_CHILD_PREFIX_V2` | 신설 — `"L "` |
| `PAYOUT_CASCADE_V2` | ★신설 — 14개 체인 |
| `CASCADE_INDEPENDENT_V2` | ★신설 — `cardiac_disease`·`cancer_general`·`cancer_minor` |
| `DISTRIBUTION_RULES_V2` | ★신설 — 2종 |
| `DISTRIBUTION_UNRESOLVED_V2` | ★신설 — 미확인 1건 주차 |

### row_id 변경 (287 → 289)
`cancer_chemo_radio`·`cancer_high_cost`·`radio_imrt_proton`·`radio_carbon_srs` **폐기** →
`cancer_surgery_davinci`·`cancer_drug`·`cancer_drug_targeted`·`cancer_drug_immune`·
`cancer_radiation`·`radio_imrt`·`radio_proton`·`radio_carbon` **신설**.
★287 V2는 **어디에도 배선되지 않은 상태**라 id 변경의 파급이 0이다(무배선의 이득).

### 케스케이드 (Human 확정 표와 1:1)
```
뇌     뇌혈관 → [뇌혈관]                  뇌졸중 → [뇌혈관, 뇌졸중]
       뇌출혈 → [뇌혈관, 뇌졸중, 뇌출혈]
심장   허혈성 → [허혈성]                  급성심근경색 → [허혈성, 급성]
       ★심장질환 = 체인 없음(독립)
암수술 암수술 → [암수술]                  다빈치 → [암수술, 다빈치]
약물   항암약물 → [항암약물]              표적 → [항암약물, 표적]
       면역 → [항암약물, 표적, 면역]
방사선 방사선 → [방사선]                  세기조절 → [방사선, 세기조절]
       양성자 → [방사선, 양성자]          중입자 → [방사선, 중입자]   ★형제 비누적
독립   일반암 · 유사암 = 체인 없음
```
★무순환 증명 방식: 체인이 **접두 폐쇄**임을 단언한다 — 체인에 들어간 상위 행의 체인이
자기 체인의 부분집합이어야 한다. 성립하면 상위→하위 방향이 일관돼 순환이 생길 수 없다.

### 분배 규칙
| rule_id | source | targets | mode |
|---|---|---|---|
| `major_treatment` | `암 주요치료비` | 암수술 · 항암약물 · 방사선 | EQUAL(동액) |
| `integrated_treatment_surgery` | 통합치료비 약관 내역 — 수술 항목 | 뇌혈관 수술비 · 심장질환 수술비 | EQUAL |
★**합산 규칙 미정의**: 분배분과 개별 특약이 같은 행에 공존할 때의 처리는 기존 sum/rep
의미론과 함께 **S4에서** 정한다 — 패킷이 명시적으로 금지했고, 이 태스크는 데이터만 놓는다.

---

## ★패킷 대비 미확인 2건 (추측하지 않고 기록)

| # | 패킷 명세 | 실측 | 조치 |
|---|---|---|---|
| ① | Q8형 "본체 → **r30(순환계 치료비)**" | ★신판 46행에 **`순환계 치료비` 행이 없다**. 전 시트 `순환계`·`통합치료` 문자열 **0건**, r30은 `뇌 졸 중`(컨설팅 전/후)·`L 뇌 졸 중`(최종) | 규칙을 만들지 않고 `DISTRIBUTION_UNRESOLVED_V2`에 주차. **본체 착지 행을 Human이 지정해야 S4가 배선할 수 있다** |
| ② | "L 접두 **위치**" | `컨설팅 전/후`에는 **0건**, `최종` 시트에만 3건 | 위 1-3에 위치를 표로 기록. ★암 계열 체인에는 아직 L이 없어, **표시 적용 범위**는 S3 결정 사항으로 남는다 |

★①은 "미결 0" 선언과 충돌하는 것이 아니라, **패킷이 가리킨 행이 파일에 없어서** 생긴
실측-명세 간극이다. 임의로 `뇌 졸 중`에 넣으면 순환계 통합치료비 5,000만원이 뇌졸중 진단비로
둔갑한다 — 지어내지 않는 것이 옳다.

---

## Step 5~6 — 검증 (1차 · 2026-08-13 · Windows 로컬)

### ★★제품 동작 무변경 — 산출물 바이트 증명 (287 방식 재실행)
HEAD의 `constants.py`를 넣은 backend 사본 vs 현재 backend로 **같은 입력 6종**을 돌려
결과 JSON sha256을 비교했다 — **완전 일치**.
`74d7fce03b97d050c0a6bd860f79040383fc471f65efce2fc869b1125429aeae`
★**287에서 측정한 해시와도 동일**하다(287·289 두 번의 개정이 산출물을 전혀 건드리지 않았다).

### 게이트
- [x] backend `pytest -q` **974 passed, 8 skipped** — 기준선 955 + **신규 19**(289 21건 신설 −
      287에서 289로 이관·삭제 2건), 회귀 0
- [x] ★★`npm run smoke:coverage` — **정본 2건 기준값 완전 불변**(604,560,000/681,312 · 1,542,990,000/4,675,189)
- [x] `npm test` **402 passed / 41 files** — 회귀 0(`src/` 무변경)
- [x] `npx tsc -p tsconfig.app.json --noEmit` / `tsconfig.node.json` — PASS · `npm run lint` 무경고
- [x] `npm run build:verify` — 343,702 B **예상 FAIL**(248 Application Control)
- [x] ★보호 영역 **diff 0**: `aggregator.py`·`export_excel.py`·`export_pdf.py`·
      **`proposal_parser.py`**·`pipeline/`·`filters.py`·`src/`·`vite.config.ts`
- [x] ★구 상수 sha256 3종 **불변**(287 스냅샷과 동일) — `KB_COVERAGES`·`GROUP12`·`KB_NAME_ALIASES`
- [x] ★**무배선 증명 확장** — 4개 파일에 `PAYOUT_CASCADE_V2`·`DISTRIBUTION_RULES_V2`·
      `KB_COVERAGES_V2` 참조 **0건**(케스케이드·분배까지 포함해 코드로 고정)
- [x] ★신판 원본 대조 — 46행 행명이 `컨설팅 전`·`컨설팅 후` 두 시트와 **문자열 단위 일치**

### 테스트
- **신설 `test_schema_v2_rev46_289.py` 21건**: 46행 골격 6 · 케스케이드 7 · 분배 4 ·
  무배선 1 · 신판 원본 대조 2(파일 부재 시 skip) · Q3 폐기 1
- **`test_schema_v2_287.py` 갱신**: 42→46 반영, 행명 전문·수기표 대조를 289로 **이관**하고
  이 파일은 **구 40행 병존 증명**과 행 성격 계약만 남겼다(중복 제거).
  ★기대값 갱신 6건은 전부 **289 변경의 직접 귀결**이다(부록 4→6 · 보류 해소 · 암 병합 해체 ·
  Q3 뒤집힘 · 신설 13→16 · 처분 구분 추가). **완화 0** — 오히려 `PENDING`이 비었음을
  단언으로 고정해 강화됐다.

## Stage 목록 (Codex용)
`backend/coverage/constants.py`(V2 블록 개정분) · `backend/tests/test_schema_v2_rev46_289.py` ·
`backend/tests/test_schema_v2_287.py` · `tasks/BOHUMFIT-289-schema-v2-rev46.md` ·
`handoff.md` · `locks.md` · 기준선 3문서(backend 955→**974**)
※제외: 실 PDF·PII·수기 엑셀·`보장분석/` 하위 전부

## 커밋 메시지 (Codex용)
feat(BOHUMFIT-289): V2 스키마 46행 개정 + 케스케이드·분배 상수(병존·무배선 유지)
