# BOHUMFIT-246 — 신 체계 2단계: 집계·스키마 전면 전환 (비분양식 정본화)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: Codex (반려 보정 2차 재검증 통과·커밋/배포 스모크)
Risk tier: ★고위험 — 풀 하네스. git 쓰기 금지(커밋 Codex). 실 PDF·엑셀 로컬 참조만·stage 금지. 이상 신호 시 즉시 중단.
Date: 2026-07-25

## 제1원칙 (Human 2026-07-25 확정)
★정확도 — 담보·금액 누락 0. 전환 전후 총액 대사(reconciliation)로 증명한다:
구체계 전 담보 합계 = 신체계 전 항목 합계 + 기타 합계 (케이스 5건 각각, 차이 0 필수).

## 확정 결정 반영
- 신 항목·순서 = 비분양식 시트2 10~44행 그대로(양식이 곧 스키마 — S3 실측 확정)
- 80%이상후유장해 제외(기타 보존) 유지 / 표적항암 단일화(245 개명 방식 유지) /
  신담보 3항목(면역항암·암 주요치료비·심혈관질환) 행 존치([후] 전용, [전]은 원문 없으면 0)
- 뇌·심장 초기/중기/말기 = 양식 수식 규칙 이식(초⊃중⊃말 포섭 + 부위수술·종수술5종·
  질병수술 가산 — S3 실측 수식이 정본, Human 매핑과 정합 확인됨). 수식 원문 주석 병기.
- [후] = 이월 모델: 유지 계약 값 그대로 + 해지 체크 제외 + 신규 제안 추가(신규 트랙
  미구현 → 신규 0 + 해지 0이면 ★전=후 완전 동일). 기존 유지/해지 로직(180 계열) 접점
  실측 후 통합.
- 계약 열 = 전부 전개(누락 0 — 상위N+기타 합산 금지)
- 차액 = 후−전(개선 +), K7류 이중합산 버그는 신 구현에서 I열 규칙으로 통일
- 238 종수술 환산의 estimated("표준 환산 기준") 필드 유실 금지 — 일반종수술 1~5종에 매핑
- ★245의 일반/재해사망 기타 귀속을 사망 그룹 정식 행으로 승격하되, 상해/질병사망과의
  상호배타 재정의 필수(동일 담보 이중 계상 0 — 총액 대사로 증명)

## 수정 범위
backend/coverage/(constants GROUP 체계·aggregator·schema·compare·consulting 필요분),
src/pages/CoverageRemodel.tsx(신 스키마 소비 정합 — 화면이 깨지지 않는 최소 반영),
관련 테스트. ※엑셀·PDF 양식 재현은 다음 단계(247~) — 이번엔 기존 export가 신 스키마로
동작만 하면 됨(양식 미적용 상태 허용, 파손 금지)

## 수정 금지
- backend/pipeline/ 무접촉. 파서 검출 로직(245까지) 재변경 금지 — 귀속·집계만 전환
- 실 PDF·PII stage 금지(픽스처 익명 합성)

## 단계별 지시
1. S0: 현행 스키마 소비처 전수 실측(화면·export·compare·consulting·테스트) — 전파 지도 작성.
   기존 유지/해지([후] 이월) 로직 위치 실측.
2. 신 GROUP 체계·항목 순서 정의(양식 10~44행) + 구→신 귀속 매핑(244 S1) + 기타 보존.
3. 뇌·심장 단계 파생 계산(양식 수식 이식 — 수식 원문을 주석으로 병기).
4. [후] 이월 모델 구현(유지=이월·해지=제외·신규=자리만). 전=후 동일성 테스트
   (해지 0·신규 0 → 전 항목 일치) 필수.
5. 화면 최소 정합 — 신 순서·신 라벨로 렌더, 기능(병기·배지·계피/납만기·수동담보) 회귀 0.
6. ★총액 대사 테스트: 케이스 5건 각각 구체계 총합 = 신체계 총합 + 기타(차이 0).
   실 PDF 5건 재실행 표(항목별 신 집계값)를 태스크 문서에 기록.
7. 245 이관 판정: A 케이스 항암약물방사선 동일 표기 100만×2행 — detail 페이지 원문
   (계약 귀속·지급사유)을 대조해 별개 담보/중복 표기 판정. 별개면 현행 합산 유지,
   중복이면 dedup 가드를 해당 패턴으로 확장. 판별 불가 시 현행 유지 + Human 확인
   항목으로 기록(증권 실물 대조 필요).

## 검증 체크리스트 (1차)
- [ ] backend pytest — 694/8 기준 + 신규분(갱신 테스트는 근거 주석 — 대분류 교체 파급은
      정당하나 값 변경은 대사로 증명)
- [ ] tsc·lint·npm test·build 통과(청크 ±10%)
- [ ] ★총액 대사 5건 차이 0 / ★전=후 동일성(해지·신규 0) 통과 / 월납·병기 부값 불변
- [ ] 238 estimated 필드 생존 확인
- [ ] PII grep 0, diff 범위 선언 파일만, pipeline/ diff 0

## Stage 목록 (Codex용)
수정 파일 전부, tasks/BOHUMFIT-246-*.md, handoff.md, locks.md — 실 PDF·엑셀 원본 제외

## 커밋 메시지 (Codex용)
feat(BOHUMFIT-246): 담보 체계 비분양식 정본화 — 집계·스키마 전환 + 뇌심 단계 파생 + 후 이월(overview 포함)

## Next (handoff 명시)
① Codex — 2차 검증(총액 대사 재현·실 PDF 5건·배포 스모크) → 커밋·push
② Human — 재업로드로 신 체계 화면 확인(항목 순서·누락 여부 육안)
③ Chat — 247(화면 완성)·248(엑셀 양식 재현)·249(PDF) 순차 발번

---

## 진행 기록 (Claude Code · 2026-07-25)

### S0 — 스키마 소비처 전파 지도 (실측)
- **그룹 소비 = 전부 상수 구동**: backend `GROUP13`(compare `_group_key`·export_excel/pdf `_grp_key`·aggregator rollup) + 프런트 `GROUP_ORDER`(`src/lib/coverageAfterDisplayCache.ts:169` 정의, CoverageRemodel 그룹핑·수동담보 select) — 그룹명 하드코딩은 이 두 상수뿐(리터럴 grep 0). → **상수 교체 + `group12` 재귀속이 곧 전파**.
- **이름 결합점**: ① diagnosis join(`build_final`·consulting `_recalculate_diagnosis`) — 파서가 같은 별칭 테이블로 키를 만들므로 정식명 개명 시 자동 일관 ② compare `known_coverages`(kb_name 문자열) ↔ proposal_registry 하드코딩 이름 — **registry 동반 개명 필수**(245 선례).
- **[후] 이월 로직 위치**: `consulting.apply_consulting_plan`(BOHUMFIT-190) — keep/cancel 정규화 → 유지 계약 키로 `by_company` 필터 → `aggregate_coverage_values` 재집계 → build_final 재진단. **패킷의 이월 모델과 정확히 동일 구조** → 신규 구현 대신 이 경로를 정본으로 유지하고 파생치(단계·Y/N) 재계산만 추가.
- 종수술 라벨(238): 환산 = `종수술비({t}종·표준환산)`+`estimated` / 원문 종별 분리형 = `종수술비` 단일 버킷(종별 미분리 — 파서 무변경 제약상 1~5종 분해 불가, 기타 아닌 종수술 그룹 귀속으로 보존).

### 구현 (귀속·집계만 — 파서 검출 로직 무변경)

- **`constants.py`**: `GROUP12/13` → 비분양식 대분류 교체(사망·후유장해·암·뇌·심장·종수술·수술·의료이용·골절·가입특약(Y/N)·기타 — 변수명은 소비처 import 안정성 위해 유지). `KB_COVERAGES` 40행(37+신담보 3: 면역항암치료·암 주요치료비·심혈관질환 — [후] 전용 자리, [전] 미가입 행)으로 재정의: 정식명=양식 담보명(암진단금·암수술·뇌혈관수술·심혈관수술·급성심근경색·상해/질병수술·질병/상해입원·상해/질병후유장해 등 12건 개명), KB 원문 표기는 `KB_NAME_ALIASES`로 계속 매칭(매트릭스·진단·overview 공통 — 길이 우선 매칭 순위도 보존). Y/N 원천 9행(운전자 4·배상 1·실손 4)은 "가입특약(Y/N)" 그룹으로 금액 보존, 신 양식 비항목 6행(고액암·3대비급여·간병인 2·보철·화재벌금)은 기타 보존. `NEW_ITEM_ORDER`(시트2 10~49행 순서)·`YN_ITEMS`(COUNTA 수식 등가)·`STAGE_COMPONENTS`/`STAGE_COMMON_ADD`(시트3 수식 — **원문 수식 7행을 주석으로 병기**, H10 심장중기 정정·K7 이중합산 미이식) 신설.
- **`aggregator.py`**: `_apply_exclusions` — ① 일반/재해사망: parser 기록 `class_amounts`(지급사유 행 금액)를 상해/질병사망 매트릭스 셀에서 계약별 차감 + 근거 있는 계약만 사망 그룹 승격, 근거 없으면 `(계약 미확인)` 라벨로 기타 보존(★이중 계상 0 > 승격) ② 중입자: `target_included`를 표적항암치료 셀에서 차감. 차감 실적 `death_dedup` 반환(총액 대사 항). `compute_yn_flags`·`compute_stage_totals` + 표시 순서 정렬(그룹·시트2 항목순). `estimated` 필드 보존.
- **`parser.py`**(주석 데이터만 — 검출·계상 결과 245와 동일): 사망 라인의 지급사유별 금액(`class_amounts`)·중입자의 고액항암 분류 포함분(`target_included`) 기록. 배타 차감의 실측 근거 확보용.
- **`consulting.py`**: [후] 파생치 재계산(yn_flags·stage_totals — 동일 수식·I열 규칙). ★결함 보정 1건: 계약 미상 키('?')를 유실하던 190 필터를 "계약 귀속 확인분만 keep/cancel, 미상 키는 이월"로 수정 — 해지 0인데 전≠후가 되는 기존 결함(전=후 완전 동일 요건 충족에 필수).
- **`jong_surgery.py`**: 환산 라벨 `일반종수술 {t}종(표준환산)` 개명(238 표준환산 표기·estimated 유지). 원문 종별 분리형("종수술비" 버킷)은 1~5종 분해 불가(파서 무변경 제약) — 종수술 그룹 보존, 단계 수식의 5종 가산에는 미참여(한계 기록).
- **`proposal_parser.py`**: `_entry`에서 텍스트 룰 구명칭을 `coverage_meta` 별칭 테이블로 정식명 정규화(compare kb_name 결합 정합 — 값 유실 방지). **`proposal_registry.py`**: 담보명·그룹 19건 동반 개명(245 선례·금액 무변경).
- **`src/lib/coverageAfterDisplayCache.ts`**: `GROUP_ORDER` 신 대분류로 교체 — CoverageRemodel.tsx는 이 상수를 소비하므로 화면 그룹핑·수동담보 select 자동 정합(직접 수정 0).

### ★총액 대사 (실 PDF 5건 — HEAD(235cb09) 코드 대비 · 차이 0 필수)

| 케이스 | 구총합(HEAD) | 신총합 | 배타 차감 | ★대사차이(구−신−차감) | 월납/부값 |
| --- | --- | --- | --- | --- | --- |
| A | 1,493,510,000 | 1,373,510,000 | 120,000,000(일반사망 상해+질병 셀) | **0** | 불변 |
| B | 604,560,000 | 604,560,000 | 0 | **0** | 불변 |
| C | 1,670,740,000 | 1,670,740,000 | 0 | **0** | 불변 |
| D | 1,783,820,000 | 1,733,820,000 | 50,000,000(중입자→표적 셀) | **0** | 불변 |
| E | 1,542,990,000 | 1,542,990,000 | 0(overview — 계약별 셀 없음) | **0** | 불변 |

- 차감 = 동일 담보의 매트릭스 중복 반영 해소분(제1원칙의 "이중 계상 0" 증명 항). A: 일반사망 6,000만이 상해·질병사망 셀에 각 6,000만 → 차감 후 일반사망 단일 행 6,000만. D: 표적 셀 1.1억 = 표적 6,000만 + 중입자 5,000만으로 상호배타 분리.
- D 재해사망 1억: 계약 idx 미해석('?')·지급사유 근거는 있으나 매트릭스 셀 대응 불가 → **승격 보류, `재해사망(계약 미확인)` 기타 보존**(이중 계상 0 우선 — Human 증권 대조 확인 항목).

### 단계 파생·Y/N (실 PDF 5건 신 산출)

- A stages: 암 7,720만 / 뇌초 1억20만·중 9,020만·말 6,520만 / 심초 1억1,520만·중 1억1,520만·말 1억20만 — Y/N: 운전자 Y·자부치 N·가일배 Y·상해실손 Y·질병실손 Y
- B: 암 1억6,820만 / 뇌초·중 2,720만·말 2,220만 / 심장 4,220만×3 — 운전자 N(원천 3종 미가입)·자부치 Y
- C: 암 2,220만 / 뇌초·중 3,020만·말 20만 / 심장 3,020만×3 — 전 Y/N 산출 정상
- D: 암 2억5,620만 / 뇌초 5,520만·중·말 3,520만 / 심초·중 6,620만·말 4,120만 — 전부 Y
- E: 암 2억8,590만 / 뇌초 6,080만·중·말 2,080만 / 심초·중 6,080만·말 2,080만 — 전부 Y
- ※D는 원문 종별 분리형(종수술비 버킷 3,410만)이라 단계 수식의 "5종" 가산 미참여(한계 — 파서 무변경 제약, 기록).

### 항목별 신 집계값 (실 PDF 5건 — 본표/기타 전량, 값 검증용 발췌)

- **A**(40행): 일반사망 6,000만(승격) · 질병사망 2억1,661만 · 상해사망 4억5,261만(차감 후) · 상해후유장해 1.1억 · 암진단금 4,000만 · 순환계 5,000만 · 일반종수술 1~5종(표준환산) 40만~2,000만 · Y/N 원천 9행 금액 보존 / 기타 8행(80%이상 1,100만·화상류·장기이식 등)
- **B**(29행): 표적항암치료 7,000만 · 항암약물방사선 500만 · 응급실 4만 · 깁스 80만 / 기타 8행
- **C**(28행): 교통사고처리지원금 5억 · 상해후유장해 7.4억 / 기타 9행(6주미만 1,000만 포함)
- **D**(39행): 중입자 5,000만 · 표적 7,000만(차감 후) · 종수술비 3,410만(원문 분리형 버킷) / 기타 9행(재해사망(계약 미확인) 1억 포함)
- **E**(42행): 일반종수술 1~5종 55만~2,000만 + 종수술비 1,830만 · 중입자 5,000만 · 표적 5,000만(overview) / 기타 8행

### 지시 7 — A 항암약물방사선 100만×2행 판정 (원문 대조 완료)

- 실측: page16(교보생명 계약) 상세에서 **행번호 7·8이 다른 별개 담보 행** — `7 정액 항암치료특약 … 100만` / `8 정액 항암치료특약 … 100만`(동명 특약 2건). 사망류의 "동일 행이 지급사유만 바뀌어 재등장"하는 형태가 아니라 KB 목록상 독립 행번호를 가진다.
- **판정: 별개 담보 → 현행 합산(200만) 유지**(지시 7의 "별개면 유지" 분기). dedup 확장 불필요.

### 관찰 (스펙 밖 — 기록만)

- E overview 표적항암치료 5,000만 vs E 중입자 5,000만: E는 계약별 셀이 없어(overview) 배타 차감 불가. E 중입자의 KB 분류는 "기타 인보험(정액)담보"(고액항암 아님)라 overview 표적 값과 별개일 가능성이 높으나 **원문만으로 확증 불가** — 금액 우연 일치 여부 Human 증권 대조 권장(잠재 이중 표시 5,000만).
- D 재해사망 1억 — 위 대사 절 참조(계약 미확인 보존, Human 확인 항목).
- 화면: CoverageRemodel.tsx 직접 수정 0(GROUP_ORDER 상수 소비 구조라 자동 정합). 기능(병기·배지·계피/납만기·수동담보) 회귀는 npm test 79(라우트 스모크 포함)로 확인 — 247(화면 완성)에서 신 항목 전용 UI(Y/N 표기·단계 블록) 반영 예정.

## 검증 체크리스트 (1차 — Code, 2026-07-25)

- [x] backend pytest — **703 passed, 8 skipped**(기준선 694 + 신규 9). 갱신 테스트 10파일 전부 근거 주석(개명·재귀속 — 값 변경은 대사로 증명).
- [x] tsc app/node · lint · npm test **79** · build **343.22 kB**(불변 — ±10% 이내)
- [x] ★총액 대사 5건 **차이 0**(위 표) / ★전=후 동일성(해지·신규 0 → coverages·프리미엄·stages·Y/N 전부 일치 — test_taxonomy_246) / 월납·병기 부값 5건 불변
- [x] 238 estimated 필드 생존(test_estimated_flag_survives + 실 PDF A·E 환산 행 확인)
- [x] PII grep 0. diff 범위 = coverage 7파일 + coverageAfterDisplayCache.ts + 테스트 11파일 + harness. `backend/pipeline/`·`supabase/` diff **0**.

---

## 회송 보정 (2026-07-25 · Codex 반려 → Claude Code)

### 반려 원인
- E(overview·239 합계-only 변형)에서 [후] 이월 파손: `apply_consulting_plan`이 합계-only 보장행
  (`summary`만 있고 `by_company={}`)을 빈 계약 셀로 재집계 → 해지 0·신규 0에서도 26행 미가입
  소실, 총합 1,542,990,000 → 142,750,000(−1,400,240,000), Y/N 전부 N, 단계 감소.
  합성 246 테스트에 overview 행이 없어 미검출.

### 해소 방법 (239 가드 방식 — 표준 경로 무변경)
- `aggregator.build_before`: overview 유래 표준 행에 `overview: True` 표식 추가(표준 매트릭스
  행은 플래그 없음 → 기존 경로 그대로).
- `consulting.apply_consulting_plan`: `overview` 행은 ★합계 수준(summary·enrolled) 그대로
  이월(해지 체크 불가 행 = 유지 간주 — 246 회송 보정 원칙). **보존+경고 정책 명시**: 해지
  요청이 있는데 overview 행이 존재하면 "합계형 문서는 해지를 보장 합계에 반영할 수 없다"
  warning을 추가(보험료 합계는 유지 계약 기준 재계산 — 정직 표기).
- ★Codex 워킹트리 최소 보정 3건 보존(되돌리지 않음): ① consulting `_paid_total` 일시납 1회
  금액(빌드 규칙 정합) ② test_179 암진단금 정확값(1억) ③ 246 전=후 테스트 의미 비교 강화
  (비어 있지 않은 계약값·`?` 키·estimated 포함).

### 회귀 고정 (test_taxonomy_246 +2)
- `test_after_equals_before_for_overview_case`: overview 합성(합계-only 9행 + 계약 키 EXTRA 2행)
  — 해지 0·신규 0 → 의미 비교 완전 동일·행수 동일·총합/프리미엄/단계/Y/N 동일.
- `test_overview_rows_survive_cancel_with_warning`: 해지 시 overview 행 [전] 수준 보존 + 경고,
  계약 키 EXTRA는 정상 해지 반영(깁스 소멸·중입자 이월), 보험료 재계산.

### 검증 (1차 재실행 · 2026-07-25)
- [x] backend pytest — **705 passed, 8 skipped**(반려 시점 703 + overview 회귀 2. Codex 보정 3건 포함).
- [x] ★전=후 동일성 실 PDF 5건 전부 OK(의미 비교·행수·총합·월납·Y/N·단계):
      A 54행·1,373,510,000 / B 46행·604,560,000 / C 48행·1,670,740,000 / D 48행·1,733,820,000 /
      **E 54행·1,542,990,000(반려 결함 해소 — 소실 0)**.
- [x] ★총액 대사 5건 차이 0 재확인([전] 기준 기존 결과 불변 — 차감 A 1.2억·D 5,000만·B/C/E 0).
- [x] tsc app/node · lint · npm test **79** · build **343.22 kB**.
- [x] PII 0 · pipeline/supabase diff 0 · diff 범위 = 기존 246 선언 + aggregator·consulting·test_taxonomy_246(보정 파일)만.

---

## 2차 검증 반려 (Codex · 2026-07-25)

- 실 PDF A~E 총액 대사·월납/부값·A/E estimated·D `?` 이월은 통과했다.
- Codex 최소 보정: test_179 암진단금 정확값 assertion, 246 전=후 의미론적 전체행/`?`/estimated assertion, consulting 일시납 total 1회 금액 유지. targeted 190/186/211 포함 30 passed.
- ★차단: E overview(239 합계-only, by_company 없음)는 해지 0·신규 0에서도 `apply_consulting_plan` 재집계로 26개 합계행이 소실된다. 총합 1,542,990,000→142,750,000, Y/N 전부 N, 단계 합계 감소.
- Code 필수 보정: overview summary-only 행의 무해지 이월 보존 + 계약별 귀속 없는 해지 요청의 보존/warning 정책 명시, matrix/overview 전=후 회귀를 각각 추가하고 전체 게이트 재실행. 해결 전 stage·commit·push 금지.

---

## 2차 재검증 통과 (Codex · 2026-07-25)

- [x] 반려 해소: 합성 overview 회귀 11 passed. 실 PDF E [후] **54행·1,542,990,000원**, overview 26행·1,400,240,000원 보존, 월납·Y/N·단계 [전]=[후].
- [x] 보존+경고: overview+해지 요청 시 보장 합계 보존·경고, 보험료는 유지 계약 기준 재계산, 계약 키 EXTRA는 정상 해지.
- [x] 표준 A~D overview 플래그 0·기존 매트릭스 경로 유지, [후] 총합 A 1,373,510,000 / B 604,560,000 / C 1,670,740,000 / D 1,733,820,000 및 전 계약값·premium·Y/N·단계 동일.
- [x] backend **705 passed, 8 skipped**. tsc app/node·lint·frontend **79 passed**·build **343.22 kB**(gzip 101.81 kB).
- [x] `235cb09` 격리 대조: 구총합−신총합−차감 5건 모두 0(A 1.2억·D 5,000만·B/C/E 0), 월납·납입완료 제외 부값 불변. 238 estimated A/E 각 5행, D `?` 이월 생존.
- [x] 비분양식 원본: 금액행 35+Y/N 5 순서가 코드 투영과 1:1, GROUP13 11·KB_COVERAGES 40, 시트3 I5:I11 수식 일치·H10 정정·K7 중복 미이식. ★Y/N 45~49는 OOXML shared formula 5개로, 모두 COUNTA 등가(이전 “47~49 고정 N” 오독 정정).
- [x] 범위 22파일·PII 0·실 PDF/엑셀 tracked 0·pipeline/migrations/package diff 0·runtime SURIT/구브랜드 색상 0·`git diff --check` 통과.
