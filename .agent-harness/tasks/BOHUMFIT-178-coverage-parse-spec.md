# BOHUMFIT-178 — KB 보장분석 제안서 파싱 규칙 명세 (조사 전용)

Owner flow: Claude Chat -> Claude Cowork -> Codex
Current owner: Cowork (조사·명세 완료) -> Codex (문서만 stage·commit·push)

> 이 파일은 태스크 **패킷(지시)**이다. 실제 산출 명세는 동일 폴더의
> **`BOHUMFIT-178-parse-spec.md`** 이다. 이 태스크는 코드 0줄 — `.agent-harness` 문서만 산출한다.

## 배경 (Human 확정)
160 보장분석 통합의 실체 = KB 신정원 "보장분석 제안서" PDF를 파싱해 설계사용 리모델링
보장분석표 2종([전]회사별 세부 / [최종]합산)을 자동 생성하는 기능. 이 태스크는 파싱 규칙
명세서만 산출한다(후속 179 백엔드·180 프런트·181 전후비교의 설계 기반).

## 산출물 (코드 0)
`.agent-harness/tasks/BOHUMFIT-178-parse-spec.md` 단일 명세 문서.

## ★수정 금지 (절대)
- src/·backend/ 하위 모든 코드 (조사 전용 — `.agent-harness` 문서만 작성)
- 고지의무 분석 파이프라인 일체

## 입력 자료
Human 제공 KB 신정원 보장분석 제안서 샘플 — `문건주님 kb보장분석 제안서.pdf` (30p, OZ Report Viewer 생성, A4).
※ 이상범님 자료는 시점 오류로 폐기 — 문건주 자료 단일 기준.

## 확정된 규칙 (Human 결정 — 명세에 반영)
1. 파싱 방식 = **규칙 기반**(좌표·텍스트 패턴). AI 파싱 미사용. KB 신정원 표준 양식 전용부터 시작.
2. [전]표 회사 = 가입한 회사 **전부**, **월보험료 내림차순** 정렬.
3. [전]표 좌측 요약열 = [최종]표 단일 컬럼 = 담보별 집계값.
   · 정액 담보(진단비·수술비·사망·후유장해 등) = 회사 **합산**
   · 중복보상 담보(실손의료비·입원일당 등) = **대표값(합산 제외)**
   · 어느 담보가 합산/대표값인지 전체 분류표를 명세에서 확정.
4. 총 납입액 = **계약별(월납 × 납입개월수) 전부 합산**. 납입기간을 p5에서 파싱. 단순 ×240 폐기. 월납 합계도 별도 산출.

## Step (요약)
- Step 1: p5·p6~7·p20 텍스트 레이어 패턴 문서화 + 페이지 역할 식별
- Step 2: 37 표준담보 → 11 대분류 매핑표 + 합산/대표값 구분 전량 + 문건주 대조
- Step 3: [전]/[최종] JSON 스키마 확정
- Step 4: 엣지케이스·대조 검증표·후속(179/180/181) 분해

## 명세 완료 조건
- [x] p5·p6~7·p20 텍스트 패턴 문서화
- [x] 담보 37개 → 11대분류 매핑표 + 합산/대표값 구분 전량
- [x] [전]/[최종] JSON 스키마 확정
- [x] 문건주 자료 대조 검증표 (규칙→실값 일치 확인)
- [x] 후속 태스크 분해 (179 백엔드·180 프런트·181 전후비교)
- [x] Human 결정 필요 항목 flag (입원일당 합산/대표 충돌 등)

## Stage 예정 (Codex — 문서만)
- .agent-harness/tasks/BOHUMFIT-178-coverage-parse-spec.md
- .agent-harness/tasks/BOHUMFIT-178-parse-spec.md
- .agent-harness/handoff.md, .agent-harness/locks.md

## 커밋 메시지 (Codex)
docs(BOHUMFIT-178): KB 보장분석 제안서 파싱 규칙 명세
