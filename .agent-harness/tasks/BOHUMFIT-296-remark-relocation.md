# BOHUMFIT-296 — 비고 정규 행 이관 · N대수술비 신설 52행 · 2열 라벨 정리 (★Human Q6 재승인 / Codex 검증 완료)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human | Current owner: **Codex**(Human Q6 재승인 반영·기준값 갱신·커밋)
기준 HEAD `3ec464b`(295 커밋). 298 미커밋분 위(프런트 무접촉). git 쓰기 0 · 실 PDF stage 0 · PII 0.

## ★구현 완료 (주간)
야간이 실측·계획·스펙 충돌을 남겼고(아래 원본 유지), 주간에 Human이 스펙 충돌을 해소(남는 비고 7항목=고액암·경증치매진단 포함)해 구현했다.
**백엔드 전용** — 프런트는 서버 payload를 그대로 렌더하고, 종합비교 17키(298)는 신규 행이 케스케이드 밖이라 무영향.

- **스키마 52행**(49 + N대수술비 최대 보상금액·3대비급여실손·교통사고처리지원금(6주미만)) · 대분류 11 불변.
- **N대수술비** → 정규 행 major_n_surgery(수 술) · N값 병기 폐기 · **여러 건이면 최대 보상금액(max)**(파서 REP).
- **3대비급여실손**(실비 상단·rep)·**교통사고처리지원금(6주미만)**(형사합의금 하단·sum) 정규 행 이관 · 비고 제거.
- **2열 병기 (좌 | 우) 라벨·`질병|상해` 헤더 행 제거**(292 F 구조물) · `track_row_of` 단순화 · 간병인 질병|상해 유지 · fitToWidth=1.

## ★★Human 확인·결정
1. **행 수 52 (패킷 "50"과 불일치)** — 3대비급여실손·6주미만이 현행 상수에 **자리가 없어**(전부 appendix) 신규 행이 됐다.
   패킷 "**없으면 행 신설 필요 여부 판단·기록**" 조항을 적용해 3행 신설로 판단. 아래 대조표에서 확인.
2. **종수술비(합성) 비고 유지** — ★Step 6 선결 확인: 종수술비 값이 **1~5종에 반영 안 됨**(정본B tier 전부 빈칸인데 종수술비 21.5M).
   삭제 시 정보 손실 → 패킷 "반영 안 됐으면 삭제하지 말고 기록" 조항대로 **삭제하지 않았다**. → "남는 비고 정확히 7항목"은
   종수술비가 있는 문서에서 8항목이 된다(정본B·C). Human 확인: 종수술비를 1~5종과 별개로 비고 존치할지.
3. **6종 이상 담보** — 1~5종만 매칭(현행 유지). 6종 이상은 무시·비고 미포함. ★4문서 관측 **0** — 전수 확인은 실 데이터 한계로 **확인 불가**(추가 문서 확보 시 재확인).

## ★★Q6 대조표 (296b 정정 · 4문서 · 익명 · 행 단위 사유) — Human 재승인 완료
★**유일한 값 변화 = N대수술비 sum→max**. 이관(3대비급여·6주미만)은 대분류 합계에 값이 **보존**된다(총액 불변). 나머지는 구조(행 위치·비고 멤버십)만.

| 문서 | 고정행 | 원시 N행 수 | N대수술비 전→후 | 3대비급여 | 6주미만 | 총액 변화 | 사유 |
|---|---|---|---|---|---|---|---|
| 정본A(표준형) | 49→52 | 1건 | 2M→2M | 없음 | 없음 | **불변** | 단건이라 sum=max |
| 정본B(overview형) | 49→52 | **8건** | **2M→1M** | 9M 이관 | 10M 이관 | **−1,000,000** | 복수 N행 sum→max. 이관 19M은 값 보존 |
| 정본C(표준형) | 49→52 | **5건** | **21.7M→10M** | 없음 | 없음 | **−11,700,000** | 복수 N행 sum→max |
| 정본D(표준형) | 49→52 | **9건** | **5M→1M** | 없음 | 없음 | **−4,000,000** | 복수 N행 sum→max |

### ★296b 오류 원인 규명
- 기존 표의 C `10M`·D `1M`은 **배선 후 max 결과**와 정확히 같고, 배선 전 sum 스냅샷(C 21.7M·D 5M)이 문서·테스트 어디에도 없었다. 즉 C·D는 HEAD 전후를 실행 대조하지 않고 후값을 전값으로도 간주했다.
- `extra["N대수술비"]["n_values"]`는 parser가 `if n not in values`로 만든 **N 종류 중복 제거 목록**이다. C `[119]`, D `[2, 142]`는 담보 건수가 아니라 N 종류 수인데, 기존 표가 이를 단건 판단 근거로 사용했다. 실제 상세 원시 행은 C 5건·D 9건이다.
- 296b 자동 검산은 원시 상세 행을 직접 세고 `legacy_total − current_total == raw N sum − current N max`를 A~D 전부 단언한다. 3대비급여·6주미만의 전후 값은 동일하고, 2열 헤더 제거는 payload 값에 영향 0이다.
- 판정은 **Step 2(a)**: 제품 코드 추가 수정 없음. Q6 표만 정정하고 B/C/D 총액 변화를 Human에게 재승인 요청한다.

**smoke 예상 FAIL 2건**(기준값 미갱신 상태 · 대조표와 일치):
- 표준(정본A): `coverageRows` 57→**59**(N대수술비 appendix→정규 행 + 신규 정규 행 노출).
- overview(정본B): `total` 1,501,690,000→**1,500,690,000**(N대수술비 sum→max −1,000,000).
★smoke는 정본 A·B만 실행하므로 예상 FAIL은 위 2건 그대로다. C·D 변화는 296b 로컬 정본 검산에 별도 고정했다.
★사유 없는 변화 0. Human 재승인에 따라 Codex가 `scripts/smoke-coverage.mjs` BASELINE·verify.md를 갱신했고 smoke가 통과했다.

## 게이트 (1차 · 주간)
- [x] 스키마 52행·대분류 11 · N대수술비 위치(종수술 하단·뇌혈관 상단)·3대비급여 실비 상단·6주미만 형사합의금 하단
- [x] ★2열 병기 라벨 셀·헤더 행 제거 · 인쇄 폭 fitToWidth=1 · 간병인 질병|상해 유지
- [x] 3대비급여실손·6주미만 정규 행 표시 · 비고 제거(잔존 0) · N대수술비 max
- [x] ★종수술비 비고 유지(값 손실 0 — 1~5종 미반영 확인) · 고액암·경증치매진단 비고 유지
- [x] ★대분류 합계 정합 · 이관 전후 이중 계상 0(296b에서 정본 A~D 원시 N행·이관분 자동 검산까지 보강)
- [x] `cd backend && python -m pytest -q` — **1065 passed, 8 skipped**(1058 + 신규 296 테스트 7 · 회귀 0)
- [x] `npm test` **413/42** 무변경(296 백엔드 전용) · tsc app/node · lint 무경고
- [x] ★`npm run smoke:coverage` — Human 재승인 기준값 갱신 후 **PASS**
- [x] 케스케이드 상수·`compute_stage_totals`·`backend/pipeline/`·`filters.py`·`vite.config` diff 0 · 295/298 수정분 무접촉
- [x] `build:verify` 예상 FAIL(248)

## 기존 테스트 갱신 — 9파일(전부 52행·N대수술비 max·2열 헤더 제거의 직접 귀결)
287·289(행 수 52·순서 리스트 3행 삽입·NEW_ROWS 21·APPENDIX 5) · 272(52) · 183(N대수술비 정규 행) ·
179b·237(N대수술비 max·정규 행·N병기 폐기) · 291(수기 대조 296 3행 제외) · 292 F·293(2열 헤더 제거) · 신설 `test_remark_relocation_296.py` 7건.

## Stage 목록 (Codex용 · 승인 후)
`backend/coverage/constants.py`·`aggregator.py`·`parser.py`·`export_excel.py` · 테스트 9파일 + 신설 `test_remark_relocation_296.py` ·
(★Human 승인 후) `scripts/smoke-coverage.mjs`·`verify.md` BASELINE 갱신 · `tasks/BOHUMFIT-296-*.md`·handoff·locks.
※제외: 실 PDF·PII·산출물

## 커밋 메시지 (Codex용)
feat(BOHUMFIT-296): 비고 정규 행 이관·N대수술비 신설·2열 라벨 정리(52행·smoke 기준값 갱신 — Human Q6 재승인)

---

# BOHUMFIT-296 — 비고 정규 행 이관 · N대수술비 행 신설 · 2열 라벨 정리 (★실측·계획 / 중단)

Owner flow: Claude Chat -> Claude Code -> Codex -> Human
Current owner: **Human**(★스펙 충돌 결정 1건) → 결정 후 **Claude Code**(주간 구현) → Codex
Risk tier: 고 — 49→50 값 계층 스키마 변경 · smoke 기준값 변화 · 29파일 테스트 blast radius. git 쓰기 0.
Date: 2026-08-18 · ★야간 순차 2/4 · 기준 HEAD `59fca67`(295/295b/298 미커밋분 위)

## ★★중단 판정 (코드 변경 0)
아래 세 가지가 겹쳐, **야간 무인 세션에서 구현하지 않고 실측·계획·스펙 충돌을 문서화**한다.
1. **49→50 스키마 변경** — `KB_COVERAGES_V2`에 행 추가 + 비고 2건을 정규 행으로 이관하면 `ROW_INDEX`가 밀려
   export 레이아웃·smoke 프로브 좌표·값 대조가 전부 바뀐다. **테스트 blast radius 29파일**(291급 풀세션 규모).
2. **값 계층·smoke 기준값 변화** — 비고 이관은 대분류 합계에 값을 더한다(현재 비고는 합계 제외). 정본 총액·귀속률·
   행수가 바뀌므로 **Q6 절차(대조표 → Human 눈 승인 → Codex 기준값 갱신) 필수**. 무인 세션에서 완료 불가.
3. **★스펙 충돌(추측 금지)** — 패킷의 "남는 비고" 목록이 현행 비고 2건을 누락(§4). Human 결정 없이는 진행 불가.

★따라서 야간 순차 규칙("중단 판정 시 뒤 태스크 시작 금지")에 따라 **297·299도 시작하지 않았다**(사유 handoff 기록).

---

## Step 1 — 실측 (완료 · 코드 무변경)

### 1-1. 삽입·이관 지점 (현행 49행 실측)
| 대상 | 현행 | 목표 위치 | row_id 삽입 |
|---|---|---|---|
| **N대수술비 최대 보상금액**(신규) | 비고(동적) | 종수술 5종[8] 하단·뇌혈관 수술비[9] 상단 | index **9** |
| `3대비급여실손` | 비고(APPENDIX) | 실비 대분류 상단(actual_inpatient[0] 위) | index **0** |
| `교통사고처리지원금(6주미만)` | 비고(APPENDIX) | 운전자 형사합의금(driver_settlement[45]) 하단 | index **46** |
| `종수술비`(합성 라벨) | 비고(동적) | **삭제**(1~5종이 대체) | — |

### 1-2. 이관 대상 현행 `resolve()` (전부 appendix)
`3대비급여실손`·`교통사고처리지원금(6주미만)`·`종수술비`·`N대수술비` → 모두 `kind='appendix'`. 정규 행으로 가려면
`v2_mapping`에 KIND_ROW 매핑을 신설해야 한다(현재 자리가 없다 — 행 신설 필요).

### 1-3. 실 문서 실재 확인 (익명 · 라벨만 · 값 미기록)
4문서 비고에서 관측: `N대수술비(32대)`·`N대수술비(124대)`·`N대수술비(119대)`·`N대수술비(2·142대)`·`3대비급여실손`·
`교통사고처리지원금(6주미만)`·`종수술비`. → 이관 대상·N대수술비 신설이 **실 데이터로 뒷받침**된다.
★N값이 `2·142대`처럼 복수인 케이스 존재 → 신규 행은 **최대 보상금액**(여러 건이면 max) 규칙이 필요(패킷 확정과 일치).

### 1-4. 2열 헤더 구조물(291 F · 292 F) 위치
`export_excel.py`: `DUAL_HEADER_BEFORE="tier_surgery_1"` · `DUAL_HEADER_ROW` · `track_row_of()`(헤더 삽입으로 +1 밀림) ·
`_sheet_track`이 종수술 위에 `2열 병기 (좌 | 우)` 라벨 행 삽입. → 패킷은 이 **헤더 행·라벨 셀 제거**를 지시(행명이 이미 열 순서 표현).
제거 시 `track_row_of` 단순화(`DATA_ROW0 + ROW_INDEX`) + smoke 프로브·layout 헬퍼·291/292 F 테스트 갱신 필요.

### 1-5. blast radius (실측)
49/50·ROW_INDEX·layout·2열 참조 테스트 = **29파일**(excel_v2_layout·248·249·250·252·254·259·261·272·287·289·290·291·
292·293·295 + 파서/그룹 계열). 291과 동급.

---

## Step 2~8 — 구현 계획 (Human 결정 후 주간 실행)

### ①스키마(constants.py) — 49→**50행**
- 신규 `CoverageRowV2("major_n_surgery", "수 술", "N대수술비 최대 보상금액")` index 9(뇌혈관 수술비 앞). 2열 아님·케스케이드 없음·독립.
- `3대비급여실손`·`교통사고처리지원금(6주미만)`을 **정규 행으로 신설**(실비 상단·운전자 형사합의금 하단). row_id 안정 키.
- `APPENDIX_ITEMS_V2`에서 `3대비급여실손` 제거. ★`고액암`·`경증치매진단`은 §4 결정 대기.
- `sum_excluded`·`dual_column` 무관(신규 3행 모두 일반 행).

### ②매핑(v2_mapping.py) — `N대수술비`(N값 무관 패턴)→major_n_surgery · 3대비급여실손·6주미만→신규 정규 행.
### ③2열 라벨 제거(export_excel.py) — `2열 병기 (좌 | 우)` 라벨 행·헤더 행 미삽입 · `track_row_of` 단순화 · 병합·fitToWidth=1 유지.
### ④매칭(parser/aggregator) — `N대수술비(124대)`·`(2·142대)` 등 → major_n_surgery, 여러 건 **max**(근거 주석).
### ⑤이관 검산 — 대분류 합계 = 이관 전 비고값이 정규 행으로 이동(값 손실·이중 계상 0). ★대조표 필수.
### ⑥종수술비 비고 삭제 — ★**선결 확인**: 종수술비 합성 라벨 값이 1~5종에 이미 반영됐는지. 반영 안 됐으면 삭제 금지·기록(238 환산과의 관계).
### ⑦6종 이상 무시 — 1~5종까지만, 6종 이상은 비고에도 안 넣음. ★무시된 담보 목록 기록(실재 확인). 현행 관측엔 6종+ 없음(추가 확인 필요).
### ⑧export 2종 반영 · 4문서 실렌더 · smoke 대조표.

---

## ★★Human 결정 필요 (구현 선결)
1. **`고액암`·`경증치매진단` 처분** — 패킷 "남는 비고: 장기요양간병비·보철치료비·화재벌금·양성종양폴립·응급실"에
   이 2건이 **없다**. 그러나 현행 `APPENDIX_ITEMS_V2`엔 있고, 이관/삭제 지시가 없다.
   · (a) 목록이 illustrative → **현행 유지**(비고 존치 · 누락 0 · 276a 원칙) ← Claude Code 권장(정보 손실 0)
   · (b) 목록이 exhaustive → 2건을 이관/삭제해야 하나 **행선지 미지정** → 추측 금지로 진행 불가
   → ★어느 쪽인지 확정 필요. (a)면 즉시 구현 가능, (b)면 행선지 지정 필요.
2. **smoke 기준값 변화 승인(Q6)** — 비고 2건 정규 행 이관 + N대수술비 신설로 정본 총액·행수·귀속률이 바뀐다.
   구현 후 대조표를 만들어 Human 눈 승인 → Codex가 기준값 갱신(이 태스크에서 갱신 금지).

## 확인 불가 (야간)
- 종수술비 값이 1~5종에 반영됐는지(⑥ 선결) — 구현 착수 시 값 대조로 확인. · 6종 이상 담보 실재(⑦) — 현재 관측 0, 전수 확인 필요.

## Stage 목록 (구현 시 · Codex용)
`constants.py`·`v2_mapping.py`·`aggregator.py`·`parser.py`(N대수술비 매칭)·`export_excel.py`·`export_pdf.py`·
`scripts/smoke-coverage.mjs`·테스트 ~29파일·프런트 표시(298 이후)·`tasks/BOHUMFIT-296-*.md`·verify.md·handoff·locks.
※이번 야간 세션 산출 = **이 문서(실측·계획·중단)뿐 · 코드 diff 0**.

## 커밋 메시지 (구현 시 · Codex용)
feat(BOHUMFIT-296): 비고 정규 행 이관·N대수술비 신설·2열 라벨 정리(1~5종 고정)
